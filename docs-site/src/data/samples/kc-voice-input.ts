// Sample data for <kc-voice-input>.
//
// `transcribe` is a function-valued JS property — it cannot be serialised as
// static data, so there are no non-scalar props to seed here.  The Playground
// and Examples render the mic button as-is; the Usage section explains how to
// wire up `transcribe` in your own code.
//
// lib/sample-data.ts auto-aggregates every src/data/samples/*.ts via glob.

export default {
  sample: {},
};
