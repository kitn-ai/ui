// Sample data for <kc-empty>.
//
// Both props on kc-empty — `emptyTitle` and `description` — are scalar:true,
// so there are no non-scalar props to seed here. The playground and examples
// drive everything via the `config` prop on <Example> / <Playground>.
//
// lib/sample-data.ts auto-aggregates every src/data/samples/*.ts via glob, so
// adding this file is all that is needed.

export default {
  sample: {},
};
