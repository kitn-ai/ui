// Sample data for <kc-thinking-bar>.
//
// All three props (text, stoppable, stopLabel) are scalar:true — they are set
// as HTML attributes or boolean properties, not JS objects. There are no
// non-scalar props, so `sample` is empty and no `named` sets are needed.
//
// lib/sample-data.ts auto-aggregates every src/data/samples/*.ts via glob, so
// adding this file never touches any shared file.

export default {
  sample: {},
};
