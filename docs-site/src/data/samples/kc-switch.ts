// Sample data for <kc-switch> — a leaf toggle with only scalar props
// (`checked`, `disabled`, `label`), all driven by the Playground controls.
// A compact preview height keeps the control centered rather than full-bleed.
export default {
  sample: { previewHeight: '140px' },
};
