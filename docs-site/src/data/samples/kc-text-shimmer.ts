// Sample data for <kc-text-shimmer>.
//
// All props are scalar (string / number attributes), so there are no non-scalar
// props to seed here. The playground and bare examples drive every value through
// config={} on <Example>. This file satisfies the sample-data auto-aggregator.
//
// `sample`  = empty (no non-scalar props)
// `named`   = not required (no non-scalar alternate sets needed)

export default {
  sample: {},
};
