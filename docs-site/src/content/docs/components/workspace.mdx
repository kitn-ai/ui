---
title: Workspace
description: A full chat shell as a single web component — resizable sidebar, conversation list, and message thread, all isolated in Shadow DOM.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kc-tag-sub">kc-workspace</p>

<p class="kc-lede">The entire chat application shell as one tag — collapsible drag-resizable sidebar, conversation list, and full message thread, isolated in Shadow DOM and droppable into any framework.</p>

<Facts items={['Shadow DOM', '11 events', 'Drag-resizable sidebar', 'Collapsible sidebar', '26 props']} />

## Preview

<Playground client:only="solid" tag="kc-workspace" />

<Aside type="tip" title="When to use">
Reach for `<kc-workspace>` when you need the whole app shell — **conversation sidebar, history, and collapse** included — dropped in as one tag. If you only need the conversation surface (thread, composer, header), use [`<kc-chat>`](/chat/components/chat/) and add your own controls through its header slots ([Custom chat header](/chat/patterns/custom-chat-header/)). In SolidJS, compose `ConversationList` and `ChatThread` directly for the finest control.
</Aside>

## Usage

Set data in JavaScript and listen for **CustomEvents** on the element:

```html
<kc-workspace id="workspace" style="display:block; height:100vh;"></kc-workspace>

<script type="module">
  import '@kitn.ai/ui/elements';

  const ws = document.getElementById('workspace');

  ws.conversations = [
    {
      id: '1',
      title: 'Web component architecture',
      scope: { type: 'document' },
      messageCount: 12,
      lastMessageAt: '2026-06-16T10:00:00Z',
      updatedAt: '2026-06-16T10:00:00Z',
    },
  ];
  ws.messages = [
    { id: 'm1', role: 'user', content: 'How do I drop the whole chat app in with one tag?' },
    { id: 'm2', role: 'assistant', content: 'Use <kc-workspace> and set the three core properties.' },
  ];
  ws.models = [{ id: 'claude-4-opus', name: 'Claude 4 Opus', provider: 'Anthropic' }];
  ws.activeId = '1';

  ws.addEventListener('kc-conversation-select', (e) => console.log('selected:', e.detail.id));
  ws.addEventListener('kc-submit', (e) => console.log('user sent:', e.detail.value));
  ws.addEventListener('kc-sidebar-toggle', (e) => console.log('collapsed:', e.detail.collapsed));
</script>
```

- **Properties vs attributes** — `groups`, `conversations`, `messages`, `models`, `suggestions`, `slashCommands`, `slashActiveIds`, and `context` are arrays and objects, so set them in JavaScript. Scalar props (`placeholder`, `loading`, sidebar dimensions, etc.) work as attributes or properties.
- **Sidebar** — drag-resizable and collapsible. Constrain with `sidebarWidth` (percent, default `22`), `sidebarMinWidth`, and `sidebarMaxWidth`. Collapse is **uncontrolled by default** — the element manages it and fires `kc-sidebar-toggle`. Start collapsed with the `default-sidebar-collapsed` attribute, or **control** it by setting the `sidebarCollapsed` property and updating it from `kc-sidebar-toggle` (lets you persist the choice or collapse from outside the header).
- **Conversation grouping** — pass a flat `conversations` array and the element auto-buckets by recency, or supply a `groups` array with `groupId` on each conversation for custom grouping.

## Examples

### Default

<Example client:visible tag="kc-workspace" />

### Loading State

Set `loading` while an assistant reply is streaming.

<Example client:visible tag="kc-workspace" config={{ loading: true }} data="loading" />

### Model Switcher

Set the `models` property to surface a model picker; listen for `kc-model-change` to update your store.

<Example client:visible tag="kc-workspace" data="withModels" />

### Starter Suggestions

Set `suggestions` to show prompt chips on an empty thread. `suggestion-mode="submit"` fires `kc-submit`; `"fill"` populates the input and fires `kc-suggestion-click`.

<Example client:visible tag="kc-workspace" data="withSuggestions" />

### Slash Commands

Set `slashCommands` to enable the `/` command palette; handle `kc-slash-select` to receive `{ command }`.

<Example client:visible tag="kc-workspace" data="withSlash" />

### Sidebar Starts Collapsed

A floating reveal button appears at the top-left; `kc-sidebar-toggle` fires on every change.

<Example client:visible tag="kc-workspace" config={{ sidebarCollapsed: true }} data="sidebarCollapsed" />

## Props

<PropTable tag="kc-workspace" />

## Events

<EventTable tag="kc-workspace" />

## Composed from

<ComposedFrom tag="kc-workspace" />

<NeedAHand />
