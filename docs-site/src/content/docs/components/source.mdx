---
title: Source
description: A single citation chip with a hover-card preview — the building block for inline source references below assistant answers.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kc-tag-sub">kc-source</p>

<p class="kc-lede">A citation chip that shows a domain label and reveals a hover card with a headline and description — drop it below an assistant message to let users verify where an answer came from.</p>

<Facts items={['Shadow DOM', 'All props are attributes', 'Optional favicon', 'Hover-card preview', 'No events']} />

## Preview

<Playground client:only="solid" tag="kc-source" />

<Aside type="tip" title="When to use">
Use `<kc-source>` for a **single citation chip** in isolation — inside a custom message template or a tooltip. For two or more sources, reach for `<kc-sources>` instead: it accepts a `sources` array and lays chips out in a wrapping row.
</Aside>

## Usage

All props are plain HTML attributes — no JavaScript needed:

```html
<kc-source
  href="https://kitn.dev"
  headline="kitn — the kit"
  description="Composable SolidJS + web-component chat UI."
  show-favicon
></kc-source>
<script type="module">import '@kitn.ai/chat/elements';</script>
```

- **`href` is required.** The element renders nothing without it.
- **`label`** defaults to the hostname (`kitn.dev`). Pass an explicit label when the domain is ambiguous or too long.
- **`headline`** (not `title`) — `title` is a reserved HTML attribute that conflicts with custom element constructors.
- Clicking the chip opens the URL directly — the element emits no events.

## Examples

### Basic Citation

<Example client:visible tag="kc-source" config={{ href: 'https://kitn.dev', headline: 'kitn — the kit', description: 'Composable SolidJS + web-component chat UI.' }} />

### With Favicon

<Example client:visible tag="kc-source" config={{ href: 'https://solidjs.com', headline: 'SolidJS', description: 'A declarative, efficient, and flexible JavaScript library for building user interfaces.', showFavicon: true }} />

### Custom Label

Override the domain default with `label` when you want a shorter or more recognisable name.

<Example client:visible tag="kc-source" config={{ href: 'https://developer.mozilla.org/en-US/docs/Web/API/Web_components', label: 'MDN', headline: 'Web Components — MDN', description: 'A suite of technologies that let you create reusable custom elements.' }} />

### Full Detail with Favicon

<Example client:visible tag="kc-source" config={{ href: 'https://vitejs.dev', label: 'Vite', headline: 'Vite — Next Generation Frontend Tooling', description: 'An opinionated web build tool that serves your source files over native ES modules during development.', showFavicon: true }} />

## Props

<PropTable tag="kc-source" />

## Composed from

<ComposedFrom tag="kc-source" />

<NeedAHand />
