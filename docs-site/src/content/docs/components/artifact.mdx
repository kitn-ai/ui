---
title: Artifact
description: A sandboxed preview + code viewer for AI-generated artifacts — the Claude Artifacts / ChatGPT Canvas pattern.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kc-tag-sub">kc-artifact</p>

<p class="kc-lede">Show an AI-generated deliverable beside the conversation — a web page, doc, image, or PDF — in a sandboxed iframe with a nav toolbar and a switchable Code view.</p>

<Facts items={['Shadow DOM', 'Sandboxed iframe', 'Preview + Code tabs', 'Configurable toolbar', '4 events']} />

## Preview

<Playground client:only="solid" tag="kc-artifact" />

<Aside type="tip" title="When to use">
Reach for `<kc-artifact>` when a response produces something runnable or viewable you want to show alongside the chat. Pair it with [`<kc-resizable>`](/chat/components/resizable/) for a draggable split-pane shell. It fills its container — give the parent a height.
</Aside>

## Usage

Point `src` at your hosted artifact and set `files` as a JS **property** for the Code tab.

```html
<kc-artifact src="https://your.app/artifacts/abc/index.html" style="display:block;height:520px"></kc-artifact>
<script type="module">
  import '@kitn.ai/chat/elements';
  const el = document.querySelector('kc-artifact');
  el.files = [
    { path: 'index.html', url: '…/index.html', type: 'html', code: '<!DOCTYPE …>' },
    { path: 'styles.css', url: '…/styles.css', type: 'other', language: 'css', code: '…' },
  ];
  el.addEventListener('kc-navigate', (e) => console.log(e.detail.url));
</script>
```

- **`files`** — each entry needs a `path`; add `url` for the preview, `code` + `language` for the Code tab, and `type` (`html | pdf | image | other`). Folders come from the `/` in paths.
- **Toolbar** — hide any default control with its `no-*` flag (`no-nav`, `no-reload`, `no-home`, `no-path-field`, `no-tabs`); `expandable` and `open-in-tab` are opt-in.
- **Sandbox** defaults to `allow-scripts allow-forms`. Widen it only for trusted content.

## Examples

### Code view

<Example client:visible tag="kc-artifact" config={{ tab: 'code', activeFile: 'app.js' }} />

### Preview only

Hide the whole toolbar for a chrome-free embedded tile.

<Example client:visible tag="kc-artifact" config={{ noNav: true, noReload: true, noHome: true, noPathField: true, noTabs: true }} />

### Open in new tab

<Example client:visible tag="kc-artifact" config={{ openInTab: true }} />

### Standalone

Rounded, bordered chrome for an artifact rendered outside a panel — a modal or inline tile.

<Example client:visible tag="kc-artifact" config={{ standalone: true, readonlyPath: true }} />

### Expandable

An expand-to-fill button that fires `kc-maximize-change`, plus the open-in-tab control.

<Example client:visible tag="kc-artifact" config={{ expandable: true, openInTab: true }} />

## Props

<PropTable tag="kc-artifact" />

## Events

<EventTable tag="kc-artifact" />

## Composed from

<ComposedFrom tag="kc-artifact" />

<NeedAHand />
