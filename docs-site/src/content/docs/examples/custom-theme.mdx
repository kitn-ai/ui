---
title: Custom theme
description: Reskin <kc-chat> into a bold, distinctive brand — magenta accent, cyan focus glow, deep-noir surfaces — using only inherited --kc-* tokens, in both light and dark.
---

import CustomThemeShowcaseDemo from '../../../components/CustomThemeShowcaseDemo.tsx';

<p class="kc-lede">This is the **same `<kc-chat>`** that powers every other example, reskinned into "Aurora" — a neon-on-noir identity with a magenta accent, a cyan focus ring, and sharp corners. Nothing pierces the Shadow DOM: a map of inherited `--kc-*` custom properties on the host does all of it. Toggle the site's theme to watch a second, matched palette take over.</p>

<CustomThemeShowcaseDemo client:only="solid" />

## How it works

The `--kc-color-*` and `--kc-radius` tokens are **inherited** CSS custom properties. Set them on the `<kc-chat>` element (or any ancestor) and they propagate into the Shadow DOM through `var()` fallbacks — no stylesheet import, no internal classes touched.

A bold look comes from overriding more than the accent. Aurora moves every surface, the focus ring, the inline-code tint, and the corner radius together so the result reads as a deliberate design, not a recolored default:

```html
<kc-chat id="chat" chat-title="Aurora"></kc-chat>

<script type="module">
  import '@kitn.ai/ui/elements';

  const chat = document.getElementById('chat');

  // Scope the whole Aurora map to this element only.
  const aurora = {
    '--kc-color-background': 'hsl(252 40% 6%)',   // deep-noir surface
    '--kc-color-card':       'hsl(250 36% 10%)',   // message bubbles / panels
    '--kc-color-foreground': 'hsl(250 30% 96%)',
    '--kc-color-primary':    'hsl(322 90% 58%)',   // magenta accent
    '--kc-color-primary-foreground': 'hsl(250 40% 7%)',
    '--kc-color-ring':       'hsl(186 95% 56%)',   // cyan focus glow
    '--kc-color-border':     'hsl(250 30% 20%)',
    '--kc-color-code-foreground': 'hsl(186 90% 66%)', // cyan inline code
    '--kc-radius':           '0.25rem',            // sharp corners
  };
  for (const [token, value] of Object.entries(aurora)) {
    chat.style.setProperty(token, value);
  }

  // Wire messages + kc-submit as usual.
  chat.messages = [];
  chat.addEventListener('kc-submit', (e) => { /* stream reply */ });
</script>
```

The aurora glow and the framed shell around the chat are **host-side chrome** — plain CSS on a wrapper that sits outside the element. The component never knows it's there.

### Cover both modes

A single token value applies in whichever mode the page is in, so a theme that's intentional in both needs **separate maps** — one for light, one for dark. Each is self-contained, with the accent and ring darkened for light mode to keep text and focus at WCAG AA:

```css
/* dark — neon on near-black indigo */
.dark kc-chat {
  --kc-color-background: hsl(252 40% 6%);
  --kc-color-card:       hsl(250 36% 10%);
  --kc-color-primary:    hsl(322 90% 58%);
  --kc-color-ring:       hsl(186 95% 56%);
  --kc-color-code-foreground: hsl(186 90% 66%);
}

/* light — same identity, inverted onto a cool studio white */
kc-chat {
  --kc-color-background: hsl(240 40% 98%);
  --kc-color-card:       hsl(0 0% 100%);
  --kc-color-primary:    hsl(322 82% 46%);   /* darker → AA on light */
  --kc-color-ring:       hsl(192 90% 38%);   /* darker → visible on light */
  --kc-color-code-foreground: hsl(192 90% 32%);
}
```

The demo above sets these from JavaScript, re-applying the matching map whenever the page's theme flips — handy when your overrides live in code rather than a stylesheet.

## Next steps

- **[Theming guide](/chat/guides/theming/)** — the full token reference, the `--kc-color-*` alias system, scoped subtree theming, and dark-mode setup.
- **[Drop-in chat](/chat/examples/drop-in-chat/)** — the streaming loop that backs any `<kc-chat>` instance.
- **[`kc-chat` reference](/chat/components/chat/)** — `proseSize`, `codeTheme`, and `codeHighlight` for non-color appearance control.
