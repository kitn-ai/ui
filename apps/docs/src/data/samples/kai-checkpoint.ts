// Sample data for <kai-checkpoint>.
//
// All props on kai-checkpoint are scalar (attributes), so there are no non-scalar
// props to seed here. The element renders from attributes alone — the Playground
// and Examples configure it entirely via `config`.
//
// lib/sample-data.ts auto-aggregates every src/data/samples/*.ts via glob.

export default {
  sample: {},
};
