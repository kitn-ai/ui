// Sample data for <kai-file-upload>.
//
// All four props (multiple, accept, disabled, label) are scalar — they map to
// HTML attributes / boolean properties and are passed directly via <Example config={}>.
// There are no non-scalar props, so `sample` is empty and no `named` sets are needed.
// lib/sample-data.ts auto-aggregates every src/data/samples/*.ts via glob.

export default {
  sample: {},
};
