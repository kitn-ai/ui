// Sample data for <kai-scroll-button>.
//
// All three props (for, variant, size) are scalar:true — they are set as
// plain HTML attributes, not JS properties. There are no non-scalar props,
// so `sample` is an empty object and the `named` map is also empty.
//
// lib/sample-data.ts auto-aggregates every src/data/samples/*.ts via glob,
// so this file just needs to exist and export the expected shape.

export default {
  sample: {},
};
