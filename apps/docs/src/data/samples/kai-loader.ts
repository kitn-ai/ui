// Sample data for <kai-loader> non-scalar props.
//
// kai-loader has NO non-scalar props — all three props (variant, size, text)
// are scalar attributes. The playground and examples set them via config/controls.
// This file is still required so sample-data.ts auto-aggregation picks up the tag.

export default {
  sample: {},
};
