---
title: Resizable Item
description: One panel of a resizable layout.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import SlotTable from '../../../components/SlotTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-resizable-item</p>

<p class="kai-lede">Carries that panel's size and limits, so the layout around it needs no extra code.</p>

<Facts items={['Shadow DOM', 'All-attribute API', 'px or % sizes', 'Lockable panel', 'Hideable with reflow']} />

## Preview

<Playground client:only="solid" tag="kai-resizable-item" />

<Aside type="tip" title="When to use">
A direct child of `<kai-resizable>`, one per panel. Give a panel with a natural default width an explicit `size`; leave `size` off the main area so it fills what remains. `locked` for chrome panels.
</Aside>

## Usage

`<kai-resizable-item>` has no standalone rendering — always place it inside `<kai-resizable>`:

```html
<kai-resizable orientation="horizontal" style="display:block;height:400px">
  <kai-resizable-item size="260px" min="160px" locked>
    <!-- sidebar content -->
  </kai-resizable-item>
  <kai-resizable-item>
    <!-- main content fills remaining space -->
  </kai-resizable-item>
  <kai-resizable-item size="35%" min="200px">
    <!-- preview / inspector pane -->
  </kai-resizable-item>
</kai-resizable>

<script type="module">
  import '@kitn.ai/ui/web-components';
</script>
```

- **`size`** — initial main-axis size: `"280px"` or `"25%"`. Omit for a flexible panel (`flex: 1 1 0%`).
- **`min` / `max`** — drag bounds in px or %, e.g. `min="120px"`, `max="50%"`.
- **`locked`** — fixes the panel; adjacent dividers become non-draggable separators.
- **`hidden`** — removes the panel and its divider; remaining panels reflow. Toggle via JS to collapse/restore.

Resize events (`kai-change`, `kai-maximize-change`) fire on the parent `<kai-resizable>`, not the item. See the [Resizable](/components/resizable) page for the full event reference.

## Examples

### Flexible Panel

No `size`, so it stretches to fill what siblings leave. Right choice for the primary content area.

<Example client:visible tag="kai-resizable-item" data="flexible" />

### Sized Panel

`size` sets the initial width; the user can still drag unless `locked` is also set.

<Example client:visible tag="kai-resizable-item" config={{ size: '260px', min: '140px', max: '50%' }} data="sized" />

### Locked Panel

`locked` fixes the panel at its `size` and turns adjacent dividers into non-draggable separators.

<Example client:visible tag="kai-resizable-item" config={{ size: '240px', locked: true }} data="locked" />

## Slots

<SlotTable tag="kai-resizable-item" />

## Props

<PropTable tag="kai-resizable-item" />

## Composed from

<ComposedFrom tag="kai-resizable-item" />

<NeedAHand />
