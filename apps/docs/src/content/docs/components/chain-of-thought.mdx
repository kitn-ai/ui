---
title: Chain Of Thought
description: A step-by-step list of an agent's reasoning.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import DeclarativeChildrenTable from '../../../components/DeclarativeChildrenTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-chain-of-thought</p>

<p class="kai-lede">Shows the plan behind an answer, with the detail hidden until the reader asks for it.</p>

<Facts items={['Shadow DOM', 'Property-driven API', 'Declarative kai-step children', 'Expandable detail per step', 'Prop + child steps merge']} />

## Preview

<Playground client:only="solid" tag="kai-chain-of-thought" />

<Aside type="tip" title="When to use">
Above or below an assistant message, to show the model's plan or reasoning trace. It is `display: block` and sizes to content height, so it needs no fixed height.
</Aside>

## Usage

Set `steps` in JavaScript — it's an array, so it can't be an HTML attribute:

```html
<kai-chain-of-thought id="cot"></kai-chain-of-thought>

<script type="module">
  import '@kitn.ai/ui/web-components';
  document.getElementById('cot').steps = [
    { label: 'Understand the request', content: 'The user wants composable web components.' },
    { label: 'Design the API', content: 'Route 1: variant + flags; rich data via properties.' },
    { label: 'Build & verify' },
  ];
</script>
```

- Each item needs a `label` string; add `content` to make the step expandable.
- **Declarative alternative** — author steps as `<kai-step>` children; `label` becomes the heading, `textContent` becomes the collapsible detail. Mix both: property items render first, then `<kai-step>` children append after.

```html
<kai-chain-of-thought>
  <kai-step label="Understand the request">The user wants composable web components.</kai-step>
  <kai-step label="Design the API">Route 1: variant + flags; rich data via properties.</kai-step>
  <kai-step label="Build &amp; verify"></kai-step>
</kai-chain-of-thought>
```

<DeclarativeChildrenTable tag="kai-chain-of-thought" />

## Examples

### Default

<Example client:visible tag="kai-chain-of-thought" />

### Agent Plan

<Example client:visible tag="kai-chain-of-thought" data="agentPlan" />

### Two-Step Query Trace

Every step has expandable detail, with no label-only summary.

<Example client:visible tag="kai-chain-of-thought" data="short" />

## Props

<PropTable tag="kai-chain-of-thought" />

## Methods

<MethodTable tag="kai-chain-of-thought" />

## Composed from

<ComposedFrom tag="kai-chain-of-thought" />

<NeedAHand />
