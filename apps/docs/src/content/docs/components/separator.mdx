---
title: Separator
description: A divider between groups of content.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import PartTable from '../../../components/PartTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-separator</p>

<p class="kai-lede">Marks a boundary in a layout that assistive technology can announce.</p>

<Facts items={['Shadow DOM', 'Horizontal + vertical', 'role: separator', 'One ::part to restyle']} />

## Preview

<Playground client:only="solid" tag="kai-separator" />

<Aside type="tip" title="When to use">
To break a layout into clear groups: a horizontal rule between stacked sections, or a vertical one between inline items. Prefer it over a bare CSS border when the divide is worth announcing.
</Aside>

## Usage

Drop it between the things you're dividing. A horizontal separator is a full-width block rule; `orientation="vertical"` turns it into a rule that stretches to its row's height, so place it inside a flex or grid row.
```html
<section>Models</section>
<kai-separator></kai-separator>
<section>Skills</section>

<div style="display: flex; align-items: center; gap: 0.5rem">
  <span>Opus 4.8</span>
  <kai-separator orientation="vertical"></kai-separator>
  <span>Fable 5</span>
</div>
```

A separator only reads in context, so these show it between real content rather than a bare line in an empty box.

### Horizontal

<div class="not-content my-4 rounded-xl border border-line bg-surface px-5 py-4">
  <p style="margin:0;font-weight:600">Models</p>
  <kai-separator style="margin:0.85rem 0"></kai-separator>
  <p style="margin:0;font-weight:600">Skills</p>
  <kai-separator style="margin:0.85rem 0"></kai-separator>
  <p style="margin:0;font-weight:600">Plugins</p>
</div>

### Vertical

`orientation="vertical"` stretches the rule to its row's height, for dividing inline items. Place it in a flex or grid row with a height.

<div class="not-content my-4 rounded-xl border border-line bg-surface px-5 py-4">
  <div style="display:flex;align-items:center;gap:0.85rem;height:1.5rem">
    <span>Opus 4.8</span>
    <kai-separator orientation="vertical"></kai-separator>
    <span>Fable 5</span>
    <kai-separator orientation="vertical"></kai-separator>
    <span>Haiku 4.5</span>
  </div>
</div>

## Styling

The line is a single `::part`, so recolor, thicken, or inset it from your own stylesheet.

<PartTable tag="kai-separator" />

## Props

<PropTable tag="kai-separator" />

## Composed from

<ComposedFrom tag="kai-separator" />

<NeedAHand />
