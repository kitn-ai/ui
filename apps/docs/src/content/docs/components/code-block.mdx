---
title: Code Block
description: Displays a syntax-highlighted code snippet with a copy button.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-code-block</p>

<p class="kai-lede">Picks up the page's light or dark theme, so a snippet matches the thread around it.</p>

<Facts items={['Shadow DOM', 'Shiki highlighting', '100+ languages', 'Built-in copy button', 'Light / dark themes']} />

## Preview

<Playground client:only="solid" tag="kai-code-block" />

<Aside type="tip" title="When to use">
For a snippet shown on its own: a message bubble, a tutorial card, a doc pane. When the code sits inside prose, [Markdown](/components/markdown/) highlights the fence for you.
</Aside>

## Usage

Set `code` in JavaScript (multiline strings don't survive HTML attributes) and `language` as an attribute:

```html
<kai-code-block id="cb" language="ts"></kai-code-block>

<script type="module">
  import '@kitn.ai/ui/web-components';

  await customElements.whenDefined('kai-code-block');

  const cb = document.getElementById('cb');
  cb.code = `export function greet(name: string) {
  return \`Hello, \${name}!\`;
}`;
</script>
```

- **`code`** — assign it in JavaScript; multiline strings won't survive HTML attribute serialization.
- **`language`** defaults to `tsx`. Pass any [Shiki grammar name](https://shiki.style/languages): `ts`, `python`, `bash`, `json`, `html`.
- **`code-theme`** defaults to `github-dark-dimmed`. Any [Shiki theme](https://shiki.style/themes) works.
- **`code-highlight="false"`** skips Shiki entirely — useful for raw log output.

## Examples

### TypeScript

<Example client:visible tag="kai-code-block" data="typescript" />

### JavaScript

<Example client:visible tag="kai-code-block" data="javascript" />

### Python

<Example client:visible tag="kai-code-block" data="python" />

### Shell / Bash

<Example client:visible tag="kai-code-block" data="bash" />

### Compact Text Size

`prose-size="xs"` tightens the font for dense snippets or embedded cards.

<Example client:visible tag="kai-code-block" config={{ proseSize: 'xs' }} data="typescript" />

### Highlighting Disabled

`code-highlight="false"` renders plain monospace — no Shiki, no colours. Good for terminal output or log lines.

<Example client:visible tag="kai-code-block" config={{ codeHighlight: false }} data="plaintext" />

## Props

<PropTable tag="kai-code-block" />

## Composed from

<ComposedFrom tag="kai-code-block" />

<NeedAHand />
