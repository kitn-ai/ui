---
title: Markdown
description: Renders a markdown string.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-markdown</p>

<p class="kai-lede">Turns text into formatted prose with highlighted code, outside a framework tree.</p>

<Facts items={['Shadow DOM', 'Shiki highlighting', '4 prose sizes', 'GFM support', 'Highlight on/off']} />

## Preview

<Playground client:only="solid" tag="kai-markdown" />

<Aside type="tip" title="When to use">
To render model output or documentation outside a SolidJS tree: message bubbles, doc viewers, help panels. In SolidJS, import the `Markdown` primitive directly.
</Aside>

## Usage

Set `content` in JavaScript with your markdown string:

```html
<kai-markdown id="md"></kai-markdown>

<script type="module">
  import '@kitn.ai/ui/web-components';

  await customElements.whenDefined('kai-markdown');

  const md = document.getElementById('md');
  md.content = '### Hello\nRenders **bold**, _italic_, and `code`.';
</script>
```

- **`content`**: assign it in JavaScript; multiline strings don't survive HTML attributes cleanly.
- **`prose-size`** — scales body text: `xs`, `sm` (default), `base`, `lg`. Use `sm` in message bubbles, `base`/`lg` for doc pages.
- **`code-theme`** — defaults to `github-dark-dimmed`; any [Shiki theme](https://shiki.style/themes) works.
- **`code-highlight="false"`** — skips Shiki entirely, rendering code fences as plain monospace.

## Examples

### Default

<Example client:visible tag="kai-markdown" />

### Rich Content

<Example client:visible tag="kai-markdown" data="rich" />

### Compact Text Size

For dense contexts: cards, sidebar panels, tooltips.

<Example client:visible tag="kai-markdown" config={{ proseSize: 'xs' }} data="short" />

### Large Prose

<Example client:visible tag="kai-markdown" config={{ proseSize: 'lg' }} />

### Highlighting Disabled

No Shiki loads, so it suits raw terminal output or log lines.

<Example client:visible tag="kai-markdown" config={{ codeHighlight: false }} data="plain" />

## Props

<PropTable tag="kai-markdown" />

## Composed from

<ComposedFrom tag="kai-markdown" />

<NeedAHand />
