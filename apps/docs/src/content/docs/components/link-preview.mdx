---
title: Link Preview
description: A link tile built from a link's metadata.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-link-preview</p>

<p class="kai-lede">Turns a bare URL into an image, a title, and a description without fetching anything itself.</p>

<Facts items={['Shadow DOM', 'Pure by default — never fetches', 'Optional backend fetcher', 'Graceful image fallback', 'Card Contract events']} />

## Preview

<Playground client:only="solid" tag="kai-link-preview" />

<Aside type="tip" title="When to use">
When an agent surfaces a URL and you want a rich tile instead of a plain hyperlink. With only the URL at render time, wire `configureLinkPreview` to your own OG-scraping backend.
</Aside>

## Usage

Set `data` in JavaScript (it's an object, not an attribute):

```html
<kai-link-preview id="lp"></kai-link-preview>

<script type="module">
  import '@kitn.ai/ui/web-components';

  await customElements.whenDefined('kai-link-preview');

  const lp = document.getElementById('lp');
  lp.cardId = 'card-link-1';
  lp.data = {
    url: 'https://example.com/blog/generative-ui',
    title: 'Generative UI, explained',
    description: 'How agents render typed, themed cards in the chat.',
    image: 'https://example.com/og.png',
    favicon: 'https://example.com/favicon.ico',
    siteName: 'Example Blog',
    domain: 'example.com',
  };
</script>
```

- **`data`** — every key except `url` is optional.
- **`cardId`** — set as `card-id` attribute or `cardId` property; correlates emitted events when multiple cards are in a `<kai-cards>` host.
- **Pure by default** — the element never makes network calls. For bare `{ url }`, call `configureLinkPreview({ fetchMetadata })` once at startup to point at your backend.
- **Click** dispatches a bubbling `kai-card` CustomEvent with `{ kind: 'open', url, target: 'tab' }`. A missing or broken image hides the image region silently; an invalid `url` renders a non-clickable "Invalid link" chip.
## Examples

### Full Preview

<Example client:visible tag="kai-link-preview" />

### No Image

With no `image`, the card falls back to the text-and-domain layout.

<Example client:visible tag="kai-link-preview" data="noImage" />

### Bare URL with Fetcher

With only `{ url }`, the card shows a skeleton until `configureLinkPreview` resolves metadata from your backend:

```ts
import { configureLinkPreview } from '@kitn.ai/ui';

configureLinkPreview({
  fetchMetadata: (url) =>
    fetch('/api/og?url=' + encodeURIComponent(url)).then((r) => r.json()),
});
```

<Example client:visible tag="kai-link-preview" data="bareUrl" />

### Invalid Link

An invalid `url` renders a non-clickable "Invalid link" chip and emits one `error` event through the `kai-card` envelope.

<Example client:visible tag="kai-link-preview" data="invalidLink" />

## Props

<PropTable tag="kai-link-preview" />

## Composed from

<ComposedFrom tag="kai-link-preview" />

<NeedAHand />
