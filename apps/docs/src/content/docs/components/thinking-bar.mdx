---
title: Thinking Bar
description: An animated indicator that the assistant is working.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-thinking-bar</p>

<p class="kai-lede">Tells the reader a response is coming, and lets them interrupt instead of waiting.</p>

<Facts items={['Shadow DOM', 'Animated shimmer', 'Optional stop affordance', '1 event', 'Fully attribute-driven']} />

## Preview

<Playground client:only="solid" tag="kai-thinking-bar" />

<Aside type="tip" title="When to use">
Sits above the streaming reply while the assistant is generating. Turn on `stoppable` so users can cut a long reasoning step short, and handle `kai-stop` to cancel the request.
</Aside>

## Usage

```html
<kai-thinking-bar text="Thinking" stoppable stop-label="Answer now"></kai-thinking-bar>
```

All three props are **attributes** — no JavaScript needed.

- **`stoppable`** — bare boolean attribute; shows the stop button.
- **`kai-stop`** — fires with no payload when the stop button is clicked; cancel the in-flight request in your handler.

## Examples

### Default

Remove the element from the DOM once the assistant reply arrives.

<Example client:visible tag="kai-thinking-bar" config={{ text: 'Thinking' }} />

### Custom Label

<Example client:visible tag="kai-thinking-bar" config={{ text: 'Searching the web…' }} />

### Stoppable

<Example client:visible tag="kai-thinking-bar" config={{ text: 'Reasoning', stoppable: true, stopLabel: 'Answer now' }} />

### Custom Stop Label

<Example client:visible tag="kai-thinking-bar" config={{ text: 'Deep thinking…', stoppable: true, stopLabel: 'Skip reasoning' }} />

## Props

<PropTable tag="kai-thinking-bar" />

## Events

<EventTable tag="kai-thinking-bar" />

## Composed from

<ComposedFrom tag="kai-thinking-bar" />

<NeedAHand />
