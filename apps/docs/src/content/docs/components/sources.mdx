---
title: Sources
description: A row of citation chips for the sources behind an answer.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import DeclarativeChildrenTable from '../../../components/DeclarativeChildrenTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-sources</p>

<p class="kai-lede">Gives a reply its references as tappable chips instead of a wall of links.</p>

<Facts items={['Shadow DOM', 'No events', 'Optional favicons', 'Numbered or domain labels', 'Declarative children']} />

## Preview

<Playground client:only="solid" tag="kai-sources" />

<Aside type="tip" title="When to use">
Below an assistant message that cites external references; sources the model streamed already render inside [Message](/components/message/).
</Aside>

## Usage

Assign the `sources` property — an array can't be an HTML attribute:

```html
<kai-sources id="srcs" show-favicon></kai-sources>
<script type="module">
  import '@kitn.ai/ui/web-components';
  document.getElementById('srcs').sources = [
    { href: 'https://kitn.dev', title: 'kitn — the kit', description: 'Composable chat UI.' },
    { href: 'https://solidjs.com', title: 'SolidJS', description: 'A reactive UI library.' },
  ];
</script>
```

- Each item accepts `href` (required), `title`, `description`, `label`, and `showFavicon`.
- **`show-favicon`** enables favicons globally; a per-item `showFavicon: false` overrides for that chip.
- **`numbered`** replaces domain labels with `[1]`, `[2]`, …
- **Declarative path**: `<kai-source>` elements as light-DOM children, appended after any prop sources. Attributes are `href`, `label`, `headline`, `description`, and the bare `show-favicon`: the item's `title` field is spelled **`headline`**, because `title` is a reserved global HTML attribute.

```html
<kai-sources>
  <kai-source href="https://kitn.ai" headline="AI/UI" description="Web components for AI chat"></kai-source>
</kai-sources>
```

<DeclarativeChildrenTable tag="kai-sources" />

## Examples

### Default with Favicons

<Example client:visible tag="kai-sources" config={{ showFavicon: true }} />

### Domain Labels Only

<Example client:visible tag="kai-sources" data="noFavicon" />

### Numbered Citations

<Example client:visible tag="kai-sources" config={{ numbered: true }} data="numbered" />

## Props

<PropTable tag="kai-sources" />

## Composed from

<ComposedFrom tag="kai-sources" />

<NeedAHand />
