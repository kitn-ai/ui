---
title: Reasoning
description: A collapsible block of a model's thinking.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-reasoning</p>

<p class="kai-lede">Opens while the model is thinking and closes when it finishes, on its own.</p>

<Facts items={['Shadow DOM', '1 event', 'Auto-expands while streaming', 'Markdown rendering', 'Controlled or uncontrolled']} />

## Preview

<Playground client:only="solid" tag="kai-reasoning" />

<Aside type="tip" title="When to use">
Between the assistant message content and any tool calls, wherever the model produces a chain-of-thought trace. Expand and collapse are automatic.
</Aside>

## Usage

Set `text` in JavaScript; toggle `streaming` to drive the auto-expand/collapse behaviour:

```html
<kai-reasoning id="reason" label="Reasoning"></kai-reasoning>

<script type="module">
  import '@kitn.ai/ui/web-components';

  await customElements.whenDefined('kai-reasoning');

  const reason = document.getElementById('reason');
  reason.text = 'First I parse the request, then I plan the steps, then I execute.';

  // Set while thought arrives; clear when stream ends.
  // reason.streaming = true;

  reason.addEventListener('kai-open-change', (e) => {
    console.log('open:', e.detail.open);
  });
</script>
```

- **`text`** — assign it with `el.text = '...'`.
- **`streaming`**: set `true` while the thought arrives, and the block auto-expands; clear it and the block collapses.
- **`open`**: leave unset for uncontrolled behaviour; set it explicitly when a parent drives the open state.
- **`label`** overrides the default trigger text (`"Reasoning"`), e.g. `"Thinking"`.

## Examples

### Default

Click the trigger to expand and collapse.

<Example client:visible tag="kai-reasoning" />

### Streaming

Auto-expands, as it would while a model generates its chain-of-thought.

<Example client:visible tag="kai-reasoning" config={{ streaming: true }} />

### Custom Label

<Example client:visible tag="kai-reasoning" config={{ label: 'Thinking' }} />

### Markdown Rendering

The text renders as styled markdown by default: headings, bold, code spans, paragraphs.

<Example client:visible tag="kai-reasoning" data="markdown" />

### Markdown Disabled

`markdown="false"` renders plain pre-formatted content, for raw log output or literal backticks.

<Example client:visible tag="kai-reasoning" config={{ markdown: false }} />

## Props

<PropTable tag="kai-reasoning" />

## Events

<EventTable tag="kai-reasoning" />

## Methods

<MethodTable tag="kai-reasoning" />

## Composed from

<ComposedFrom tag="kai-reasoning" />

<NeedAHand />
