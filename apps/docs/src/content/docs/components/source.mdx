---
title: Source
description: A citation chip for a single source behind an answer.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-source</p>

<p class="kai-lede">Shows the domain as the label and the headline on hover, so a reader can check where a claim came from.</p>

<Facts items={['Shadow DOM', 'All props are attributes', 'Optional favicon', 'Hover-card preview', 'No events']} />

## Preview

<Playground client:only="solid" tag="kai-source" />

<Aside type="tip" title="When to use">
For a single citation inside a custom message template or a tooltip. When an answer has two or more, [Sources](/components/sources/) lays the chips out in a row.
</Aside>

## Usage

All props are plain HTML attributes — no JavaScript needed:

```html
<kai-source
  href="https://kitn.dev"
  headline="kitn — the kit"
  description="Composable SolidJS + web-component chat UI."
  show-favicon
></kai-source>
<script type="module">import '@kitn.ai/ui/web-components';</script>
```

- **`href` is required.** The element renders nothing without it.
- **`label`** defaults to the hostname (`kitn.dev`). Pass an explicit label when the domain is ambiguous or too long.
- **`headline`** (not `title`) — `title` is a reserved HTML attribute that conflicts with custom element constructors.
- Clicking the chip opens the URL directly — the element emits no events.

## Examples

### Basic Citation

<Example client:visible tag="kai-source" config={{ href: 'https://kitn.dev', headline: 'kitn — the kit', description: 'Composable SolidJS + web-component chat UI.' }} />

### With Favicon

<Example client:visible tag="kai-source" config={{ href: 'https://solidjs.com', headline: 'SolidJS', description: 'A declarative, efficient, and flexible JavaScript library for building user interfaces.', showFavicon: true }} />

### Custom Label

Override the domain default with `label` when you want a shorter or more recognisable name.

<Example client:visible tag="kai-source" config={{ href: 'https://developer.mozilla.org/en-US/docs/Web/API/Web_components', label: 'MDN', headline: 'Web Components — MDN', description: 'A suite of technologies that let you create reusable custom elements.' }} />

### Full Detail with Favicon

<Example client:visible tag="kai-source" config={{ href: 'https://vitejs.dev', label: 'Vite', headline: 'Vite — Next Generation Frontend Tooling', description: 'An opinionated web build tool that serves your source files over native ES modules during development.', showFavicon: true }} />

## Props

<PropTable tag="kai-source" />

## Composed from

<ComposedFrom tag="kai-source" />

<NeedAHand />
