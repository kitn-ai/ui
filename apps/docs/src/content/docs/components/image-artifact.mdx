---
title: Image artifact
description: Renders image data a model or tool produced — bare base64 or raw bytes, with the media type it must be labelled with.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-image-artifact</p>

<p class="kai-lede">An image the model or a tool produced, rendered straight from its payload: bare base64 or a <code>Uint8Array</code>, plus the media type that labels it. Nothing is uploaded and no URL is needed.</p>

<Facts items={['Shadow DOM', 'Base64 or Uint8Array', 'Skeleton while it resolves', 'Media type required']} />

## Preview

<Playground client:only="solid" tag="kai-image-artifact" />

<Aside type="tip" title="When to use">
Use this when the image *arrives as data* — a generated chart, a screenshot a tool returned, a frame from an image API. It owns the two things that are easy to get wrong on that path: the media type it must be labelled with, and the object URL that has to be revoked. If you already have a URL, [Image](/components/image/) is the shorter road.
</Aside>

## Usage

```html
<kai-image-artifact media-type="image/png" alt="A generated chart"></kai-image-artifact>

<script type="module">
  import '@kitn.ai/ui/web-components';

  const el = document.querySelector('kai-image-artifact');

  // a base64 payload (a string attribute or property):
  el.data = '<...base64 encoded image data...>';

  // or raw bytes, as a property:
  // el.data = new Uint8Array([...]);
</script>
```

- **`data`** is the payload: a string (BARE base64, the shape the providers hand back) or a `Uint8Array` (set as a property). It is never a URL: for a `data:` URI or an https address, use [Image](/components/image/)'s `src`.
- **`media-type`** is required, and that is the point of the component. A base64 payload does not describe itself, so guessing `image/png` labels a JPEG wrong: browsers sniff and render it, while everything downstream that trusts the label (your API, a file written to disk, the wire) is told the wrong type. Provide it and the label is true.
- **`alt`** describes the image. Pass a real sentence; it is what a screen reader announces in place of the picture.

<Aside type="caution" title="Missing media type">
With `data` set and no `media-type`, the component reports once in the console and renders the placeholder rather than an image. That is deliberate: a picture labelled wrong is worse than a visible gap with a message that names the fix.
</Aside>

## The AI SDK shape

The SDK hands generated images back as one object that carries both representations (`GeneratedFile`):

```ts
const { image } = await generateImage({ model, prompt });
// image: { base64, uint8Array, mediaType } — pick the one you have

el.data = image.base64;      // or image.uint8Array
el.mediaType = image.mediaType;
```

One line each, because the component's input is the same fact the SDK reports. It is deliberately not the SDK's object: `data` plus `mediaType` describes the thing, so a change in how the SDK packages it does not reach your markup.

## Examples

### A generated chart

A base64 SVG with its media type and a description.

<Example client:visible tag="kai-image-artifact" />

### A second artifact

Swap `data` and `media-type` per artifact; nothing else changes.

<Example client:visible tag="kai-image-artifact" data="circle" />

### With descriptive alt text

The same artifact, announced properly. Every artifact needs its own sentence, not a shared "generated image".

<Example client:visible tag="kai-image-artifact" data="checkmark" />

## Props

<PropTable tag="kai-image-artifact" />

## Composed from

<ComposedFrom tag="kai-image-artifact" />

<NeedAHand />
