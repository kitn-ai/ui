---
title: Workspace
description: A chat-agnostic layout shell as a single web component. Five slots, resize handles, per-aside collapse, and a mobile drawer mode, all isolated in Shadow DOM.
---

import { Aside } from '@astrojs/starlight/components';
import WorkspaceDemo from '../../../components/WorkspaceDemo.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-workspace</p>

<p class="kai-lede">The layout shell for a full chat app: five slots (header, start, main, end, footer), drag-resizable columns, per-aside collapse, and a mobile drawer mode. It knows nothing about chat. You slot the parts in and drive them directly.</p>

<Facts items={['Shadow DOM', '5 slots', 'Resize handles', 'Per-aside collapse', 'Mobile drawer']} />

## Preview

`<kai-conversations>` slotted into `start`, `<kai-chat>` in the main region, and the app scripting both parts directly:

<WorkspaceDemo client:only="solid" />

<Aside type="tip" title="When to use">
Reach for `<kai-workspace>` when you want the classic app layout, a collapsible rail beside a main surface, without writing the resize, collapse, and drawer plumbing yourself. It arranges whatever you slot in: a file tree in `start` is as valid as `<kai-conversations>`. If you only need the conversation surface, use [`<kai-chat>`](/components/chat/) on its own. The [Use a workspace](/guides/use-a-workspace/) guide walks the full composition.
</Aside>

## Usage

Slot content into the regions, then drive each part as its own element. The shell itself takes only layout props:

```html
<kai-workspace collapse-below="720" drawer-below="640" style="display:block; height:100vh;">
  <kai-conversations slot="start"></kai-conversations>
  <kai-chat></kai-chat>
</kai-workspace>

<script type="module">
  import '@kitn.ai/ui/web-components';

  const ws = document.querySelector('kai-workspace');
  const rail = document.querySelector('kai-conversations');
  const chat = document.querySelector('kai-chat');

  // Chat data lives on the chat parts, not the shell.
  rail.conversations = await api.listConversations();
  chat.messages = [];

  rail.addEventListener('kai-conversation-select', async (e) => {
    rail.activeId = e.detail.id;
    chat.messages = await api.loadThread(e.detail.id);
  });
  chat.addEventListener('kai-submit', (e) => send(e.detail.value));

  // The shell fires layout events only.
  ws.addEventListener('kai-aside-toggle', (e) => console.log(e.detail.side, e.detail.collapsed));
</script>
```

- **Regions** are the `header`, `start`, `main`, `end`, and `footer` slots. Unnamed children land in `main`. A region renders only when you project content into it, so an empty shell paints nothing. `start` and `end` are logical: on an RTL page, `start` is the right column.
- **Aside widths** are CSS custom properties, not props: `--kai-workspace-start-width` (280px), `--kai-workspace-start-min-width` (200px), `--kai-workspace-start-max-width` (480px), and the `--kai-workspace-end-*` trio (320px / 200px / 480px). Each drag, keyboard nudge, or handle double-click fires `kai-aside-resize` with `{ side, width }`.
- **Collapse** is uncontrolled by default: the element manages it and fires `kai-aside-toggle` with `{ side, collapsed }`. Start collapsed with the `default-start-collapsed` or `default-end-collapsed` attribute, or control an aside by setting the `startCollapsed` / `endCollapsed` property and updating it from `kai-aside-toggle`. `collapse-below` auto-collapses both asides when the shell narrows past that width.
- **Drawer mode.** Below `drawer-below`, an expanded aside paints as an overlay drawer above the main region. Escape closes it and returns focus to the opener.

## Examples

### Sidebar starts collapsed

The rail begins collapsed and the main region takes the full width. Expand it from the shell's handle, or call `expandAside('start')`:

```html
<kai-workspace default-start-collapsed style="display:block; height:100vh;">
  <kai-conversations slot="start"></kai-conversations>
  <kai-chat></kai-chat>
</kai-workspace>
```

### An inspector in the end aside

Any element works in a region. Here the `end` aside holds a preview pane beside the thread:

```html
<kai-workspace drawer-below="640" style="display:block; height:100vh;">
  <kai-conversations slot="start"></kai-conversations>
  <kai-chat></kai-chat>
  <kai-artifact slot="end"></kai-artifact>
</kai-workspace>
```

<Aside type="note" title="Migrating from the chat preset?">
Before the 2026-08-20 re-cast this element was a chat preset with `messages`, `conversations`, and 30 other props. That surface is gone. The prop-by-prop migration table is in [Use a workspace](/guides/use-a-workspace/#breaking-migrating-from-the-chat-preset).
</Aside>

## Props

<PropTable tag="kai-workspace" />

## Events

<EventTable tag="kai-workspace" />

## Methods

<MethodTable tag="kai-workspace" />

## Composed from

<ComposedFrom tag="kai-workspace" />

<NeedAHand />
