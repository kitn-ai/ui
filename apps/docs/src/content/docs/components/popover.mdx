---
title: Popover
description: A floating panel of arbitrary content behind a trigger.
---

import { Aside } from '@astrojs/starlight/components';
import PopoverPlayground from '../../../components/PopoverPlayground.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import SlotTable from '../../../components/SlotTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-popover</p>

<p class="kai-lede">Holds what a dropdown cannot, such as rows, toggles, and nested groups.</p>

<Facts items={['Shadow DOM', '1 event', 'Slotted trigger + panel', 'Escape / outside-click close', '12 placements']} />

## Preview

<PopoverPlayground client:only="solid" />

<Aside type="tip" title="When to use">
For a button-plus-card affordance: a header model menu, a settings popover, an account dropdown with mixed controls. For a flat list of commands, [Menu](/components/menu/) is simpler.
</Aside>

## Usage

Slot the control as `slot="trigger"` and the panel as the default slot:

```html
<kai-popover placement="bottom-start">
  <button slot="trigger">GPT-5.5 ▾</button>

  <div>
    <button>GPT-5.5 — Flagship model</button>
    <button>Legacy models ▾</button>
    <label>Temporary chat <kai-switch label="Temporary chat"></kai-switch></label>
  </div>
</kai-popover>

<script type="module">
  import '@kitn.ai/ui/web-components';

  document.querySelector('kai-popover')
    .addEventListener('kai-open-change', (e) => console.log('open:', e.detail.open));
</script>
```

- **Slots** — `trigger` is the control; the default slot is the panel. Clicking the trigger toggles it.
- **Dismissal** — Escape or a click outside closes the panel; clicks **inside** it do not.
- **`placement`** — floating placement relative to the trigger (`bottom-start`, `top`, `right`, …); defaults to `bottom-start`. The panel flips and shifts to stay on screen.
- **Controlled** — omit `open` for click-to-toggle, or set the `open` property and drive it from `kai-open-change` to control it yourself.
- **Panel content lives in your light DOM**, so your own styles apply to it directly and no shadow piercing is needed. It defaults to a compact `text-sm`; row icons and chevrons are yours to supply.

## Slots

<SlotTable tag="kai-popover" />

## Props

<PropTable tag="kai-popover" />

## Events

<EventTable tag="kai-popover" />

## Methods

<MethodTable tag="kai-popover" />

## Composed from

<ComposedFrom tag="kai-popover" />

<NeedAHand />
