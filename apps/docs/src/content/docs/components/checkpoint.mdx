---
title: Checkpoint
description: A button that marks a restore point in a conversation.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-checkpoint</p>

<p class="kai-lede">Fits a dense action bar, taking either an icon alone or a short label.</p>

<Facts items={['Shadow DOM', '1 event', '3 variants', '5 sizes', 'Icon-only or labeled']} />

## Preview

<Playground client:only="solid" tag="kai-checkpoint" />

<Aside type="tip" title="When to use">
Wherever readers may want to save a spot and jump back to it: a message action bar, a chat toolbar. For the whole row it sits in, use [Message](/components/message/).
</Aside>

## Usage

All props are scalar attributes — no JavaScript needed:

```html
<kai-checkpoint label="Restore" tooltip="Restore this checkpoint" variant="outline" size="sm"></kai-checkpoint>

<script type="module">
  import '@kitn.ai/ui/web-components';
  document.querySelector('kai-checkpoint')
    .addEventListener('kai-select', () => console.log('restore checkpoint'));
</script>
```

- Use `sm` (default) for inline action bars, `icon` or `icon-sm` when no label is shown, `md` or `lg` for standalone placements.
- When omitting `label`, set `tooltip` — it becomes the accessible name (`aria-label`). Falls back to `"Checkpoint"` if neither is set.

## Examples

### Labeled

<Example client:visible tag="kai-checkpoint" config={{ label: 'Restore', variant: 'outline' }} />

### Icon Only

`icon-sm` size with a tooltip — for tight action bars.

<Example client:visible tag="kai-checkpoint" config={{ size: 'icon-sm', tooltip: 'Restore this checkpoint' }} />

### Ghost Variant (Default)

<Example client:visible tag="kai-checkpoint" config={{ variant: 'ghost', tooltip: 'Save checkpoint', label: 'Save' }} />

### Default Variant

The filled `default` variant raises the button off the surface — useful when the checkpoint is the primary affordance in its container.

<Example client:visible tag="kai-checkpoint" config={{ variant: 'default', label: 'Checkpoint', size: 'md' }} />

## Props

<PropTable tag="kai-checkpoint" />

## Events

<EventTable tag="kai-checkpoint" />

## Composed from

<ComposedFrom tag="kai-checkpoint" />

<NeedAHand />
