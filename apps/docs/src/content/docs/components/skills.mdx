---
title: Skills
description: A strip of badges for the skills a message used.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import DeclarativeChildrenTable from '../../../components/DeclarativeChildrenTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-skills</p>

<p class="kai-lede">Wraps onto as many lines as it needs, so a long tool list still fits the row.</p>

<Facts items={['Shadow DOM', 'Property or declarative children', 'Badges wrap automatically', '2 composition paths']} />

## Preview

<Playground client:only="solid" tag="kai-skills" />

<Aside type="tip" title="When to use">
Beneath an assistant message, to say which tools were active for that turn. For the calls themselves, use [Tool](/components/tool/).
</Aside>

## Usage

Assign the `skills` property after the element upgrades — arrays can't be HTML attributes:

```html
<kai-skills id="skills"></kai-skills>
<script type="module">
  import '@kitn.ai/ui/web-components';
  document.getElementById('skills').skills = [
    { id: 'web-search', name: 'Web Search' },
    { id: 'code',       name: 'Code' },
  ];
</script>
```

- **Declarative path** — nest `<kai-skill id="…">Label</kai-skill>` children directly; useful for server-rendered HTML with no JS wiring.
- **Mixed** — when both are provided, prop items render first and declarative children are appended after.
- **SolidJS** — import `MessageSkills` from `@kitn.ai/ui` and pass a `skills` prop instead.

<DeclarativeChildrenTable tag="kai-skills" />

## Examples

### Two Skills

<Example client:visible tag="kai-skills" data="twoSkills" />

### Multiple Skills

Badges wrap horizontally when they overflow — no extra layout work needed.

<Example client:visible tag="kai-skills" data="richSkills" />

### Declarative Composition

<Example client:visible tag="kai-skills" />

## Props

<PropTable tag="kai-skills" />

## Composed from

<ComposedFrom tag="kai-skills" />

<NeedAHand />
