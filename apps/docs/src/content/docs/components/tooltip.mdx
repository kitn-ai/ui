---
title: Tooltip
description: A popup that labels a control on hover or focus.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import SlotTable from '../../../components/SlotTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-tooltip</p>

<p class="kai-lede">The hint sits above the page, so an icon-only button can still say what it does.</p>

<Facts items={['Shadow DOM', 'Hover + focus', 'Self-positioning', 'Text-only hint']} />

## Preview

<Playground client:only="solid" tag="kai-tooltip" />

<Aside type="tip" title="When to use">
Label a control whose purpose isn't obvious from its face: an icon-only button, a terse menu item. It carries plain text only; for rich content, use [Hover card](/components/hover-card/) instead.
</Aside>

## Usage

Put the trigger in the default slot (any focusable control) and set the hint text. The bubble is portaled inside the shadow root so it is not clipped, and it dismisses on Escape or an outside click. `openDelay` tunes the hover wait; focus shows it immediately.

```html
<kai-tooltip content="Voice input">
  <kai-button variant="subtle" size="icon" icon="mic" label="Voice input"></kai-button>
</kai-tooltip>
```

## Examples

### On an icon button

<Example client:visible tag="kai-tooltip" config={{ content: 'Attach a file' }} data="attach" />

### On a labeled button

<Example client:visible tag="kai-tooltip" config={{ content: 'Regenerate the last response' }} data="button" />

### A snappier delay

`openDelay` is the hover wait in milliseconds, 600 by default.

<Example client:visible tag="kai-tooltip" config={{ content: 'Voice input', openDelay: 150 }} />

## Slots

<SlotTable tag="kai-tooltip" />

## Props

<PropTable tag="kai-tooltip" />

## Methods

<MethodTable tag="kai-tooltip" />

## Composed from

<ComposedFrom tag="kai-tooltip" />

<NeedAHand />
