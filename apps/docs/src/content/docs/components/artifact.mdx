---
title: Artifact
description: A sandboxed preview of an AI-generated artifact.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import EventTable from '../../../components/EventTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-artifact</p>

<p class="kai-lede">Shows a generated page, document, or file beside the conversation, with its code a click away.</p>

<Facts items={['Shadow DOM', 'Sandboxed iframe', 'Preview + Code tabs', 'Configurable toolbar', '4 events']} />

## Preview

<Playground client:only="solid" tag="kai-artifact" />

<Aside type="tip" title="When to use">
When a response produces something runnable or viewable you want beside the chat. Pair it with [`<kai-resizable>`](/components/resizable/) For a split-pane shell and give the parent a height.
</Aside>

## Usage

Point `src` at your hosted artifact and set `files` in JavaScript (it's an array, so it can't be an HTML attribute) for the Code tab.

```html
<kai-artifact src="https://your.app/artifacts/abc/index.html" style="display:block;height:520px"></kai-artifact>
<script type="module">
  import '@kitn.ai/ui/web-components';

  await customElements.whenDefined('kai-artifact');

  const el = document.querySelector('kai-artifact');
  el.files = [
    { path: 'index.html', url: '…/index.html', type: 'html', code: '<!DOCTYPE …>' },
    { path: 'styles.css', url: '…/styles.css', type: 'other', language: 'css', code: '…' },
  ];
  el.addEventListener('kai-navigate', (e) => console.log(e.detail.url));
</script>
```

- **`files`** — each entry needs a `path`; add `url` for the preview, `code` + `language` for the Code tab, and `type` (`html | pdf | image | other`). Folders come from the `/` in paths.
- **Toolbar** — hide any default control with its `no-*` flag (`no-nav`, `no-reload`, `no-home`, `no-path-field`, `no-tabs`); `expandable` and `open-in-tab` are opt-in.
- **Sandbox** defaults to `allow-scripts allow-forms`. Widen it only for trusted content.

## Examples

### Code view

<Example client:visible tag="kai-artifact" config={{ tab: 'code', activeFile: 'app.js' }} />

### Preview only

<Example client:visible tag="kai-artifact" config={{ noNav: true, noReload: true, noHome: true, noPathField: true, noTabs: true }} />

### Open in new tab

<Example client:visible tag="kai-artifact" config={{ openInTab: true }} />

### Standalone

Chrome for an artifact rendered outside a panel.

<Example client:visible tag="kai-artifact" config={{ standalone: true, readonlyPath: true }} />

### Expandable

<Example client:visible tag="kai-artifact" config={{ expandable: true, openInTab: true }} />

## Props

<PropTable tag="kai-artifact" />

## Events

<EventTable tag="kai-artifact" />

## Methods

<MethodTable tag="kai-artifact" />

## Composed from

<ComposedFrom tag="kai-artifact" />

<NeedAHand />
