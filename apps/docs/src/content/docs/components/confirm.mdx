---
title: Confirm
description: Asks the user to approve an action.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import MethodTable from '../../../components/MethodTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-confirm</p>

<p class="kai-lede">Collects an explicit yes or no before something consequential runs, then closes itself.</p>

<Facts items={['Shadow DOM', 'Single kai-card event', 'Resolves on action (no double-fire)', '3 action styles', 'Optional dismiss affordance']} />

## Preview

<Playground client:only="solid" tag="kai-confirm" />

<Aside type="tip" title="When to use">
When the model proposes an action the user must approve: deploying, deleting, sending. For plain choices that aren't approval-gated, use `<kai-choice>` instead.
</Aside>

## Usage

Set `data` in JavaScript (it's an object) and listen for the `kai-card` CustomEvent:

```html
<kai-confirm heading="Run database migration?"></kai-confirm>

<script type="module">
  import '@kitn.ai/ui/web-components';

  await customElements.whenDefined('kai-confirm');

  const el = document.querySelector('kai-confirm');

  el.data = {
    body: 'This will apply 3 pending migrations to production. This cannot be undone.',
    tone: 'warning',
    actions: [
      { id: 'approve', label: 'Run migration', style: 'primary', default: true },
      { id: 'reject',  label: 'Cancel' },
    ],
  };

  el.addEventListener('kai-card', (e) => {
    const ev = e.detail; // { kind: 'action', cardId, action, payload? }
    if (ev.kind === 'action') console.log('chose', ev.action);
  });
</script>
```

- **`data`** — shape: `ConfirmCardData` from `@kitn.ai/ui`. Malformed payloads render an inline error.
- **Action styles** — `'primary' | 'default' | 'destructive'`. Mark one `default: true` to make it the keyboard default; combine with `autofocus` to focus it on mount.
- **Restore resolved state** via `el.resolution = { kind: 'action', action: 'approve' }` — useful when replaying past conversations.

## Examples

### Approve or Reject

<Example client:visible tag="kai-confirm" config={{ heading: 'Run database migration?' }} />

### Destructive Action

`tone: 'danger'` with `style: 'destructive'`: colour and icon both signal severity.

<Example client:visible tag="kai-confirm" config={{ heading: 'Delete files?' }} data="destructive" />

### Choice Set

<Example client:visible tag="kai-confirm" data="choiceSet" />

### Dismissible

`dismissible: true` adds a close (×) affordance that fires `{ kind: 'dismiss', cardId }`; the card stays in the DOM.

<Example client:visible tag="kai-confirm" config={{ heading: 'Send email?' }} data="dismissible" />

### Resolved (Read-only)

<Example client:visible tag="kai-confirm" config={{ heading: 'Run database migration?' }} data="resolved" />

### Error State

An empty `actions` array renders the inline error view and fires `{ kind: 'error', cardId, message }`.

<Example client:visible tag="kai-confirm" data="errorState" />

## Props

<PropTable tag="kai-confirm" />

## Methods

<MethodTable tag="kai-confirm" />

## Composed from

<ComposedFrom tag="kai-confirm" />

<NeedAHand />
