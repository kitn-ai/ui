---
title: Image
description: Displays an image from a URL.
---

import { Aside } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.tsx';
import Example from '../../../components/Example.tsx';
import PropTable from '../../../components/PropTable.astro';
import ComposedFrom from '../../../components/ComposedFrom.astro';
import Facts from '../../../components/Facts.astro';
import NeedAHand from '../../../components/NeedAHand.astro';

<p class="kai-tag-sub">kai-image</p>

<p class="kai-lede">Alt text is required, so a picture never goes out unnamed to a screen reader.</p>

<Facts items={['Shadow DOM', 'https / data: / blob:', 'Alt text required', 'Kit chrome']} />

## Preview

<Playground client:only="solid" tag="kai-image" />

<Aside type="tip" title="When to use">
When you have a *reference*: a photo, a chart your API rendered. For image *data* from a model, use [Image artifact](/components/image-artifact/) instead.
</Aside>

## Usage

Set `src` to the image address and `alt` to a real description. Every form of image URL works: https, a `data:` URI, and a `blob:` object URL the app created itself.

```html
<kai-image src="https://example.com/photo.jpg" alt="A mountain landscape at dusk"></kai-image>

<!-- an inline SVG, no request at all -->
<kai-image src="data:image/svg+xml;base64,PHN2ZyB4bWxucz0i…" alt="A purple star icon"></kai-image>
```

`alt` is required rather than optional: an image with no name is invisible to a screen reader, and a placeholder string ("image") is worse than none.

The element filters no schemes, because an `<img>` cannot execute one. Where a URL is *navigable* the kit applies its own policy; that is a different guard for a different sink.

## Examples

### A hosted image

<Example client:visible tag="kai-image" />

### A second source

<Example client:visible tag="kai-image" data="portrait" />

### An inline data URI

No request, no CDN: the bytes ride in the URL.

<Example client:visible tag="kai-image" data="inline" />

## Props

<PropTable tag="kai-image" />

## Composed from

<ComposedFrom tag="kai-image" />

<NeedAHand />
