# The coupling map

Two things in this repo depend on each other, nothing enforces the link, and it
breaks in front of a user. That was four separate defects in one day. This is the
map of every such pair there is evidence for, so the next one is a lookup instead
of an incident.

**Scan for the thing you are about to touch.** Each row says what else has to
move, how it fails, and what catches it.

- **Enforced by** is the column that matters. It names a command, a test file or
  a CI step. `NOTHING` means the next person to change that side finds out from a
  user, from a red release, or not at all.
- Every row points at a file, a symbol or a command you can run. Nothing here is
  reasoned from architecture.
- No counts, versions or timings that a script can produce — those rot. Where a
  number would go, the command that prints it goes instead.
- This is a map, not a work plan. It proposes no fixes. The unenforced rows are
  collected at the end so they are easy to find.

**Verified against `fd26f798` on 2026-08-15**, row by row, against the tree
rather than against each row's own prose. **Amended on 2026-08-18** on
`feat/composition-catalog` with the rows that branch introduced (the composition
catalog and the acceptance deck), and with two corrections: `web-component-manifest.json`
and `derived.json` are both covered by `verify:generated`, and the rows claiming
otherwise were wrong before this branch, not made wrong by it. Only the rows
touched were re-checked; the rest still carry the 2026-08-15 stamp.

The map is subject to the coupling it documents, and has been bitten by it
twice — both times a branch editing this file merged without being re-read
against what had landed meanwhile. #249 (`b892497b`) merged four seconds before
this file first did, so two rows shipped describing a create-kai release path
that had already changed under them, one of them asserting a script "does not
exist" while naming the `ls` that finds it. #254 merged twenty-eight minutes
after #258 and re-asserted that `release-please-config.json` "carries no
`extra-files`", directly below the row #258 had just added naming the four it
does carry — so it did not go stale, it re-asserted a diagnosis already
corrected on `main`.

That second one was **predicted in writing and happened anyway**. #258's PR body
says: *"When #254 lands, item 10 and its `NOTHING` row should be dropped in
favour of the enforced row. Expect a small textual conflict there."* #254
landed; nothing was dropped. A stale green over code is reachable by a branch
setting; this was a stale paragraph over prose, and **nothing in CI reads
prose**. **Re-read the rows you touch against the merge result, not against the
tree you branched from** — that sentence is the only mechanism this file has.

Related: [`docs/package-consumer-issues.md`](package-consumer-issues.md) for
consumer-facing packaging defects, and `packages/create-kai/README.md`, which
carries that package's own guard table with the incident behind each one.

---

## 1. Release and publish

Failures here surface during a publish, on a tag already cut, which is the worst
moment to learn anything.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| The `packages` map in `release-please-config.json` | the release **branch name**, and the publish loop's literal list in `.github/workflows/release-please.yml` (`for pkg in packages/ui packages/create-kai`) | release-please defaults `separate-pull-requests` from the package count, so adding the second package moved the branch from `release-please--branches--main--components--@kitn.ai/ui` (the merge of #228, `30214c3a`) to `release-please--branches--main`, orphaning the open release PR. Removing a package flips it back. A package added to the config but not to the loop is never published, and nothing says so | **NOTHING** |
| The order of the publish loop | `packages/ui` must publish **before** `packages/create-kai`: create-kai bakes `^<kit version>` into every project it emits | Publishing create-kai first ships a scaffold whose own dependency is not on the registry yet; the user's first `npm install` fails | The loop runs under `bash -e`, so a kit publish failure aborts before create-kai is reached. That the two literals stay in this order: **NOTHING** |
| `"plugins": ["node-workspace"]` in `release-please-config.json` | whether a kit release drags a **create-kai** release behind it, which is the only thing that re-cuts the `^<kit>` pin baked into the CLI bundle. The plugin merges devDependencies into the dependency graph unconditionally and bumps on the graph edge rather than on having a range to rewrite, so create-kai's unrewritable `"@kitn.ai/ui": "workspace:*"` still triggers a release | Remove it and the two packages release independently again: a kit-only release leaves the published CLI pinning the previous kit, the idempotent `npm view` guard correctly skips a republish because create-kai's own version did not change, and the artefact on the registry decays while the tree stays green. That is how `create-kai@0.1.2` came to pin `^0.24.0` after `0.24.0` was deprecated for a critical XSS, scaffolding vulnerable projects with no upgrade path inside their own range (#249, `b892497b`). The ordering rule stops the pin running *ahead* of the registry; only this stops it running *behind* | **NOTHING** reads the `plugins` array. That the plugin fires is observed rather than asserted: `b8db4ca4` and `3a2c8abd` each bumped `packages/create-kai/package.json` alongside the kit and left a `Dependencies · @kitn.ai/ui bumped to …` entry in its changelog |
| `bootstrap-sha` | every package at once. It sits at the top level of `release-please-config.json`, not inside a `packages` entry, and it re-arms whenever the package count grows (`needsBootstrap = releasesFound < expectedReleases`) | Inert today. Adding a third package re-arms it against a sha that will by then be many cycles stale, and the walk re-proposes commits already released | **NOTHING**. No test reads it |
| `include-component-in-tag` | the tag namespace. Unset, it defaults to true for non-root paths — `git tag --sort=-creatordate` shows `@kitn.ai/ui-v*` and `create-kai-v*` | Setting it false makes two packages compete for one bare `v<x.y.z>` namespace and orphans every changelog compare link already written | **NOTHING** |
| Adding a `"."` package | a root `CHANGELOG.md` appears, and the unprefixed `release_created` output starts being emitted again | The workflow's `if:` gates lead with that now-dead output; the repo was already bitten in the other direction when the config path moved from `.` to `packages/ui` and `release_created` evaluated false while the tag and release existed | The `\|\| releases_created \|\| workflow_dispatch` fallback in every gate. **No test** |
| A version bump by release-please | every CDN pin literal the docs, the npm README and the examples carry. `lint:cdn-pins` asserts each **equals** `packages/ui/package.json`, and a bump moves that side only | The release commit is red on a required check, and was: `8d56f1d7` bumped 0.25.0 -> 0.25.1, `test` concluded `failure`, and `@kitn.ai/ui@0.25.1` published a minute later regardless. Ungated that is a permanently red `main`; with the publish gate above it blocks every release instead. Note the guard had already been red one commit earlier (`8baffe4d`, the commit that added it) for a different reason — three create-kai incident narratives naming past versions with no `historical` waiver | `packages["packages/ui"].extra-files` in `release-please-config.json`, which rewrites the live pins as part of the bump; each pin line is wrapped in `x-release-please-start-version` / `x-release-please-end`. `pnpm --filter @kitn.ai/ui run lint:cdn-pins` (required CI) runs `--check-release-wiring`, which fails if a live pin's file is unlisted, if its line carries no annotation, if a frozen version sits ahead of it on that line (release-please rewrites only the FIRST semver per line), or if a `lint-cdn-pins: historical` line is annotated — the release falsifying a record. `packages/ui/tests/scripts/cdn-pins-guard-wiring.test.ts` covers each. Observed rather than assumed: `3a2c8abd`, the first release after the fix, rewrote all four `extra-files` in the release commit itself with no `fix:cdn-pins` landed ahead of it |
| `prepublishOnly` in either `package.json` | the tarball. Both packages ship a gitignored `dist/` | `npm pack` on a fresh checkout emits a tarball with no code and **exits 0** — npm never checks that a `bin` target exists | `packages/create-kai/test/publish-shape.test.ts`, which derives the rule rather than asserting a string, follows `npm run` chains through intermediate scripts, rejects the `prepublish`/`postpublish` near-misses, and carries over-breadth, over-narrowness and vacuity controls. It covers **create-kai only**; `packages/ui`'s hook is unasserted |
| Whether a `verify:*` script is invoked from a workflow | the guard stops guarding, silently. `verify:pack` existed for some time invoked from nowhere automatic | A check that runs nowhere has the shape of the bug it guards against | `publish-shape.test.ts` ("the CI wiring for the tarball verifier", with a would-notice-if-deleted control), `packages/ui/mcp/tests/emitted-project-wiring.test.ts`, and the `packages/ui/tests/scripts/*-guard-wiring.test.ts` set. Guards outside that set: **NOTHING** |
| Anything at all on `main` | the publish gate, which must pass before `npm publish` runs. `release-please.yml` and `test.yml` are separate workflows on the same `push: main` trigger, so they race — and until the gate existed the publish won, because the release job has no `needs:` and nothing else consulted a status | A red `test` on main did not stop `npm publish`. `gh api repos/kitn-ai/ui/commits/<sha>/check-runs --jq '.check_runs[] \| "\(.name) \(.conclusion) \(.completed_at)"'` on `af6f7717` and `12a51dad` prints the race: `release` completed before `test` did, and on `12a51dad` `test` concluded `failure`. A `needs:` cannot fix it (cross-workflow), and neither can gating on the release PR — release-please opens it with `GITHUB_TOKEN`, so its runs sit in `action_required` and never execute (`4b276894`, a release-branch head, carries zero check runs), which is why release PRs merge with `--admin` | `.github/scripts/require-green-checks.mjs`, between release-please and the publish steps: it queries the checks API for `github.sha` — the exact commit being published — and refuses unless every required context is `completed` with conclusion `success`, waiting while they run and denying by default on absence, timeout or an unreadable API. `packages/ui/tests/scripts/publish-gate-wiring.test.ts` fails if it is removed, reordered after the publish, given `continue-on-error`, or if its `if:` drifts from the publish step's |
| The `permissions:` block in `release-please.yml` | the publish gate's API reads. `permissions:` is an allowlist, so an unlisted scope is `none` — the gate needs `checks: read` and `statuses: read` alongside the existing `contents` / `pull-requests` / `id-token` writes | Drop either and the gate cannot read the commit's checks. It fails **closed**, so nothing unvetted ships — but every release is blocked until somebody reads the log, which is the other way this ships broken | `publish-gate-wiring.test.ts` asserts both scopes are granted |
| The required contexts in the branch ruleset | the `--require` list in the publish gate step. `GITHUB_TOKEN` cannot read rulesets — that needs `Administration: read`, which the workflow `permissions:` block has no key for — so the list cannot be derived at runtime and is a literal | A context added to the ruleset is not gated on: the release ships having satisfied only the contexts the literal names. The reverse is safe and loud — naming a context that does not exist makes the gate refuse every release rather than publish one. Print the live list with `gh api repos/kitn-ai/ui/rulesets/18328421 --jq '.rules[] \| select(.type == "required_status_checks") \| .parameters.required_status_checks[].context'` | As of 2026-09-01, ruleset 18328421 requires two contexts, `test` and `storybook-gate` (raised from `test` alone), and `--require` in `release-please.yml` now names both. This list is still hand-typed, still **NOTHING** derives it from the ruleset, and `publish-gate-wiring.test.ts` still only asserts the list is non-empty, not that it matches the ruleset — a third context added to the ruleset would repeat this gap silently |
| Root `.npmrc` | `enable-pre-post-scripts=true` is what makes pnpm fire `prepublishOnly`, `prebuild` and `postbuild`; `build:css`, `build:theme` and `build:api` are only reachable through those hooks | Delete the line and `nx build ui` prints success while generating nothing, while `npm publish` still runs them — CI and the shipped artifact diverge | **NOTHING**. Recorded as the guarantee in `docs/superpowers/plans/2026-06-29-monorepo-migration-pr1.md` |
| Root `.npmrc` | `node-linker=hoisted` means one version of a package wins for the whole workspace, so a devDependency range in **any** workspace package is a workspace-wide fact | An `@types/node: ^22` copied into create-kai downgraded the hoisted `@types/node` for `packages/ui` and timed out its emitted-code suite. Nothing in that failure pointed back | `sharedDevDepsProblem` (`packages/create-kai/src/build-guards.ts`) fails the CLI build on a disagreement — but only between create-kai and `packages/ui`. Starters pinning a different range: **NOTHING**, named at `packages/ui/scripts/verify-starters.mjs` |

---

## 2. What survives `npm pack`

The working tree, the build output and the tarball are three different things,
and the gaps between them are silent by construction.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| Add a dotfile to a starter that npm strips at publish | a row in `STRIPPED_DOTFILES` (`packages/create-kai/src/template-dotfiles.ts`). The rename on the way in (`scripts/build.mjs`), the un-rename on the way out (`src/generate.ts`) and the pack check (`scripts/verify-pack.mjs`) all read that one list | `.npmrc` was written as a copy of the `.gitignore` mechanism rather than a row in it, so `create-kai@0.1.0` shipped two of eight frameworks that died on `ENOENT ... open '.../myapp/.npmrc'` on the first command a user runs. Every local check was green — they read `dist/templates` on disk, where the file always exists | `scripts/verify-pack.mjs` (per-template `_gitignore`, a literal-dotfile scan of the **built tree** because npm strips the file before it reaches the listing, and every file a patch table OPENS must be in the tarball) plus a round-trip test in `test/generate.test.ts` with an explicit vacuity assertion over the same list. Adding a row arms all of them |
| The `files` array (not just its negations) in `packages/ui/package.json` | `ALLOWED_ROOT_PREFIXES` / `ALLOWED_EXACT_FILES` in `scripts/verify-pack-weight.mjs` (Rule 3) — see §4, this pair is a deliberate hand-kept copy of what `files` ships, not a derivation from it | Until 2026-08-27, `files: ["dist", "src", "bin", ...]` shipped the ENTIRE `src/` tree (367 files, 4.27 MiB) when only two `exports` subpaths resolve into it (`./web-component-meta.json`, `./icon-names.json`) — `mcp/mcp/tools/scaffold.ts` (then at `src/agent-tooling/mcp/tools/`, 326.9 KiB) and `src/web-components/web-component-types.d.ts` (311.9 KiB) rode along unreachable by any consumer. `scripts/emit-subpath-dts.mjs` checks declared `types` targets **on disk**, not in the tarball, and until Rule 3 existed `verify-pack-weight.mjs` read the packed listing but only weighed it — a `src/` re-creep of many small files could stay under the total-size ceiling for a while | `verify:pack` Rule 3 (`findRootViolations`, self-tested with `--self-test`): every packed file must live under `dist/`, `bin/`, `frameworks/`, or be one of the exact allowlisted paths — including the two `src/` JSON exports. **Widen `files` and forget to widen the allowlist and the guard fails the build, by design** — that's the derived-list-copy contract in §4 |
| Add a large generated file under `packages/ui/src/` | the allowlist in `scripts/verify-pack-weight.mjs`, each entry carrying a written reachability reason | 300 KB of generated JSON rode along to every consumer for months: nothing imported it, `exports` could not reach it, and no check looked at what gets PACKED | `verify:pack` — a precise rule (every packed file outside `dist/` over the threshold must be allowlisted) plus a crude total-size ceiling as the backstop for growth inside `dist/`, where content-hashed names make an exact allowlist impossible |
| Anything that makes an existing generated file under `packages/ui/src/` or `packages/ui/mcp/` **bigger** | the same allowlist, and `verify:pack`'s 64 KiB per-file ceiling (`MAX_FILE_BYTES`). A generated file does not have to be new to cross it — it only has to grow, and a generator gets more to write every time the tree it reads does | The instance: `mcp/catalog/derived.json` (then at `src/agent-tooling/catalog/`) grew to 97.7 KiB and shipped to every consumer, unreachable — not bundled into `dist/mcp.es.js`, referenced by nothing under `dist/`, and outside the two `src/` paths the `exports` map exposes. **Nothing outside CI caught it**, and CI's own catch was worth little in practice: the required `test` job was red on it from before the branch merged until it was fixed, which is a red nobody was reading. The generator's inputs are the whole web-component set and the whole registry, so this file grows on its own whenever the kit does | `verify:pack` (required `test` job, needs a build). Exclude it with a negation in `files` — `"!mcp/catalog/derived.json"` — after confirming nothing reaches it, and confirm that by EXECUTION: install the tarball into a throwaway app and drive `bin/mcp.js` over stdio. "Nothing imports it" is a grep-strength claim; `component_reference` answering for `kai-chat` out of an install that lacks the file is the other kind |
| An emitted chunk filename in any `packages/ui/config/vite/*.ts` target | the `sideEffects` globs in `packages/ui/package.json`, hand-written against filenames the build produces (`./dist/register-impl-*.js`, `./dist/web-components/*.js`) | A consumer's bundler tree-shakes away every `kai-*` registration. 0.19.0 shipped a blank page with a silent console; Vite 6 and 7 build the same broken package fine | `verify:consumer` (`scripts/verify-consumer-sideeffects.mjs`) packs the build into a throwaway app, bundles it with a Rolldown-based Vite, and counts `customElements.define`. It also WEIGHS minimal-import probes (browser and node conditions, ceilings and vacuity floors held in the script), so the same guard catches a `sideEffects` regression and a tree-shaking one |
| `packages/create-kai`'s `files` or `bin` | `dist/templates/**` and `dist/index.js` must be in the tarball | `npx create-kai` installs cleanly and then finds no template | `scripts/verify-pack.mjs`, wired into `prepublishOnly` and run twice in CI. Its template set is the packed listing **union** the on-disk dirs, because a template that packed to nothing is simply absent from the listing |
| `dedupe:shiki`'s position in the `build` script (`packages/ui/package.json`) | it must run **after** every `vite build --config config/vite/*.ts` invocation (`KAI_BUILD=register`, `index`, `index.server`, `solid`, `solid.server`, and the `split` web-components build — `build:web-components` specifically) and **before** `verify:web-components-bundle`/`verify:react-wrappers`/`verify:shader-lazy`, which read `dist/` as the final artifact | `dedupe-shiki-chunks.mjs` finds near-duplicate `core-*.js`/`engine-javascript-*.js` chunks by scanning `dist/` non-recursively (see the file's own header) — run it before all builds finish and it dedupes a partial `dist/`, missing whichever build hasn't written its chunk yet, so the "duplicate" it should collapse simply isn't there to find. Run it after `verify:web-components-bundle` etc. and those guards would inspect chunks the dedupe step hasn't rewritten yet, which is order-dependent for no reason | **NOTHING** asserts the ordering itself — the coupling is enforced only by the fact that `npm run build`'s `&&`-chain is hand-written in one place and nobody has split it. A reordering would surface as `dedupe-shiki-chunks.mjs` reporting "no near-duplicate shiki chunk families found" on a tree that should have them, which reads as a quiet no-op, not a failure |
| Adding a `vite.config.*` file at `packages/ui/` root | `.storybook/main.ts` — Storybook's Vite builder auto-discovers a root Vite config and merges it in *before* `viteFinal` runs. Until the 22-config consolidation there was one (the register-all library build), so Storybook silently inherited a second `solidPlugin()`, a post-pass esbuild minifier applied to `storybook build`, `build.lib`, `treeshake: false` and `emptyOutDir: true` | A library build config leaks into the Storybook dev server and static build. It does not error: Storybook starts, stories render, and the difference shows up as a duplicated Solid runtime or a mangled `storybook build` output. The inverse (what actually happened here) is silent too — deleting the root config CHANGED Storybook's effective Vite config and no check noticed | **NOTHING** but the absence of the file. `.storybook/main.ts` carries a comment saying so. The explicit fix, if a root config ever has to come back, is `core.builder.options.viteConfigPath` pointed at a path that does not exist |

---

## 3. Values baked in at build time

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| The `define` keys in `packages/create-kai/scripts/build.mjs` | the matching `declare const` in `packages/create-kai/types/globals.d.ts` | Rename one side only: tsc still passes (the `declare` is still there), esbuild emits the bare identifier, and `npx create-kai` throws `ReferenceError` on first run | **NOTHING** |
| `packages/kai/package.json`'s `version` | the `__KAI_VERSION__` define in `packages/kai/config/vite/node.ts` → the MCP's `instructions` (`packages/ui/mcp/mcp/server.ts`), the matching `declare const` in `packages/ui/mcp/mcp/kai-version.d.ts`, and a SECOND substitution in `packages/ui/vitest.config.ts` so the unit suite needs no CLI build | This is the only place the CLI's OWN version is reported — `serverInfo` names the kit deliberately, because that is the API the tool answers describe — so an agent that cannot see it cannot tell a stale CLI from a stale kit. It cannot be read at runtime: `@kitn.ai/kai` has no `exports` map to self-resolve through, and the module runs at two different depths (`packages/ui/mcp/mcp/` under vitest, `packages/kai/dist/` in the bundle), which is the path-arithmetic class this file's neighbours were rewritten to remove. Rename the define key on one side only and tsc still passes — the declaration is still there — while the bundle keeps a bare identifier | `verify:bundle-shape` reads the built `dist/mcp.es.js` back and fails on a leftover identifier or on a missing version literal (mutation-proved both ways, and the mutated artifact was RUN: `node bin/mcp.js` exits 1 with `[kitn-ui-mcp] fatal: ReferenceError: __KAI_VERSION__ is not defined`, so the runtime half is loud and only the build-time half needed the guard). `server.test.ts` anchors the global to kai's manifest opened independently, and asserts `instructions` is non-empty — the SDK spreads it only when truthy, so an empty string is ABSENT from the initialize result rather than an error |
| `packages/ui/package.json`'s `version` | `derivePin()` (`packages/create-kai/src/kit-pin.ts`) → the `__KIT_RANGE__` define → `DEFAULT_KIT_RANGE` (`src/index.ts`) → the pin in every emitted project's `package.json` and the `kit` range in its `kai.json`, which separately records `kitBuiltAgainst` from `__KIT_VERSION__` — the range is the constraint the project was given, that is the point it started from. The `^` shape is written **once**, in `kit-pin.ts`, because `scripts/build.mjs` emits it and `src/pin-guards.ts` grades it — two copies of `` `^${version}` `` is exactly how the emitted pin and its own guard drift apart | Pre-1.0 a caret cannot cross a minor, so a kit minor strands a published create-kai on a range that cannot resolve forward. What stops that recurring is the `node-workspace` row above, not this one | Two halves, and only one is automatic. **The bundle's pin vs the kit beside it in the tree:** `verify:pack` (offline, in create-kai's `prepublishOnly` *and* run twice in the required `test` job, the second time under the npm the release job pins). The artefact-level question can genuinely fail, because `dist/index.js` freezes the pin at build time and an NX cache restore is the live route to a stale one. **Whether that range still resolves to something published and undeprecated:** **NOTHING**. `packages/create-kai/scripts/verify-pin.mjs` is that check and it does exist — added by #249, which merged before this map did. It reads the pin back out of the PUBLISHED tarball, the only place the shipped value lives, and it is referenced by exactly two things: an npm script, and a comment in `release-please.yml` that says "(network, run manually)". `npm run smoke` runs nowhere either |
| The React wrapper build (`packages/ui/config/vite/react.ts`) | the `'use client';` banner Rollup would otherwise strip from the source | `dist/react.js` stops being a client module in an RSC app | `verify:react-wrappers`, which self-tests both directions |
| `packages/ui/package.json`'s `version` | `serverInfo` in `packages/ui/mcp/mcp/server.ts`, resolved through the package's OWN `exports` map (`packageIdentity()`), plus the kit version named in the MCP's `instructions` | The version every MCP harness is told on initialize. This row described a **hand-typed** string that had gone ten minors stale (`0.15.0` while the kit shipped on) and asserted nothing did — that was true when it was written and false by the time it was next read: the derivation and its two tests landed in between, and the row outlived them | `server.test.ts`: it reads this package's package.json off disk (not through the specifier the code under test resolves, so the two sides are separate reads) and compares it against what a client gets back, plus a comment-stripped scan of server.ts that fails ANY version literal. The pair is deliberate — one catches a drifted literal, the other catches a literal typed while it is still correct, which is the state every stale literal starts in |
| The `pdfjs-dist` CDN pin | `PDFJS_VERSION` in `packages/ui/src/primitives/pdf-preview.ts`, which ships in the browser bundle as a live CDN URL | Not cross-checked against any dependency entry. Exact-pinned by design; overridable via `configurePdfPreview` | **NOTHING** |
| `FAMILIES`' markers in `packages/ui/scripts/dedupe-shiki-chunks.mjs` (`createHighlighterCore`, `createJavaScriptRegexEngine`) | shiki's actual exported names for its `core`/`engine-javascript` chunks — these are hand-typed positive controls, not read from `shiki`'s own package | A `shiki` upgrade that renames either export moves the marker out from under the family's filename `pattern`: the file still matches `^core-[\w-]+\.js$`, but the marker string is no longer in its body. The script's own `missingMarker` check is the backstop, and it REFUSES rather than guesses — pushing a `problems` entry ("filenames matched the pattern but the marker went stale") and exiting nonzero, so a stale marker fails the build loudly instead of silently forwarding the wrong export set through an `export *` shim | The self-test's "stale marker" case (`--self-test`) proves the refusal fires; there is no check that the markers still match the CURRENT `shiki` version's real exports outside of running the real `build` (which would hit the same refusal on a genuine rename). Bumping `shiki` is the trigger to re-derive both markers by hand |

---

## 4. Derived lists

One source, many re-fires. These are the good kind of coupling: adding a member
makes every consumer object on its own. They are here so you know what will go
red, and so nobody quietly turns a derivation back into a literal.

| Source of truth | Derived by | What adding a member does | Enforced by |
|---|---|---|---|
| The `MessagePart` union in `packages/ui/src/web-components/chat/chat-types.ts` | three independent derivations: `scripts/lint-silent-drops.mjs` and `scripts/verify-scaffold-compiles.mjs` each TS-parse the union (each with its own anti-vacuity floor), and `src/state/stream.ts` derives a forbidden-key set from it at the type level | A seventh variant re-fires **every waived site** in `src/wire/`, including waivers written before it existed, and requires a `partAs(part(), '<new>')` branch in the emitted Solid `renderPart` | `lint:silent-drops` (required CI, no build, `--self-test`) · `verify:scaffold` (needs a build) · `tsc` plus the `@ts-expect-error` pins in `src/state/stream-types.test.ts`. The best-guarded list in the repo |
| The `MessagePart` union, again — as **authored prose** this time | `partConsumption` in `mcp/catalog/surfaces.ts`, a hand-written table of which web component consumes which variant. Not a derivation: somebody types it, which is the point — it records a fact about the web components that no type expresses | A seventh variant covered by no record fails the lint by name (`variant 'x' is covered by no record`), and so does a record claiming a variant the union does not have, or naming a tag `derived.json` does not | `lint:catalog-drift` (required CI, no build). Its anti-vacuity floor refuses an empty table, and its `--self-test` plants each fault and watches it fire. The union side is read from `derived.json`, so this rides on the row above it |
| `scripts/lib/consumer-tsc-projects.mjs` — the temp consumer tree, the six tsconfig projects and their anti-theatre self-test | `verify-scaffold-compiles.mjs` and `acceptance-gate-compiles.mjs` both compile through it, and `lib/compile-plan.mjs` takes `liftScript` from it. It was extracted out of the first so the second's rubric anchor ("resolving `@kitn.ai/ui` through the shipped exports map") is a claim about the real projects rather than about a copy | Change a project's options and BOTH gates move at once — which is the point, and also the risk: a project relaxed to make one gate green silently relaxes the other. The sandbox self-test is the floor that keeps either from passing over a dead harness | `verify:scaffold` (needs a build) · `gate:compiles:self-test`, whose 16 controls include a sandbox whose tsconfig has been gutted. Neither runs on the other's fixtures, so a change that only breaks one is only caught by that one — run both |
| `codeUnits()` in `scripts/lib/output-scan.mjs` | three consumers now: `gateWebComponentsExist` and `gateAuditClean` inside that same module, and the `compiles` gate, which writes each unit to disk and compiles it | Widening `CODE_FENCE_LANGS` widens what all three see at once. **Read its limits before building anything on top of it**: it extracts only recognised code files and LABELLED fences, so an unlabelled fence is indistinguishable from prose. A branch that read "found no code" as "there is no code" scored an S1 run with zero output files 8.93/10, exit 0 — see the comment at the deleted branch in `scripts/lib/rubric.mjs` | `tests/scripts/acceptance-eval.test.ts` covers the scanner directly (including that a `json` fence and a bare fence yield nothing) · `gate:compiles:self-test`. That the three consumers agree about what code is: **NOTHING** asserts it — they agree by calling one function, which is the cheap kind of coupling and the reason to keep it that way |
| `src/wire/media-types.ts`'s `ENCODABLE` array | `encodableMediaTypes()`, the composer's `accept`, and every scaffolder emitter's `accept` | A new row teaches the encoder and widens the picker at once | `scaffold.test.ts` asserts both the emitted value **and** that no emitter contains a media-type literal at all — the second exists because the first cannot see a copy until `ENCODABLE` next changes, one release too late |
| The integration registry (`packages/ui/mcp/registry.ts`) | `verify:scaffold`'s front-end and route axes (esbuild-bundled and imported at run time, with a refusal to fall back to a hand-written list), the route-contract harness, `listGatewayGroups()`, and `packages/create-kai/src/catalog.ts` | Registering an integration moves the cell counts by itself. No count is pinned in the script; it prints what it ran | `verify:scaffold` · `emitted-route-contract.live.test.ts`, whose exclusions must each name a re-derived reason. **Except** `mcp/mcp/matrix.test.ts`: its loop is derived from the registry but the `SIGNATURE` table it indexes is hand-written and has no key for two registered integrations. Nothing asserts that table covers the registry — contrast `scaffold.test.ts`, which makes exactly that completeness assertion for `COMPONENT_ENTRY` over `Framework.options` |
| The `Framework` enum in `mcp/types.ts` | `scaffold.test.ts` derives its axis from `Framework.options` and says why: "a hand-written list is how a target gets added and covered by nothing" | A twelfth member is covered by that test | **NOTHING** for the compile gate: `verify-scaffold-compiles.mjs` carries a hand-written `FRAMEWORKS` plus parallel `EXT` and `PROJECT` maps, and nothing asserts they equal `Framework.options`. This is the one place the repo's own stated rule is broken inside the gate that states it |
| `listSurfaceProbes()` in `mcp/archetypes.ts` | the surface axis of `verify:scaffold`, and create-kai's feature validation | Read the `WHY NOT THE POWER SET` comment before touching it | `assertSurfacesAreDistinct()` refuses two surfaces with identical component lists, and refuses one without `kai-chat` |
| The `# Runtime:` label the scaffolder emits | `RUNTIME_PROJECT` in `verify-scaffold-compiles.mjs`, which picks the tsc project modelling that host's globals | A new host must be added to the map | **Hard failure** on an unrecognised label, never a skip — the correct shape for a string-keyed map |
| The test spec file name `tests/e2e/input-mask-ivp.spec.ts` and the `input-mask` project's testMatch in `packages/ui/config/playwright/storybook.config.ts` | the `testMatch` pattern **and** the npm script name `test:input-mask-ivp` | Renaming the spec makes it unreachable; a mismatched config and script prevents tests from running | The spec itself — it names both source paths in a comment and carries a plant error that fires if the paths diverge. Since the 14-to-3 consolidation, `verify:playwright-projects` covers the general case: see the row below |
| The `testMatch` pattern of every project in `packages/ui/config/playwright/{storybook,bare,cross-origin}.config.ts` | the spec FILENAMES under `packages/ui/tests/e2e/` | Rename a spec, or edit a `testMatch`, and that project matches nothing. Playwright runs zero tests, prints `0 passed` and **exits 0**, so the npm script is green and the CI step is green while the guard it names has stopped running. That is the whole hazard: every project across the three files is one line away from silently covering nothing, and the guard prints the per-project counts it measured. Nine-in-one adds a second edge: a spec that THROWS AT MODULE LOAD aborts collection for its whole config, so all nine storybook projects report zero at once. A `--project=` run never sees it (it loads only its own files), so this guard, which lists the whole config, is the only thing that can | `verify:playwright-projects` (required CI, `dist-guards` leg, no browser and no server: `playwright test --list` only, ~seconds). It lists every project of all three configs and hard-fails on a project that reports zero tests or that has gone missing entirely, and its `--self-test` plants an unmatchable `testMatch` in a temp copy of a real config and requires the check to fire |
| `packages/ui/src/web-components/register/register-impl.ts` (hand-maintained) | `scripts/gen-web-components-manifest.mjs` writes `src/web-components/web-component-manifest.json`, which is imported at runtime by `src/web-components/autoloader/autoloader.ts`, read by `verify-web-components-bundle.mjs`, `verify-consumer-sideeffects.mjs` and `gen-web-component-dts.mjs`, and drives the per-web-component build entry points | Register a web component and commit without rebuilding and the committed manifest is stale, so the autoloader cannot lazily register it | `verify:generated`, since #265 (`8e20acbc`). This row read `NOTHING` and called it "the one derived artifact missing from `verify-generated-sync.mjs`'s list" for two edits after that stopped being true — see §6 |
| The `src/web-components/` directory | `scripts/gen-web-component-api.mjs` scans the directory, minus a `SKIP` set — a **different** web-component set from the one `register-impl.ts` defines | A facade on disk but not imported by `register-impl.ts` lands in `web-component-meta.json`, the CEM, the docs tables and the React wrappers, but is not in the register-all bundle. That is `remote`'s documented state, and nothing distinguishes it from an accidental omission | **NOTHING** for the divergence itself |
| `src/primitives/card-schemas/` | the expected set in `verify-schemas-exported.mjs` is a `readdirSync` of that directory, never a literal | A new schema is covered the day it is added | `verify:schemas` · `verify:tool-schemas` (every keyword in any authored schema must be classified by both provider tables) |
| An eighth card **type** | `cardSchemas`, `cardSchemaNames`, `cardTools()` and the parity tests all move | `CARD_TYPES` in `scripts/gen-card-validation-schemas.mjs` is a hand list, and the only test over it compares the generated artifact back against **the generator's own list** — a circular check. The browser-side validator silently omits the new type. `BUILTIN_CARD_TAGS` and `BUILTIN_CARD_COMPONENTS` are hand-parallel to it and to each other | **NOTHING** ties `CARD_TYPES` to `Object.keys(cardSchemas)` |
| A `--kai-color-*` token in `packages/ui/theme.css` | `dist/theme.tokens.css` is a transform of it, so that half cannot drift | The `kai` MCP's `theme` tool must name the same tokens it brands. It derives them: `declaredKaiTokens(themeCss)` reads theme.css and its curated `BRAND_TOKENS` are resolved against that set, so a renamed token is a loud failure there rather than a dead name in an agent's paste | **`mcp/mcp/theme.test.ts`** on the derived set (a stale name is an error, not a silent no-op). This row used to claim the tool "hardcodes token names and never reads `theme.css`" — it does read it, and has since `declaredKaiTokens` went in |
| A `@keyframes` name in `packages/ui/theme.css` | every `animate-[<name>_…]` arbitrary value and every `--animate-<name>` variable in `src/`, and the hoisted copies in `dist/theme.tokens.css` | Rename or misspell one and Tailwind compiles an `animation:` that names nothing; the web component renders and silently never animates. Every one is `kai-` prefixed because `theme.css` is `@import`ed into a consumer's Tailwind build (see §9); the old `collapsible-*` pair, which shadowed tw-animate-css's keyframes of the same name and was referenced only from `data-[state=*]` classes on a div that never carries `data-state`, was deleted as dead | `tests/web-components/theme-globals.test.ts`: reads the names out of `theme.css`, asserts the prefix rule (`.chat-markdown` the one allowed exception), resolves every reference in `src/`, and compares the `dist/theme.tokens.css` hoist back to the source. Watched failing on the unprefixed tree before the rename |
| A starter's own `.gitignore` | `templateSkips()` in `packages/create-kai/src/build-guards.ts` decides what is not copied into a template | The basename list it replaced could not express `src/routeTree.gen.ts` or build trees, so template contents differed between CI and a clean checkout | `gitignoreProblem` (a starter must have one) and `templateIgnoreProblem` (a negated rule is refused rather than mis-read) |
| The `exports` map, or `examples/starters/` and `examples/apps/` | `verify-ssr-imports.mjs` derives its entry list from `exports`; `verify-starters.mjs` derives its roster from **both** corpus directories (starters and the ladder's apps) and each package's own kit dependency spec | A new subpath, a ninth starter or a new rung's app is covered the day it lands; an unclassifiable package — or a corpus directory that vanishes — is a hard failure, not a skip | `verify:ssr` · `verify:starters` |
| `DEFAULT_FORMAT` in `packages/ui/src/components/input.tsx` — the `format="default"` opt-in sentinel | `src/web-components/input/input.tsx` imports it to resolve a seeded value ahead of the masker (`throughMask`), which is the one path that must recognise the sentinel without going through the widget | Renaming the sentinel moves both sites at once, because there is one constant and an import. It is an import rather than a repeated literal specifically because the drift here is SILENT: a stale `format === 'default'` comparison simply goes false, `compileMask('default')` compiles seven literals, and a seeded value is garbled with no error anywhere | **NOTHING** asserts the pair, and nothing needs to while it stays one exported constant. The failure mode to watch for is somebody re-inlining the string; `tests/web-components/input-element-mask.test.tsx` (`a value present at FIRST render is masked and canonicalized`, plus the `format="default"` property-set pin) is what would go red if the two sides ever stopped agreeing |
| `FIELD_SEMANTIC_TYPES` in `packages/ui/src/primitives/field-semantics.ts` — the four field-format tokens | **two copies that cannot import it**, both registered here: (1) the `x-kai-format` `enum` in `src/primitives/card-schemas/form.schema.json`, which is JSON and has no imports, and where the enum is the whole point — a small model picks a token off a list it can see (form-field-formats spec §7.3); (2) the union on `FormField['x-kai-format']` in `src/primitives/card-data-types.ts`, spelled out because that file must stay compilable from a Node/no-DOM project and `field-semantics.ts` reaches `console` — measured, TS2584 | A fifth token must be added in all three places. The constant is the authority; each copy is guarded, in a different language, by a different mechanism. Two more consumers ride on copy (1) rather than being copies themselves: the **wire projection** (`cardTools()` in `src/schemas/tool-defs.ts` recurses `form.schema.json`'s `properties` by name, so the enum a model is shown IS the schema's), and the **docs tables** in `apps/docs/src/content/docs/guides/field-formats.mdx`, which are hand-typed prose — see the §9 row | Copy (1): `tests/primitives/form-schemas.test.ts` asserts the schema enum **equals** `[...FIELD_SEMANTIC_TYPES]`, order included — mutation-checked from both sides (drop `custom` from the schema, add `postal` to the constant; each reddens). Copy (2): the mutual-assignability pin in `tests/components/form-field-formats.test.tsx`, which only holds if the unions are IDENTICAL, checked by `nx typecheck ui` — mutation-checked (dropping `'custom'` from the union is TS2322). The runtime companion in the same file drives EVERY token of the constant through `resolveFieldMask` and fails a token with no default format. The projection: `src/schemas/tool-defs.test.ts` ("keeps the FIELD-FORMAT hints, with the enum intact, on all three providers") asserts the projected enum equals `FIELD_SEMANTIC_TYPES` per provider, so a strip-list change that ate the hints goes red. The docs tables: **NOTHING** — §9 |
| The URL scheme lists in `packages/ui/src/primitives/url-scheme-policy.ts` (`SAFE_SCHEMES`, `SCRIPT_SCHEMES`) and `link-preview.ts` (`RENDERABLE_SCHEMES`) | the invariant catalog's `examples[].right` forms, which USED to hand-type the three navigable schemes as a second copy of the policy, because the acceptance floor executed every snippet as a SCRIPT in a vm where `import` fails with "Cannot use import statement outside a module". The floor now resolves a snippet's kit imports to the module that defines each symbol and bundles it (`scripts/lib/kit-imports.mjs`, driven by the package's own `exports` map plus `src/index.ts`'s own re-export statements), so an example imports the shipped predicate and executes that | Add a scheme to a policy list and the catalog's own example runs the NEW list on the next `--floor`, with nothing to keep in step. The reverse — a snippet that restates a list — is refused rather than tolerated: `invariants.test.ts` reads the scheme literals out of those two modules and fails any `right` form that spells one out, so the copy cannot come back quietly | `--floor`, which executes the shipped predicate (mutation-checked by adding `javascript:` to `SAFE_SCHEMES` and watching the catalog's own example open a `javascript:` URL) and whose report NAMES each symbol executed from source rather than asserting it in general terms · `invariants.test.ts`'s scheme-restatement check · the floor's `--self-test`, which plants a name the barrel does not re-export and a snippet importing with no resolver supplied |
| `MAX_FORMAT_LENGTH` in `packages/ui/src/primitives/field-mask.ts` (64) | `maxLength: 64` on `x-kai-mask` / `x-kai-mask-guide` in `form.schema.json`. A JSON literal, unavoidably — the same no-imports problem as the row above | Moving the cap in the engine must move it in the schema, or the model is told a limit the engine does not hold | `tests/primitives/form-schemas.test.ts` derives it BEHAVIOURALLY rather than comparing to a literal: it reads the cap out of the schema, then asserts `compileMask('#'.repeat(cap))` compiles and `cap + 1` throws. A cap that drifted either way fails on one of the two |
| Which generators write into the source tree **after** `dist/` is built — `src/` (`build:css`, `gen-web-components-manifest.mjs`, `gen-web-component-api.mjs`, `gen-web-component-nonscalar.mjs`, `gen-web-component-types.mjs`, `gen-catalog.mjs`), `mcp/` (`gen-construct-schema.mjs`, `gen-construct-template-fixtures.mjs`) and `scripts/` (`gen-blocks.mjs`) | `GENERATED_SOURCES` in `packages/ui/scripts/verify-artifact-fresh.mjs` is a **hand-maintained copy** of that set, each entry commented with the generator that authors it. Not a derivation — the authority is those generator scripts, and nothing relates the two. Two entries are subtree **prefixes** (`mcp/construct/fixtures/templates/`, `scripts/block-driver/pages/generated/`) because those outputs are SETS with derived membership — one file per template, one page per block — which a hand-listed copy would rot in the loud direction below | A new postbuild-written source file that is NOT added there makes a correctly built tree fail `verify:fresh` instantly, because postbuild writes it a second AFTER `dist/`. So the drift direction is **loud**: the guard names the file and the freshness gap on the very next build | **NOTHING** ties the set to the generators. The mitigation is the failure direction, not a check: a missing entry is a false alarm on a good tree (annoying, self-announcing), never a silent pass on a stale one. The inverse — an entry left behind after its generator is deleted — is the quiet one, and nothing catches it. Each exclusion does carry the guard that owns those bytes beside it (`verify:generated` for the `src/` and `mcp/` artifacts, `verify:blocks`' spawned `gen-blocks --check` for the driver pages), so an entry moves coverage rather than dropping it — but that is a comment, not a check |
| The type-rendering helpers in `packages/ui/scripts/_ts-helpers.mjs` (`renderType`, `cleanEmittedType`, `bindsLoosely`, `unwrapOuterParens`) | `scripts/gen-web-component-types.mjs` and `scripts/gen-web-component-react.mjs`, which import them. `clean` used to be **two byte-identical local copies**, one in each emitter, related by nothing | A change to how a type renders moves `src/web-components/web-component-types.d.ts`, `dist/web-components.d.ts` and `frameworks/react/index.tsx` at once, which is the point: fixing one copy and leaving the other is exactly how they diverged. Function types were not parenthesised inside a union or before `[]`, so the union case emitted TS1385 into `dist/web-components.d.ts` and the array case emitted a type that PARSES and is silently wrong (`() => void[]` where `(() => void)[]` was read). The React half is the sharp edge: `frameworks/react/index.tsx` is a shipped consumer entry point (`@kitn.ai/ui/react`), and its breakage was invisible locally because `packages/ui`'s `typecheck` script is `&&`-joined with the Solid `tsc` pass first, so a broken `web-component-types.d.ts` fails before the react project is ever compiled | `packages/ui/tests/scripts/gen-type-render-precedence.test.ts` drives the shared renderer over fixtures, asserts every rendered string parses, and checks the parsed AST **node kind** for the array case, the only way to tell `(() => void)[]` from `() => void[]` when both parse. It covers both directions, so an over-eager fix that parenthesises where parens are noise goes red too, and it asserts `cleanEmittedType` has one owner that both emitters import rather than redefine. What it does not cover: it guards the RENDERER, so a second hand-copied helper of any other name is caught by **NOTHING**. That the generated artifacts are themselves current is `verify:generated`'s job, not this test's (§6) |
| `files` in `packages/ui/package.json` | `ALLOWED_ROOT_PREFIXES` / `ALLOWED_EXACT_FILES` in `scripts/verify-pack-weight.mjs` (Rule 3) — **a deliberate hand-kept copy, not a derivation**: `files` is an npm glob array, there is no runtime import path from a `package.json` field into a `.mjs` script's constants, so unlike every other row in this section this pair cannot be wired together by an import. It is here anyway because the failure mode is the same shape (widen one side, forget the other) even though the fix (parse `package.json` at guard-run time instead of hand-copying) was available and deliberately not taken — the two-copy version stayed because it makes the guard's own `--self-test` legible: fixtures name literal paths, not "whatever `files` currently says" | Widen `files` (e.g. re-add a `src/` glob) without widening `ALLOWED_ROOT_PREFIXES`/`ALLOWED_EXACT_FILES` and `verify:pack` Rule 3 fails the build the next time anything under the newly-widened glob actually gets packed — loud, not silent, and the whole point of Rule 3 existing (§2, the 2026-08-27 `files` trim). Narrow `ALLOWED_EXACT_FILES` without narrowing `files` just leaves stale allowlist entries, which `verify:pack`'s own output flags ("N allowlist entries are no longer packed... and can be dropped") rather than hiding | `verify:pack` Rule 3 catches the dangerous direction (allowlist too narrow for what's shipped) as a hard failure. The safe direction (allowlist wider than what's shipped) is caught only as an advisory note in `verify:pack`'s success output, never a failure — **NOTHING** forces a stale entry to be removed |
| The private `TAG_RE` inside `packages/ui/mcp/construct/schema.ts`, which validates `Construct.name` (the construct schema's custom-element-tag rule) | `CONSTRUCT_TAG_RE` in `packages/create-kai/src/wizard.ts` — **a deliberate hand-mirror, not a derivation**: the regex is a private, unexported `const` (nothing outside its module can import it, including a test), and importing the module it lives in would drag `zod` into the `create-kai` CLI bundle the same way `CONSTRUCT_SCHEMA_URL` did (§3, `bundleGraphProblem` in `packages/create-kai/src/build-guards.ts`) — but that fix doesn't generalize here: this regex is used to DECIDE (`constructTagName` derives a valid name from an arbitrary project name), not restated as a finished fact esbuild can hand over via a `define` | If the schema's tag rule ever changes shape, the mirror keeps generating names against the OLD rule with nothing in `create-kai` announcing that the schema moved | **Not byte-comparison** — the private regex can't be read from outside its module to diff against. `packages/create-kai/test/wizard.test.ts`'s property test runs `constructTagName` over one representative of every character class `validateProjectName` accepts, crossed with both shapes, and `safeParse`s every result against the real, live `ConstructSchema` imported from `@kitn.ai/ui/construct`. A schema-side tightening the mirror doesn't track fails THAT test, not a diff of two regex literals |
| The template registry (`packages/ui/mcp/construct/templates.ts`) — a leaf: data + type-only imports, no zod, so the browser builder, the Node MCP tool and create-kai's bundle all read ONE module | four consumers: the generated fixture JSONs under `mcp/construct/fixtures/templates/` (written by `scripts/gen-construct-template-fixtures.mjs` in build:api), create-kai's template shape axis (`shapeAxis` maps `buildableTemplates()`), `BuilderStart`'s card list (`BUILDER_TEMPLATES` maps `TEMPLATES`), and the MCP `construct` tool's template statements | Adding a buildable template grows the wizard's menu, the builder's card grid and the MCP statements on its own, and its starter is ejected/compiled/built by `verify:construct` the moment build:api writes its fixture. The one DELIBERATE COPY is the fixture JSONs — they exist because `verify-construct.mjs` is an `.mjs` gate that reads JSON fixtures, not TS; a new fixture must also be listed in `verify-generated-sync.mjs`'s `GENERATED`, and that guard now enforces BOTH directions: a listed file going missing is its existing precondition failure, and — since the 2026-08-28 fix wave — a fixture written to `fixtures/templates/` that was never added to `GENERATED` is caught too (`cfg.fixtureDir` globs the directory and fails on anything on disk the list doesn't name), because until then a NEW fixture for a newly-added template was invisible to this guard: never sentinel-planted, never checked for drift | `templates.test.ts` itself only pins the leaf's own import discipline (no zod, no `./schema`, no components) plus the schema-validity of every starter/variant — it says nothing about whether a consumer imports vs. restates. Each consumer instead earns that pin on its own side: `builder-start.test.tsx` asserts `BUILDER_TEMPLATES`/`BUILDABLE_BUILDER_TEMPLATES` equal a map/filter over `TEMPLATES`, byte-for-byte; `wizard.test.ts` and `construct-tool.test.ts` both drive their own test axis by iterating the real `buildableTemplates()` rather than a hand-written list, so a template added there is exercised without either test file changing | `verify:generated` (fixture drift in both the REMOVE and ADD directions, both sentinel/self-test-proofed) · `verify:construct` (the starters actually eject/compile/build) · create-kai's `bundleGraphProblem` (the entry stays zod-free) |
| The `needs:` graph of `.github/workflows/test.yml` -- `test` and the five legs it aggregates | the gate set of `packages/ui/scripts/lint-gate-parity.mjs` (`test` union its transitive `needs:`, parsed from the workflow) and `requiredGateBlock()` in `packages/ui/tests/scripts/lib/required-gate-block.ts`, which thirteen `*-wiring.test.ts` files use to ask whether CI still runs their guard. Neither carries a list of legs; both re-derive the graph, by the same rule, from two independent parsers on purpose | Add a leg and every guard-wiring assertion covers it the day it lands. Drop a `needs:` edge and its steps leave the merge gate while still running on every PR: `--list` would keep printing them, and the required check would go green over checks that gate nothing. Move a gate into a job outside the graph and the same thing happens one level up | `lint:gate-parity` (required, no build): a job carrying a gate-shaped step and not reachable from `test` is a **hard failure** naming the job and the gate, with `storybook` / `storybook-gate` exempt by name in `EXEMPT_JOBS`; a root `test` with no `needs:` and no gates is a hard failure too. Both watched firing in `--self-test`. The helper carries its own vacuity floor and every caller keeps `expect(block).not.toBe('')`, because one derivation feeding thirteen guards is one bug away from blinding all of them. What is **NOT** enforced: that the ruleset's required contexts equal `{test, storybook-gate}` -- see the ruleset row in §1 |
| The `className` field of each web component in `packages/ui/src/web-components/web-component-meta.json` | `scripts/gen-web-component-react.mjs`, which emits one `import type { Kai<Name>Element } from '@kitn.ai/ui/web-components'` and one `createWebComponent<XProps, Kai<Name>Element>` per web component, and `scripts/gen-web-component-types.mjs`, which emits the interfaces themselves into `src/web-components/web-component-types.d.ts` and `dist/web-components.d.ts` | A renamed or removed interface breaks the generated wrapper's import, so `tsc -p tsconfig.react.json` fails by name rather than the wrapper silently degrading to `HTMLElement` refs. The BARE `@kitn.ai/ui/web-components` key in `tsconfig.react.json` and `tsconfig.react.test.json` is what makes that pass see the real interfaces at all; pointed back at `tests/react/web-components-stub.d.ts` the whole import resolves to nothing and every ref type quietly becomes an error rather than a silent widening. That key **must** stay pointed at `src/`, never at `dist/web-components.d.ts`: the first build in the chain empties `dist/`, and `dist/web-components.d.ts` is written by `scripts/gen-web-component-types.mjs` (via `gen-web-component-api.mjs`, i.e. `build:api`) in **postbuild**, after the whole build chain and so long after the react build runs. Its other end is `aliasesExclude: [/^@kitn\.ai\/ui\/web-components$/]` on the `dts()` plugin in `config/vite/react.ts`: without it vite-plugin-dts's `pathsToAliases` resolves that same tsconfig key into a relative `../../src/web-components/web-component-types.d.ts` in the EMITTED declaration, which `srcSpecifiersToDist` beside it cannot repair, because it probes for a `.ts`/`.tsx` source and a `.d.ts` has none, so it bails by design. **Consumer-facing side effect, deliberate and disclosed:** the emitted `dist/react/index.d.ts` now carries that same bare import, so every consumer of `@kitn.ai/ui/react` loads `dist/web-components.d.ts` (345 KB, `wc -c`) into their type program, including its `declare global HTMLElementTagNameMap`, its `declare module 'react'` `JSX.IntrinsicElements` block and its `declare module 'vue'` `GlobalComponents` block. Raw `<kai-*>` JSX is therefore type-legal by default in a React consumer's app, which is the workaround the blocks spec's section 5.2 forbids for a generated block, so that check must stay STRUCTURAL over the emitted source and can never be relaxed to "tsc would have caught it" | `nx typecheck ui` (both React passes) and `verify:generated`, which reruns `build:api` and diffs. `verify:dts` covers the emitted half: the bare self-reference must name a subpath the exports map declares, and it is what caught the relative `src/` spelling vite-plugin-dts wrote before `aliasesExclude` was set. `tests/web-components/types-lib-check.test.ts` compiles the interface file with `skipLibCheck: false`, which is what keeps the newly-reachable `declare module 'vue'` block honest. The REASON the bare specifier has to be there -- which `verify:dts` alone does not cover, since it would also pass if the wrappers stopped importing the interfaces altogether -- is held by `verify:dts:consumer`: its `wrongRef` fixture type-checks the INSTALLED tarball under both `bundler` and `nodenext` and fails if a `@kitn.ai/ui/react` ref stops resolving to `KaiViewStackElement`, which is exactly what a dropped or unresolvable interface import produces. What is covered by **NOTHING**: nobody asserts the two React tsconfigs agree with each other, so repointing one key and not the other leaves the wrappers checked and the tests not; and nothing fails if a generated block starts using raw `<kai-*>` JSX now that it type-checks |
| The handler-prop rule (`onName` in `packages/ui/scripts/gen-web-component-react.mjs`) and the `displayName` / `className` fields of `packages/ui/src/web-components/web-component-meta.json` | `handlerName` and `pascalTag` in `packages/blocks/src/forms/react.ts`. **A RESTATEMENT WITH A GUARD, not a derivation**, and the distinction is the point of this section: `packages/blocks` cannot import from `packages/ui` (that dependency direction is what keeps it free of a build-order dependency on the kit), so the rule is written a second time | A web component or event whose name does not follow the rule emits a wrapper name or a handler prop that does not exist, and the react block form fails to compile in the consumer's project | `packages/ui/mcp/tests/blocks-artifacts.test.ts` -- the suite that already holds BOTH packages' inputs -- asserts `handlerName(event) === onName(event)` for every event in `web-component-meta.json` and re-derives `displayName` / `className` from the tag for every entry, each with an anti-vacuity floor on the roster |
| The esbuild options that strip a block controller's types (`{ loader: 'ts', format: 'esm', target: 'es2022' }`) in `packages/ui/scripts/gen-blocks.mjs` | the same four options in `packages/create-kai/scripts/build.mjs`'s post-copy twin walk. **A COPY, deliberately**: the twin has to be written twice because it travels two ways -- into the emitted item JSON, which the docs site's /blocks section and any registry consumer read, and beside the copied source in the CLI's own tarball -- and the renderer that runs on a consumer's machine cannot strip types itself (`create-kai add` runs on Node >= 20.19, which predates `module.stripTypeScriptTypes`). Two strippers emitting two different files would make `packages/blocks/src/forms/index.ts`'s central claim -- that the docs site's /blocks section shows byte-for-byte what `add` writes -- false | Change one side's options and the same controller ships as two different files through two doors, with nothing failing: both are valid JavaScript | `packages/create-kai/test/block-twins.test.ts` re-derives each twin in `dist/blocks/` from the `.ts` it sits beside and asserts byte equality, with an anti-vacuity floor; `packages/ui/mcp/tests/blocks-artifacts.test.ts` does the same for the emitted item JSON. `verify-pack.mjs` refuses a tarball carrying a `.ts` block source with no `.js` twin |
| `INSTALL_ROOTS` in `packages/blocks/src/targets.ts` | `FormFile.target` on every file every renderer emits (`src/forms/{html,react,cdn}.ts` all reach it through `fileTarget()`), the `/blocks` file tree and `planAdd`, which writes each rendered file's own `target` | A framework's install root moves in ONE place, and every emitted target, every displayed path and every written path moves with it. The ruling the table exists to enforce: the path the site DISPLAYS is the path the CLI WRITES, byte for byte. Move it and the page tells a reader a file lands somewhere it does not, which is found only after running the command | `verify:blocks [react-tree]` re-derives `fileTarget('react', ...)` for every emitted file and names any target that disagrees (self-test class 9 plants one); `packages/create-kai/test/add-targets.test.ts` closes the WRITE side, asserting for every block and every framework form that `planAdd`'s paths AND bytes equal the `target`/`content` in `dist/blocks/f/<id>.<form>.json`, which is the artifact the page displays; `pnpm --filter create-kai run verify:add` re-asks it of the PACKED CLI on a real filesystem; `apps/docs/test/blocks-targets.test.ts` closes the DISPLAY side, asserting for every block and every framework that the target the page shows is the one `fileTarget()` returns |
| The `@source` list in `packages/ui/src/web-components/styles.css` — `@source "../components"`, `"../primitives"`, `"../web-components"`, `"../utils"`, all under `@import "tailwindcss" source(none)` | `src/web-components/compiled.css`, the one sheet adopted whole into every `kai-*` shadow root. `.storybook/styles.css` imports the same sheet and adds its own `@source` lines for the story trees | Auto-detection is off deliberately, so a class string counts only if its file sits under a `@source`d directory. **Move** a component out of one of them — or add a new top-level `src/` directory holding Solid components — and its classes leave `compiled.css` with the build still green and the sheet still valid: the component renders unstyled inside the shadow root, and because Storybook's own sheet scans `src/` more widely the story that shows it looks correct. The `src/ui` → `src/components` merge removed one `@source` line for this reason; the `@source not` patterns are the other half, excluding the stories and tests sitting inside those directories | **NOTHING**. The §6 row covers `styles.css` → `compiled.css` staleness (a missing sheet), not a directory missing from the `@source` list. The merge's verification was a hand-written probe — every class token used by a moved file present in `compiled.css` — and it is not committed, so the next `src/` directory to gain components re-runs this from scratch |

- `packages/blocks/package.json`'s `exports` map -- parsed by `packages/ui/scripts/gen-blocks.mjs` and `packages/ui/scripts/verify-blocks.mjs` to find the registry entry for their esbuild round trip; also resolved normally by `packages/create-kai`'s CLI bundle, the docs site's astro build, Storybook's builder and four tsc passes. Renaming or restructuring a key leaves the two generators unable to locate the entry -- they fail loudly and by name (`@kitn.ai/blocks has no exports["."].default`), which is the designed outcome; the failure mode worth naming is the opposite one, adding a key nothing reads. Enforced by the two generators' explicit hard failure, plus `pnpm --filter @kitn.ai/ui run verify:blocks`, which drives both. There is no check that every exports key has a consumer -- that is exactly the rot the dead `dist/agent-tooling/blocks/*.d.ts` emission was, and it took a package move to notice.
- The "a directory is a block iff it holds a `registry-item.json`" walk -- authored four times: `packages/ui/scripts/gen-blocks.mjs`, `packages/ui/scripts/lib/scan-blocks.mjs`, `packages/create-kai/src/blocks.ts`'s `loadBlocks`, and `packages/ui/mcp/tests/blocks-artifacts.test.ts`. PR B Task 11 lifted the walk out of `verify-blocks.mjs` into `scripts/lib/` so the react runtime cell could share it instead of authoring a fifth; `gen-blocks.mjs` still keeps its own, because it runs first in `postbuild` and reports the scan in its own vocabulary. Enforced by NOTHING except that a walk drifting from the generator's would change the block-id set `verify:blocks` reads. The package itself cannot hold the loader, because it imports no `node:*`.
- A block source under `packages/blocks/blocks/` -- couples to `packages/ui`'s cached `build` output (`dist/blocks/**` and the generated driver pages), which `gen-blocks.mjs` writes in `postbuild`. `nx.json`'s default `namedInputs` are `{projectRoot}/**/*`, and the block sources are no longer inside `ui`'s project root -- the only thing that makes a block edit invalidate `ui`'s build hash is the project-graph edge `ui -> blocks`, created by the `workspace:*` devDependency in `packages/ui/package.json`. Drop that devDependency -- because "nothing in `src/` imports it", which is true -- and `nx build ui` can serve a cache hit over changed block sources and print success. Enforced by NOTHING automated: the edge is asserted once, by hand, in this move's Task 2 Step 10 (`nx show projects` lists `blocks`; the graph's `ui` dependencies include `blocks`). `verify:blocks` would eventually catch the stale artifact through its `fresh` check, but only on a run that did not itself come from cache.

---

## 5. Tool versions

Anything that parses another tool's output is coupled to that tool's version, and
the versions differ between jobs.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| The npm the release job installs (`npm install -g npm@…` in `release-please.yml`) | the same version in `test.yml`'s second `verify:pack` step | npm 12 moved the top level of `npm pack --json` from an array to an object keyed by package name. `verify:pack` is the second half of create-kai's `prepublishOnly`, and npm 12 was the one npm it had never run under — so it threw at publish time, after the kit had already gone out, leaving a broken create-kai on the registry with its own fix unpublishable | `packages/create-kai/test/publish-shape.test.ts` asserts the two workflows name the same version, with a vacuity control and a drift control. The parser (`scripts/pack-listing.mjs`) is separately graded against committed captures of both shapes |
| The npm that `packages/ui`'s scripts run under | `scripts/verify-pack-weight.mjs`, `scripts/verify-consumer-sideeffects.mjs` and `scripts/verify-dts-consumer.mjs` all read a pack listing, and all three used to assume the pre-npm-12 array shape (`JSON.parse(...)[0]`) | The same defect, one package over, in three places. Measured against real npm 12.0.2 packing `packages/ui`: two threw `TypeError … reading 'filename'`, and `verify-pack-weight.mjs` — which sliced from the first literal `[`, landing inside the `files` array — threw a `SyntaxError` rather than the legible `undefined.files`. Unlike create-kai's, none of the three runs at publish time (`packages/ui`'s `prepublishOnly` is only `npm run build`), so this was a CI-and-local defect, not a release-breaking one | All three now import the shared parser, which moved up to `scripts/pack-listing.mjs` so neither package owns it. `lint:pack-parse` (required CI, no build) keeps every script that spawns `npm pack --json` routed through it and rejects the hand-parse spellings; its `--self-test` carries the destructuring probe that beat an earlier version, and its docblock states the measured detection boundary. The pinned-npm CI step now runs `packages/ui`'s `verify:pack` alongside create-kai's, so both are exercised under the npm the release publishes on |
| `node-version` in any workflow | all of them. `release-please.yml` says so in a comment: it MUST match `test.yml`, or you publish a compiled package built on a Node major nothing tests on | Prose only. Related and also unguarded: the pinned npm declares an `engines.node` floor against a floating `node-version: 22`, and a floor moving raises `EBADENGINE` at publish time on a cut tag | **NOTHING** for the match. The exact npm pin is the mitigation for the engines half |
| `VITE` in `packages/ui/scripts/verify-consumer-sideeffects.mjs` | what the consumer guard actually proves. It pins a Rolldown-based Vite specifically because the Rollup-based one passes on a broken package, and the kit's own devDependency is the older major | A floating range resolved from the registry at run time, so a new minor changes what CI tests with no commit. Failure reads as missing web-component registrations, not as a version problem | **NOTHING**, on either the pin or its deliberate divergence from the kit's own Vite |
| `typescript@5` in `packages/ui/scripts/verify-dts-consumer.mjs` | what the shipped `.d.ts` are type-checked against | Same floating-range exposure | **NOTHING** |
| A route host's typings | they must stay **real devDependencies** (`@sveltejs/kit`, `@tanstack/react-start`, `express`, `@cloudflare/workers-types`, `@angular/ssr`, `ai`, `@langchain/*`, `@mastra/client-js`) | A `declare module` stub resolves every SDK call to `any` and the gate goes back to proving nothing | `verify:scaffold` runs a per-project probe that must produce a specific tsc error, so a package degraded to `any` fails loudly |
| A starter's tsconfig | the harness copies of those flags in `verify-scaffold-compiles.mjs` — `experimentalDecorators` + `noPropertyAccessFromIndexSignature` for Angular, `jsx: preserve` + `jsxImportSource` for Solid, and the explicit `types`/DOM split for routes | The harness goes on compiling against a config no consumer ships. `request.json()` is `Promise<any>` under the DOM lib and `Promise<unknown>` under undici's, which is why the route projects exist at all | **NOTHING** ties the harness's copies to the starters' real ones |
| `nx.json`'s `build.outputs` | what a cache restore puts back. A new emitted directory not declared there restores a partial `dist/` that nothing re-checks, and the lifecycle never runs to notice | Local only today: CI caches the pnpm store, not `.nx/cache`, so the build always runs cold. That mitigation is accidental | **NOTHING**. Hoisting `verify:dts` to its own CI step buys independence from the caching model but asserts nothing about `outputs` |

---

## 6. Generated files that live in the source tree

Some derived artifacts are committed, so a stale one shows up in a diff. Some are
gitignored, so a stale one is invisible. The build writes both into the **source**
tree, which is why an NX cache hit can restore a "successful" build that
regenerated nothing.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| Anything `build:api` reads (web-component facades, slots, parts, icons — and, since `gen-catalog.mjs` joined the chain, the integration registry, `archetypes.ts`, the `MessagePart` union and `theme.css`) | every committed artifact the chain writes. Read the list where it lives — `GENERATED` in `scripts/verify-generated-sync.mjs` — rather than from a copy here; it currently covers `src/web-components/web-component-meta.json`, `web-component-manifest.json`, `web-component-nonscalar.json`, `icon-names.json`, `web-component-types.d.ts`, `frameworks/react/index.tsx`, `llms.txt`, `llms-full.txt`, `mcp/catalog/derived.json`, and the marked blocks of `docs/web-components.md` | A new `::part()` was registered once with none of them regenerated. Nothing went red, and because the `kai` MCP's `component_reference` and the docs site's tables read those files, a part that existed in the shipped web component was invisible to every tool a developer would use to find it | `verify:generated` (required CI). It runs the real `build:api` and diffs, never through NX; seeds each artifact with a single-use sentinel so a generator that no-ops or was deleted goes red instead of reading as in-sync; `--self-test` proves the comparator half still detects drift. Regenerate with `pnpm --filter @kitn.ai/ui run build:api` |
| `src/web-components/register/register-impl.ts` | `src/web-components/web-component-manifest.json` — committed and generated | Register a web component and commit without rebuilding: the autoloader cannot lazily register it | `verify:generated`. **This row and §4's used to say it was the one derived artifact missing from that guard, and that was already false** — #265 (`8e20acbc`) added it on 2026-08-15, before this file's last edit. The claim was carried forward twice without being checked against the list it describes, which is the failure this map exists to prevent |
| The sources `gen-catalog.mjs` reads (`web-component-meta.json`, the `MessagePart` union in `src/web-components/chat/chat-types.ts`, `mcp/registry.ts`, `archetypes.ts`, `theme.css`) | `mcp/catalog/derived.json` — committed, generated, and **never** hand-edited | It is the ground truth `lint:catalog-drift` resolves every authored catalog claim against, so a stale one lets the lint bless prose that no longer matches the tree while reporting a clean run. Neither guard alone is sufficient: `verify:generated` keeps `derived.json` faithful to the tree, `lint:catalog-drift` keeps the prose faithful to `derived.json` | `verify:generated` (required CI, `probe: 'overwrite'`). It does **not** ship to consumers — see §2 |
| A card schema | `src/primitives/card-validate-cards.ts` — committed and generated | | `verify:card-validation`, the same generator run with `--check` |
| `src/web-components/styles.css` | `src/web-components/compiled.css` — gitignored, produced by `build:css` via `prebuild` | A fresh clone or worktree without it fails a large batch of tests on `Failed to resolve import "./compiled.css?inline"`, which reads like a broken checkout rather than a missing step | **NOTHING** catches staleness; a bare `vitest` does not run `prebuild` |
| What `nx build ui` writes into the **source** tree (`src/web-components/compiled.css` via `prebuild`, `scripts/block-driver/pages/generated/` via `build:blocks`) | the `path:` list of the `Upload the kit build for the downstream legs` step in `.github/workflows/test.yml`. That list is the artifact's `outputs:`, and four legs (`construct`, `unit`, `dist-guards`, `browser`) see exactly what it names | A downstream leg never runs `postbuild`, so a build-written path the glob omits is **absent** there rather than stale, and it surfaces three jobs later as a failure that reads like a broken build. The same hazard the `nx.json build.outputs` row describes, promoted from local-only to live CI by the split. A second, quieter half: `actions/upload-artifact` roots the artifact at the least common ancestor of its paths, so one glob outside `packages/ui/` moves the root and every `download-artifact` lands its files somewhere else | `pnpm --filter @kitn.ai/ui run verify:artifact-glob` in the `build` leg. It reads the glob out of the upload step rather than restating it, diffs `git status --porcelain --ignored=matching packages/ui` across the build, and fails on any uncovered path that is not a named runtime cache with a written reason. It refuses to run with no pre-build snapshot, and treats an empty `git status` read as its own breakage rather than a clean tree. `packages/ui/tests/scripts/artifact-glob-guard-wiring.test.ts` fails if CI stops invoking it |
| How you regenerate | never run `scripts/gen-llms.mjs` standalone. Its no-argument path falls back to reading `dist/custom-elements.json`, which drops each slot's `mode`, so it silently rewrites `llms-full.txt` with **less** data than `build:api` produced. The file says so itself, and there is deliberately no npm script for it | The oversized diff is the only tell | `verify:generated` catches the result, not the act |

---

## 7. The scaffolder and the kit

The scaffolder emits code as string literals, so the compiler is not watching. It
is coupled to the kit at three seams: the request shape, the web-component API, and the
starters it copies. Its templates **are** `examples/starters/*`, copied verbatim
apart from the patch tables.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| The chat request shape | `chatRoutePreamble` in `packages/ui/mcp/route-emit.ts` — one function, consumed by both the `kai` MCP and `create-kai`. Its symbol list is derived from the declaration text, not restated | The kit's own tests never see the emitted file; the assembly step is where declarations go missing | `routeSymbolsProblem` (`packages/create-kai/src/build-guards.ts`) grades the emitted TEXT for each declaration; `verify:scaffold` compiles it against the shipped exports map and `.d.ts` |
| The wire's content-part encoding | the preamble's `wireParts`/`wireText` branches are a hand-written restatement of what `toOpenAIMessages` produces | A silent mismatch: the route compiles, and the model receives a part it cannot read | **NOTHING** relates the two by type — the preamble is a string |
| A web-component **property** name | the emitted front ends in `mcp/tools/scaffold.ts` | Caught: the `emitted` vitest project mounts a real `<kai-chat>` and reads `chat.messages` / `chat.loading` off it | `pnpm --filter @kitn.ai/ui exec vitest run --project=emitted` |
| A web-component **event** name or its `detail` key | the same emitted front ends, once per framework flavour (`addEventListener('kai-submit', …)`, `@kai-submit`, `onkai-submit`, `(kai-submit)`) | **Not caught.** The live test synthesizes the event itself — `chat.dispatchEvent(new CustomEvent('kai-submit', …))` in `packages/ui/mcp/tests/emitted-mock-path.live.test.ts` — so the emitted listener hears the name the test chose, not the web component's. `verify:scaffold` sees strings; `scaffold.test.ts` asserts strings against strings | **NOTHING** |
| A web-component **attribute** name (`suggestion-mode`, `variant`, `removable`, `accept`, …) | the emitted markup | Attributes are inert strings there, and the shipped React JSX augmentation types every web component as `{ [attr: string]: unknown }` | **NOTHING** for the scaffolder. The docs site does cross-check attributes against `web-component-meta.json`; nothing under `mcp/` or `create-kai/` reads that file |
| A starter's text that a patch matches | the `find` in `PATCHES` or `GATEWAY_PATCHES` (`packages/create-kai/src/patches.ts`) | A reworded comment would ship a half-patched project — `workspace:*` instructions, or a browser tab that does not name the user's project | `patchMatchProblem` (zero matches is fatal; more than one is fatal without `multiple`) and `emittedContentProblem`, which patches in memory and greps the result |
| A starter's file layout | `framework.paths` in `packages/create-kai/src/frameworks.ts`, copied verbatim into the emitted `kai.json` and quoted in the emitted README | Nothing opens those paths at build time, so a row copied from the one above diverges silently — `solid` declared a stylesheet path its starter never had, for as long as the row existed | `appPathProblem` and `declaredPathsProblem`. Neither covers a framework still marked `planned` |
| What a gateway's route hands the browser | `BROWSER_WIRE` in `packages/create-kai/src/catalog.ts` must match the reader the go-live patch emits | The worst silent failure in the CLI: `readOpenAIStream` does not throw on a dialect it cannot read, it yields nothing, and every turn ends as an empty bubble. It cannot be derived from `streamFormat`, which describes the provider — `anthropic` is `native` and still correct here because its route re-frames | `routeWireProblem` — a declaration, not a derivation, so wiring a gateway forces someone to answer the question |
| An import in any starter | the kit must export it | create-kai pins a **published** range while starters track the workspace kit. When `parts[]` landed in the tree but not on npm, emitted projects installed cleanly and then failed the user's build | `test/kit-contract.test.ts` against the workspace kit. Against the **published** kit it is manual: `KAI_KIT_ROOT=<extracted tarball> npx vitest run test/kit-contract.test.ts` |
| What the CLI bundle imports | it must not reach `mcp/mcp/` or `zod`. Read from the leaf modules at the root of `mcp/` instead | Three symbols out of `mcp/tools/scaffold.ts` took `dist/index.js` from 203 kB to 904 kB and every other check stayed green — the emitted output was byte-identical. The cause is a module-scope schema build, a property of the module rather than of what you import from it | `bundleGraphProblem`, which grades the esbuild metafile's real module graph rather than a byte ceiling |
| `mergeToolArgsIntoFormDefaults` in `packages/ui/mcp/construct/codegen.ts` (CD-1, Task 19g) | **the same function exists twice on purpose**: a real, exported module-level version (what this file's own tests import directly) and an EMITTED-STRING twin inside `emitCardsImport`'s return value, which App.tsx gets verbatim — construct-glue code the eject artifact must own standalone, same as every other piece of logic this file emits inline, so it cannot just `import` the kit copy. The two are kept in sync **by hand** | A future edit to one (recursion depth, precedence between a model arg and a static default, a new hint key) can drift silently from the other — `verify:construct`'s tsc pass proves the twin **compiles** under a real cell's strict tsconfig, but nothing before this proved it **behaves** the same | `codegen.test.ts`'s `CD-1 drift guard` describe block: extracts the twin's actual source out of a generated App.tsx, strips its TS annotations with the real TypeScript compiler (not `.toString()` on the real function, which would lose the annotations the emitted copy needs for strict tsc), executes it via `new Function`, and runs a shared case table (merge / unknown-key / precedence / mask-keys-preserved) against both, asserting identical output |

---

## 8. The module graph

This decides what could be split into its own package, and it is the layer with
almost no automated check — two bullets below are the exceptions.

- **`packages/ui/apps/**` (the two dev-tool apps: `builder`, `theme-studio`)
  imports `../../src/**` by relative path**: they live outside
  `src/`, so the library tsconfig (`src/**` only) and the dts emit never see
  them. Enforced by: `tsconfig.apps.json`, which is in the `typecheck` chain
  (`packages/ui/package.json`'s `typecheck` script), so a broken relative
  import into `src/` fails `nx typecheck ui` / `pnpm --filter @kitn.ai/ui run
  typecheck` — a required CI check.
- **`wire/` → `state/` is one-directional.** `src/wire/consume.ts` imports runtime
  values from `src/state/parts.ts`; nothing in `src/state/` imports `wire/` outside
  tests. Verify: `grep -rn "from '\.\./wire" packages/ui/src/state/`.
- **Both reach into `web-components/` and `components/` for the type model** —
  `chat-types.ts`, `tool-types.ts`, `attachment-types.ts` — and `state/parts.ts`
  additionally takes one **value**, `classifyTool`, from `components/`. Those
  modules are a DOM-free shared type core that happens to live inside two
  component directories; they are not the component tree.
- **`components/` ↔ `web-components/` is a genuine cycle with value imports both ways.**
  These two cannot be separated.
- **`src/components/{construct-form-paths,builder-panel-derived,builder-start}`
  import `mcp/construct/*`, and since the 2026-09-02 move the source depth and
  the dist depth no longer match.** Their emitted declarations carry the source
  specifier verbatim, so `'../../mcp/construct/schema'`, correct from
  `src/components/`, points from `dist/components/` at a directory `files` does
  not ship. It is rewritten at emit time to `'../agent-tooling/'` by the
  `beforeWriteFile` hook on the `index` target in
  `packages/ui/config/vite/lib.ts`. Without it a consumer's tsc cannot resolve
  `Construct` and the build still succeeds. Read the count off the tree, never
  off this line: `grep -rln "\.\./agent-tooling/" packages/ui/dist --include='*.d.ts'`.
  Enforced by: the hook itself, which THROWS on any depth it does not know,
  naming the file. That is the mechanism. `verify:dts` (run from `postbuild`)
  is the backstop: it fails on any relative specifier in an emitted declaration
  that resolves outside `dist/`, but only after the whole emit, and it names
  the file rather than the depth.
- **`mcp/` is independent of the components** but takes one runtime
  value from `wire/media-types`, and imports the kit's own `@kitn.ai/ui/schemas`
  through the public specifier deliberately — a relative import of that barrel
  would drag the card registry in and force `jsx` + DOM into `tsconfig.mcp.json`.
  Web-component data reaches the MCP by reading `dist/custom-elements.json` at runtime.
- **Two `exports` subpaths resolve into `src/`**, so `src` has to stay in `files`
  — see §2.
- **Enforced by: NOTHING**, for every bullet above except the `apps/**` one and
  the `dist/components` depth-mismatch one. No
  dep-cruiser config, no ESLint anywhere in the repo, no test asserting the rest
  of the module graph. The nearest things are shape guards (`verify:dts` over
  emitted declarations, `verify:ssr` over entry points) and `tsconfig.mcp.json`'s
  DOM-free `lib`, which is what actually keeps `mcp/mcp/**` honest —
  and which does not cover the leaf modules at the root of `mcp/`.

---

## 9. Docs and machine surfaces that restate code

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| A web-component name, prop, event or `::part()` | every fenced code block, every `kai-*` markup sample and every symbol named in prose under `apps/docs/src/content/docs/` | Docs are the one consumer surface with no compiler behind them | `pnpm --filter @kitn.ai/docs run verify:docs` (required CI). It reads the built `dist/*.d.ts` through the package's own exports map and `src/web-components/web-component-meta.json` at run time — nothing baked in — compiles every snippet, and checks markup, events, parts and the scalar-vs-array attribute split against the same source. Its anti-theatre self-test must go red first, so `0 findings` cannot mean "the kit types resolved to `any`". It gates on `high` only |
| Anything a doc states as a **number or a version** | prose, not symbols, so `verify:docs` does not look | `guides/introduction.mdx` still states a rounded web-component count on a page whose own generated index prints the real one. The version half was the same defect and was live: `packages/ui/README.md` and three `apps/docs` guides illustrated CDN pinning with versions inside a Critical advisory range | Versions, for `@kitn.ai/ui@<version>` literals only: `lint:cdn-pins` (required CI), plus `--check-release-wiring` for whether a release still updates them. Every other number, counts included: **NOTHING** |
| A web-component tag, event, property, invariant id, corpus path or inventory title | the **authored** catalog records under `mcp/catalog/` — `invariants.ts`, `surfaces.ts`, `scenarios.ts`, `labs-titles.ts`. These are prose written by a person about a tree that moves, which is the same shape as a docs page and fails the same way | An authored record naming a web component or event that no longer exists reads as authoritative to every agent the `kai` MCP serves, and there is no compiler behind a string in a catalog record any more than behind one in an `.mdx` | `lint:catalog-drift` (required CI, no build): every authored claim must resolve against `derived.json` and the tree, with an anti-vacuity floor per record kind and a `--self-test` that plants each fault. **Its ground truth is `derived.json`, so it inherits that file's freshness from `verify:generated`** — the two together, and neither alone. What it cannot check is whether a record is *right*: a claim that resolves to real symbols and describes them wrongly passes |
| An invariant's `enforcedBy` or `status` in `catalog/invariants.ts`, or a recipe's `wiring` in `surfaces.ts` | what `component_reference` tells an agent about coverage. `coverageOf()` in `mcp/mcp/tools/reference.ts` renders the `enforcedBy` kind into a sentence and switches on `status`, so the MCP's wording is a projection of those two fields, never a second copy | Overstatement is the failure, not staleness: three catalog records are enforced by nothing and two more by half of what they say, and a reference tool that printed every rule as an equal-looking bullet would put that overstatement back one layer further out, where an agent meets it and a reader of the catalog cannot see it | `mcp/mcp/reference.test.ts` ("component_reference does not overstate what the catalog enforces") · `invariants.test.ts`, which pins `status: 'open'` ⟺ `enforcedBy.kind === 'none'` in both directions, the equivalence that makes the unreachable branch in `coverageOf()` unreachable |
| Anything in `README.md`, `packages/ui/README.md` or `context7.json` | `verify:docs` scans `apps/docs/src/content/docs` only | The `kai-` contract is restated in `context7.json`'s `rules` and in both READMEs, and is served to agents from there | **NOTHING** |
| How a list component **keys its rows** (the reference-keyed `<For>` in `conversation-list.tsx` and `thread.tsx`) | the re-render rule, restated wherever a consumer meets it: every framework guide under `apps/docs/src/content/docs/guides/frameworks/`, both READMEs, the starter READMEs, root `CLAUDE.md`, `debug.ts`'s no-re-render advice and the scaffolder's emitted comments. `grep -ril "new array" --include=README.md --include='*.mdx' .` finds the live ones | The docs stated the weaker half — "a new array reference per chunk" — for as long as the rule has existed. It is not sufficient: an item mutated in place renders stale inside a brand-new array, because a row whose item identity is unchanged is never re-invoked. #224, the first issue filed from outside the project, was a user following the documented rule exactly and getting a stale sidebar. Nothing was wrong with the web components | `packages/ui/src/components/reactivity-contract.test.tsx` pins the **behaviour** (both halves, plus that adds/removes/reorders need only the fresh array), so a change to the keying goes red and names the docs to update. Its stale-case assertions are each paired with an update case over the same harness, so "nothing rendered" cannot pass vacuously — verified by mutation. That the **prose** still matches the behaviour: **NOTHING**, and deliberately so — a grep for one phrasing of a rule cannot prove the absence of another phrasing of the wrong one |
| `FIELD_SEMANTIC_TYPES` in `src/primitives/field-semantics.ts`, or the mask token vocabulary in `src/primitives/field-mask.ts` (`#` `@` `*`, positional literals, the 64-char cap) | the tables and prose in `apps/docs/src/content/docs/guides/field-formats.mdx` restate both, plus the semantics table's per-type `inputmode` / `autocomplete` / default-format / canonical column, the `kai-input` blurb in this repo's own `docs/web-components.md` (the hand-written line above the generated block — the generated tables move on their own via `verify:generated`), and the field-format bullet in `apps/docs/src/content/docs/components/form.mdx` | A fifth semantic token, a new mask token or a moved cap ships with docs teaching the old contract. `verify:docs` compiles snippets and checks `kai-*` markup and attribute names against `web-component-meta.json`, so a RENAMED attribute in a sample is caught — but an enum member, a token glyph or a numeric cap in a prose table is not a symbol it resolves | **NOTHING** for the tables and prose. The enforced neighbours are §4's `FIELD_SEMANTIC_TYPES` row (code-side copies) and `MAX_FORMAT_LENGTH` row (schema cap); the docs restatement is exactly the §9 shape: prose about a tree that moves |
| Register an integration | the docs site's integrations section is a hand-written page set | Several integrations the scaffolder supports have no page. Compare `ls apps/docs/src/content/docs/integrations/` against `ls packages/ui/mcp/integrations/` | **NOTHING** |
| Add a docs page | the hand-listed sidebar in `apps/docs/src/topics.mjs` (astro.config.mjs hands that array to `starlight-sidebar-topics`) | A slug with no page fails the Astro build; a **page with no sidebar entry is invisible and silent**. Undocumented-web-component coverage is reported by `verify:docs`, not failed on | Partial — one direction only |
| Add a top-level topic (a header nav entry) | the docs header, `apps/docs/src/components/overrides/Header.astro` | The header used to carry its own hardcoded copy of the nav, so the Storybook topic added to the plugin config in #361 was live in the sidebar switcher and absent from the header. The breakpoints are the second half of the same coupling: the nav collapses at `--breakpoint-nav`, and the in-sidebar switcher has to appear at exactly that width or a band of the viewport has no topic navigation at all | Closed by construction. Both read `src/topics.mjs`, and both breakpoints are named tokens in `app.css` that the header classes and the switcher media query resolve from |
| Anything `packages/ui/theme.css` declares at top level: `@custom-variant dark`, the `--color-*` / `--radius-*` / `--radius-pill` / `--code-radius` / `--text-*` / `--spacing` / `--shadow*` / `--font-weight-*` names in `@theme`, `@utility` rules, and any global class | the **consumer's** Tailwind theme. The file is `@import`ed into their build, so a new declaration here is a new edit to their `dark:` variant, their `rounded-*` / `text-*` scales or their utility namespace, and the list of those edits is prose in `guides/theming.mdx` ("What theme.css changes in a Tailwind build") plus the header comment in `theme.css` itself | Add a `--spacing-*` or a `--font-*` to `@theme` and a consumer's `p-4` or `font-sans` moves with no doc saying so; the two lists drift the day either is edited alone. THIS HAPPENED: `--spacing` was re-pointed at `--kai-density` on 2026-09-18, and both lists were updated in the same change — which is the process working, not a guard firing | **NOTHING** for the list. The prefix on global *names* (keyframes, classes) is enforced by `tests/web-components/theme-globals.test.ts` (§4); which Tailwind namespaces the file re-points is documented, not derived |
| `packages/ui/theme.css` | `dist/theme.tokens.css`, a transform of it by `scripts/build-theme-tokens.mjs` on every build (`build:theme`) | Cannot drift, by construction — but the transform decides what a `<link>` consumer gets: it hoists the keyframes, strips `@utility`, drops the `@custom-variant` and keeps `--radius-*` / `--text-*` on `:root`, where Tailwind utilities still read them at runtime. The docs state that residue; the script does not check it | `theme-globals.test.ts` compares the hoisted keyframe set and the scrollbar class back to the source; the residue list is prose |
| Any link | — | | **NOTHING**. There is no link checker in any workflow |
| `packages/ui/apps/theme-studio/ThemeStudio.tsx` | `apps/docs/src/components/ThemeStudio.tsx:6`, which re-exports it by a deep relative import (`../../../../packages/ui/apps/theme-studio/ThemeStudio`), and `apps/docs/src/styles/app.css:19`, which `@source`s the same directory so Tailwind's scanner sees classes written there | The docs site's `/theme/editor` page renders the kit's theme-studio app through a shim import; a path or export-shape change on the kit side breaks the shim, and a class added only in `theme-studio` without the `@source` line rescanning it loses its styling on the docs page | `nx build docs`, which compiles the shim import and runs the Tailwind build over the `@source`d directory — wired into `deploy-docs.yml` (push to `main` or manual dispatch), **not** the required `test` job, so a break here is caught on deploy, not on the PR |

---

## 10. Blocks and the facades that still paint their own copies

Phase 1 of the blocks-and-parts plan (2026-08-31) made the widget's chrome
public — `kai-panel`/`kai-panel-header`, `kai-tab-bar`(`-item`),
`kai-view-stack`/`kai-view`, `kai-row`, `createConversationController` — but
`kai-chat`'s widget mode still renders its own private copies of that chrome
(`chat-container.tsx`, `home-panel.tsx`, `widget-tab-bar.tsx`). Until the P-9
refactor rebuilds `kai-chat` ON the parts, every pair below is two copies of one
design, and a fix landing on either side alone is the failure mode.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| `kai-panel` / `kai-panel-header` chrome (`components/panel.tsx`: the frame, the 56px header row) | the widget-box chrome `kai-chat`'s widget mode paints itself (`chat-container.tsx` / `home-panel.tsx`) — `kai-panel-header` is documented as "the exact chrome the `kai-chat` facade paints for its own built-in header" | Restyle one and the public part no longer matches the facade it claims to reproduce; a consumer composing `kai-panel` beside a `kai-chat` sees two different headers | **NOTHING** until P-9 lands (which closes it by construction: one component, two call sites). Stories catch the look only when a human compares |
| `TAB_BAR_CLASS` / `tabBarTabClass` in `components/tab-bar.tsx` (the h-14 bar, the icon-over-label tab) | `WidgetTabBar` (`components/widget-tab-bar.tsx`), which `kai-chat`'s widget mode renders and which restates the same bar markup WITHOUT importing those exports | The two bars drift the day either is edited alone; the public `kai-tab-bar` stops looking like the built-in one | **NOTHING**. P-9 is the fix (retire `WidgetTabBar` onto the tab-bar controller) |
| The drilled rule in `createViewStack` (`components/view-stack.tsx`): a drilled view hides the tab bar and shows a back affordance | `kai-chat`'s own home/messages/chat view switching, which implements that policy privately | The web-component docs say consumers should wire chrome to `kai-view-stack[drilled]` "never to their own copy of that policy" — `kai-chat` currently IS such a copy | **NOTHING**. The stack's own suite pins ITS rule (`view-stack.test.tsx`); agreement between the two copies is checked by nothing |
| `kai-row` anatomy (`components/row.tsx`: leading / title+subtitle / trailing / chevron) | the widget home tab's hand-built rows in `home-panel.tsx` (recent conversation, CTA, help link) — the rows `kai-row` was measured off (P-4) | A padding or type-scale change on either side and the public row no longer matches the facade's | **NOTHING** until P-9 recomposes the home tab from `kai-row` |
| `createConversationController` (`src/stores/conversation-controller.ts`) | the conversation selection/unread state `kai-chat` + `kai-conversations` manage internally — the controller is the headless restatement a compose-your-own consumer drives | The controller's fold and the facades' internal state answer the same questions; a semantic change to one (what counts as unread, what selection means) ships without the other | The controller's own unit tests pin ITS fold (`src/stores/conversation-controller.test.ts`); agreement with the facades: **NOTHING** |
| `NAMED_ICONS` in `src/components/icon.tsx` | the icon roster on every doc/agent surface: `docs/web-components.md`'s `<!-- spec:icon-roster -->` region, llms-full.txt's "Icon roster" section, `src/web-components/icon-names.json` (all derived by `iconNames()` in `gen-web-components-md.mjs`, shared with `gen-llms.mjs` — never a hand list), and the fail-loud fallback in `renderIcon` | An icon-shaped name outside the roster used to paint as literal text with the names enumerated nowhere (spike finding F-7); a stale roster would send agents right back to guessing | `verify:generated` (`verify-generated-sync.mjs`: `icon-names.json`, `llms-full.txt` overwrite probes + the `web-components.md` in-block probe) for freshness · `tests/scripts/llms-index-coverage.test.ts` for the llms.txt pointer · `iconNames()` hard-fails on a zero-match extraction · `src/components/icon.test.tsx` pins the fail-loud render |

### The authored contract (PR B, 2026-09-02)

The rows above are two copies of one design waiting for a refactor. These two
are the opposite shape: ONE owner, several generated readers. They are here
because the generated side is code nobody writes by hand, so a grammar change
is invisible until a form is rendered.

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| The binding grammar in `packages/blocks/src/contract/parse-template.ts` (`.prop`, `:attr`, `@event`, `*for` + `:key`, `seed:`, `#ref`, `data-block-root`) | every renderer under `packages/blocks/src/forms/`, and every authored block page | a renderer that does not know a kind emits a tree missing that wiring, and the `html` form has no typecheck to catch it: the binder is a string until a browser runs it | `verify:blocks [contracts]` -- the parser is the ONE owner of the grammar and its errors are surfaced verbatim, never restated -- plus `[html-binder]`, `[react-tree]`, the block compile cells inside `verify:scaffold`, and `verify:blocks:react`, which runs the react form in a real browser |
| The controller contract in `packages/blocks/src/contract/analyze-controller.ts`: `createController`, and the three interfaces named after the block (`<Component>State`, `<Component>Actions` including its `boot`, `<Component>Refs`) | the generated react adapter, which imports those exact names, and the generated html binder, which calls them | a renamed interface makes the adapter import a type nobody exports, and an `Actions` without `boot` leaves the html form with nowhere to hydrate from storage | `analyzeController` refuses at generation time naming the identifier it wanted (`verify:blocks [contracts]`); the react compile cell in `verify:scaffold` and `verify:blocks:react`'s `tsc --strict` stage are the backstop, and they are also the ONLY typecheck the authored controllers get, because this package's tsconfig covers `src/**` and not `blocks/**` |

### The site section (PR C, 2026-09-03)

`/blocks` on the docs site reads the same generated registry `create-kai add`
writes from, so the page and the CLI are two readers of one source. What follows
is what breaks when a reader stops agreeing with it, plus the two dependency
facts PR C had to change to get a test runner into `apps/docs` at all.

<!-- gate-list: partial -- the coupling rows PR C adds, not an enumeration of the CI gate set; the gate set is what `node packages/ui/scripts/lint-gate-parity.mjs --list` prints -->

| If you change | What else moves | How it fails | Enforced by |
|---|---|---|---|
| `packages/ui/package.json` version | the CDN pin in the site's preview iframe, written by `apps/docs/scripts/copy-blocks.mjs` out of that same file | the preview loads a version the CDN does not serve yet, or an old one | `lint:cdn-pins` (equality, not range membership) for the literals; the generated preview module cannot go stale, because it reads the version out of the package.json the guard compares against |
| A block directory added or removed | the `/blocks` category strip, the cell axes of every gate, the CLI listing | a block ships ungated, or the page shows a block the CLI cannot write | the registry scan is the one derivation; `verify:blocks` hard-fails a zero-block scan |
| `KAI_BLOCKS_KIT` | the footer naming the preview source, and whether the deploy previews a kit path that is not deployed | it works, because the path happens to resolve, and the page stops proving the published artifact runs while the footer still claims it does | `apps/docs/scripts/verify-preview-source.mjs`, in the required graph (`dist-guards`) and again as `--require-published` in `deploy-docs.yml` |
| The forms list (`FRAMEWORK_BLOCK_FORMS` in `packages/blocks/src/forms/index.ts`) | the site's framework dropdown, the compile-cell axis, `FRAMEWORK_SIGNALS` in `packages/create-kai/src/blocks.ts` | the page offers a framework nothing generates | `apps/docs/test/blocks-source.test.ts`; both sides read the forms list; `packages/create-kai/test/add.test.ts` drives every `--form` value the flag accepts through a real `add`, and derives each detection row's expected landing form from the same list, so a renderer added or withheld moves the CLI's menu and its detection together |
| Docs deploy vs npm publish ordering | whether the version the site's preview pins is on the CDN when the page goes live | **today they race:** `deploy-docs.yml` and `release-please.yml` both trigger on `push: main` with no ordering between them (cross-workflow, so `needs:` cannot express it), so merging a release PR builds the docs against the NEW pin while the publish is still running | The **consequence** is caught: `verify:preview -- --require-published` runs in `deploy-docs.yml` before the upload, so a deploy whose pinned entries jsDelivr does not serve goes red instead of shipping empty previews, and Pages keeps the last deploy live. Recovery is a `workflow_dispatch` re-run once the publish lands. The **race itself** is enforced by **NOTHING**: nothing orders the two workflows, so the red is expected rather than prevented. §1 already records the same two workflows racing for the publish gate |
| `apps/docs`'s `vite` devDependency range, or the root `pnpm.overrides.postcss` | which single vite `packages/ui` typechecks against. `node-linker=hoisted` keeps ONE variant per package at the workspace root, so a range declared in `apps/docs` is a workspace-wide fact | Raise `apps/docs` back to `vite ^7` and the root slot flips to the vite-7 flavour: `packages/ui`'s `tsconfig.tests.json` pass then sees two vite type identities and four build configs stop compiling, with an error naming neither `apps/docs/package.json` nor the root override. The postcss override deduplicates the patch-level twin the same flip exposed. The durable fix is aligning `packages/ui` onto vite 7, which is a small-tickets item, not this section's business | **NOTHING**. `nx typecheck ui` does go red, which is the saving grace, but it points at `packages/ui`'s own configs and never at the declaration that moved. `apps/docs/astro.config.mjs` carries two ungated `TS2322` from the same split (root vite vs the vite Astro nests), ungated because no type pass includes that file |

---

## The unenforced list

Everything above whose last column says `NOTHING`, in one place. An unenforced
coupling is a future incident; an enforced one is just a fact.

These numbers are cited from outside this file, so an item that is **retracted
or closed keeps its number** and says so in place rather than being removed, and
an item that narrows is rewritten under its own number.

This list and the `NOTHING` rows above it do **not** count the same, and neither
figure means anything unless you say which you counted:
`grep -c '^[0-9]\+\.' docs/coupling-map.md` for the list,
`grep -c '^|.*NOTHING' docs/coupling-map.md` for the rows. The gap is structural
and permanent, so reconcile against these four before reading a difference as
drift: one coupling can hold **two rows in two sections** and still be one item;
§8 is prose rather than a table, so its item has no row; a row may **name the
gap without the word** (`packages/ui`'s `prepublishOnly` is "unasserted"); and a
retracted item keeps its number with no row. Do not quote either figure without
naming which command produced it.

**Release**

1. The `packages` map vs the publish loop's literal list, and vs the release branch name.
2. The order of that loop (`packages/ui` before `packages/create-kai`).
3. **Narrowed** — that `"plugins": ["node-workspace"]` stays in `release-please-config.json`. Nothing reads that array. It is the only thing making a kit release re-cut create-kai's baked pin; #249 (`b892497b`) added it and closed the original form of this item, a kit-only release stranding the published CLI on a range that could not reach the kit.
4. `bootstrap-sha` being global, and re-arming stale when a package count grows.
5. `include-component-in-tag` and the tag namespace.
6. `packages/ui`'s `prepublishOnly` (the derived rule covers create-kai only).
7. The publish gate's `--require` context list vs the ruleset's required contexts — `GITHUB_TOKEN` cannot read rulesets, so it cannot be derived. Safe direction only: a wrong name refuses every release rather than publishing an unvetted one.
8. Root `.npmrc`: `enable-pre-post-scripts=true` gating every `pre`/`post` hook.
9. Starter dependency ranges under `node-linker=hoisted`.
48. The ordering of `deploy-docs.yml` against `release-please.yml`. Nothing orders the two workflows, so a release merge builds the docs against a pin the registry has not served yet. The CONSEQUENCE is caught (`verify:preview -- --require-published` gates the deploy, and Pages keeps the last deploy live), so the failure is a red workflow and a `workflow_dispatch` re-run, not a broken site. The race itself stands. Next free number per the rule at item 38.
10. **Retracted** — it was never true on `main`. #258 (`b688ae74`) had already made the bump rewrite the pins (`packages["packages/ui"].extra-files`, with `lint:cdn-pins --check-release-wiring` in required CI) twenty-eight minutes before #254 added this entry claiming nothing did, and #258's PR body asked for it to be dropped when #254 landed. The number is held rather than deleted only because items 16 and 17 are under edit on other branches; delete this line and renumber in one pass once those land.

**Packaging**

11. `packages/ui`'s `files` negations vs the four `exports` subpaths resolving outside `dist/`.

**Build-time constants**

12. `define` keys in `create-kai/scripts/build.mjs` vs the `declare const`s in `types/globals.d.ts`.
13. Whether create-kai's pinned range still **resolves** to a published, undeprecated version. `scripts/verify-pin.mjs` is that check and it exists (#249) — but it needs the registry, so it runs by hand and sits in no workflow, named only in a `release-please.yml` comment. `npm run smoke` is in no workflow either. The offline half — the built bundle's pin vs the kit beside it — is covered by `verify:pack`.
14. **Retracted — it had already stopped being true when it was written.** The MCP `serverInfo` version WAS hand-typed and stale (`0.15.0` against a kit ten minors ahead) and `server.test.ts` did not assert it; by the time this item was filed, `packageIdentity()` resolved `@kitn.ai/ui/package.json` through the package's own `exports` map and the test read the manifest off disk and compared. Corrected in the same pass as the §3 row above, which had gone on describing the hand-typed string. The coupling that replaced it — kai's OWN version, a build-time define — is guarded; see the `__KAI_VERSION__` row in §3. The number is held rather than deleted, per the rule at item 38.
15. `PDFJS_VERSION`.

**Derived lists**

16. **Retracted — it was already false when written.** `src/web-components/web-component-manifest.json` has been in `verify-generated-sync.mjs`'s `GENERATED` list since #265 (`8e20acbc`, 2026-08-15), which landed *before* this file's last edit; the item and its two rows went on calling it "the one derived artifact missing from `verify:generated`" through that edit and the next. The number is held rather than deleted, per the rule above. `derived.json` is in the same list, and neither is a `NOTHING`.
17. `matrix.test.ts`'s `SIGNATURE` table vs the integration registry it loops over.
18. `verify-scaffold-compiles.mjs`'s hand-written `FRAMEWORKS`/`EXT`/`PROJECT` vs the `Framework` enum.
19. `CARD_TYPES` in `gen-card-validation-schemas.mjs` vs `cardSchemas` — the only check over it is circular. Same for `BUILTIN_CARD_TAGS` and `BUILTIN_CARD_COMPONENTS`.
20. The `kai` MCP `theme` tool's hardcoded token names vs `theme.css`.
21. A facade in `src/web-components/` that `register-impl.ts` does not import.
38. `GENERATED_SOURCES` in `verify-artifact-fresh.mjs` vs the generators that actually write into the source tree after `dist/` — a hand-maintained copy with each generator named beside its entry, and nothing relating the two. It spans three trees now, not one: `src/`, `mcp/` and `scripts/`, and two of its entries are subtree prefixes (`mcp/construct/fixtures/templates/`, `scripts/block-driver/pages/generated/`) for outputs whose membership is derived from templates.ts and the block registry respectively. **Numbered out of sequence deliberately**, and do not "fix" it by renumbering: these numbers are cited from outside this file, so a new item in an existing group takes the next free number rather than pushing 22–37 along. The saving grace here is the *direction* of the drift, not a check: a postbuild-written source file missing from the set makes a correctly built tree fail `verify:fresh` loudly on the next build. The quiet direction — an entry left behind after its generator is deleted — is the unguarded one.
40. A helper copied into **both** type emitters (`gen-web-component-types.mjs`, `gen-web-component-react.mjs`) instead of imported from `scripts/_ts-helpers.mjs`. `gen-type-render-precedence.test.ts` pins the single owner of `cleanEmittedType` by name and asserts both emitters import it; any *other* helper is related by nothing, which is how two byte-identical `clean` functions sat there until a type-rendering fix landed on one of them alone and shipped a broken `@kitn.ai/ui/react`. Next free number per the rule at item 38.
41. Which Tailwind namespaces `theme.css` re-points in a consumer's build (`dark` variant, `--radius-*` and the kit's own `--radius-pill` + `--code-radius`, `--text-*`, `--spacing`, `--shadow*`, `--font-weight-*`, `@utility`) vs the list of them in `guides/theming.mdx` and the file's own header. The global-name prefix is enforced; the namespace list is prose.
46. The "a directory is a block iff it holds a `registry-item.json`" walk, authored four times (`gen-blocks.mjs`, `scripts/lib/scan-blocks.mjs`, create-kai's `loadBlocks`, `mcp/tests/blocks-artifacts.test.ts`) with nothing tying the copies together. PR B Task 11 gave two gates one copy to share; the rest stand. Next free number per the rule at item 38.
47. The nx `ui -> blocks` project-graph edge, created only by the `workspace:*` devDependency in `packages/ui/package.json`. Nothing automated asserts it stays wired, so dropping the "unused" dependency lets a cache hit serve stale block output over changed block sources. Next free number per the rule at item 38.
50. The `@source` directory list in `packages/ui/src/web-components/styles.css` vs the directories that actually hold Solid components whose class strings must ship. A component moved outside the list keeps its classes out of `compiled.css` silently — green build, valid sheet, unstyled shadow root, and the story looks fine because Storybook's sheet scans `src/` more widely. This is the one derived list whose members are DIRECTORIES rather than names, so adding a `src/` directory is what fires it. Next free number per the rule at item 38.

**Tool versions**

22. Three `npm pack --json` parsers in `packages/ui/scripts/` still on the pre-npm-12 array shape.
23. `node-version` agreement across workflows (a comment, not a check).
24. The `VITE` pin, and its deliberate divergence from the kit's own Vite major.
25. The `typescript@5` pin.
26. Harness tsconfig flags vs the real starter tsconfigs.
49. `apps/docs`'s `vite` range and the root `pnpm.overrides.postcss` vs `packages/ui`'s `tsconfig.tests.json` pass, under `node-linker=hoisted`. Raising the first turns the second red with an error naming neither file. Aligning `packages/ui` onto vite 7 retires the item. Next free number per the rule at item 38.
27. `nx.json`'s `build.outputs` completeness.

**Generated artifacts**

28. `src/web-components/compiled.css` staleness.

**Scaffolder**

29. The preamble's content-part encoding vs `toOpenAIMessages`.
30. Web-component **event** names and `detail` keys vs the emitted front ends — the live test synthesizes the event.
31. Web-component **attribute** names vs emitted markup.

**Module graph**

32. Layering. No lint rule, no dep-cruiser, no test.

**Docs**

33. Version literals and counts in prose.
34. `README.md`, `packages/ui/README.md` and `context7.json`.
35. Integration pages vs the integration catalog.
36. Links.
37. Whether the re-render rule as *worded* still matches the keying it describes. The behaviour is pinned; the prose is not, and a phrasing grep would be theatre.
39. The field-format docs (`guides/field-formats.mdx`, the `form.mdx` bullet, the hand-written `kai-input` line in `docs/web-components.md`) vs `FIELD_SEMANTIC_TYPES` and the mask token vocabulary they restate. The code-side copies are the best-guarded pair in §4; the prose tables are checked by nothing — `verify:docs` resolves symbols and markup, not enum members or caps in a table. Next free number per the rule at item 38.

**Blocks vs the facades' private copies (§10 — every item here is closed by the P-9 refactor when it lands, by construction: one component, two call sites)**

42. `kai-panel`/`kai-panel-header` chrome vs the widget-box chrome `kai-chat`'s widget mode paints itself.
43. `TAB_BAR_CLASS`/`tabBarTabClass` vs `WidgetTabBar`'s restated bar markup.
44. The drilled rule in `createViewStack` vs `kai-chat`'s private view switching.
45. `kai-row` anatomy vs the hand-built rows in `home-panel.tsx`.
46. `createConversationController`'s fold vs the facades' internal conversation state — each side's own suite is green; their agreement is checked by nothing.
