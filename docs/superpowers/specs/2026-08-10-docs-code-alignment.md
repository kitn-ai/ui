# apps/docs vs the shipped API — mechanical alignment report

Generated by `apps/docs/scripts/docs-alignment/` at commit `de79a00e`, against `@kitn.ai/ui` 0.32.0 as built into `packages/ui/dist`.

**This is a snapshot.** Every name checked here is read at run time from the built `dist/*.d.ts` and from `src/web-components/web-component-meta.json` — nothing is baked into the harness — so the numbers move when the API moves. Re-run rather than trusting the counts below once the surface changes.

## How this was found

Doc snippets are fragments: they call the reader's own functions, omit imports, and import packages this repo does not install. Compiling them naively produces a wall of false positives and the check gets switched off. The line this harness holds instead:

> A snippet referencing the READER's code must not fail. A snippet that imports or uses a **kit** symbol that does not exist, or passes a prop a kit component does not accept, must fail.

Four mechanisms do that:

1. **Unknowns are resolved by the compiler, not guessed.** Compile, collect every `Cannot find name 'X'`, declare those as `any`, recompile until it settles. The compiler's own scope analysis decides what is undeclared. Stubs sit on one prepended line inside a module, so they are scoped to their own snippet and every reported line number is off by exactly one, which is subtracted back out. When the unknown name IS a kit export and the snippet already imports from the kit, the real import is injected instead of an `any` stub, so a fragment that omits an import still gets its props checked.
2. **Kit modules are never shimmed.** Third-party specifiers that do not resolve become shorthand ambient modules (`any`). `@kitn.ai/ui` and its subpaths are excluded by a hard guard — an unresolved kit entry point or a missing exported member always fails.
3. **Findings are split by ORIGIN, not by error code.** For each diagnostic the checker is asked where the offending type was *declared*; anything declared under `packages/ui` is kit drift. That is what separates "unknown prop on a kit component" from "unknown prop on the reader's own object", and it lets untyped `js` blocks be checked for kit drift only.
4. **Fragments are interpreted, not rejected.** A block that does not parse as a module is retried as a type shape, a class member, and a bare value; the first interpretation that parses is used. Genuinely malformed code still fails, which the self-test proves.

The `kai-*` contract is checked **structurally** against `web-component-meta.json` rather than by tsc, because the kit's shipped React JSX augmentation types every element as `{ [attr: string]: unknown }` and cannot reject a misspelled attribute — and because 155 of the 406 blocks are plain `html` that tsc never sees.

Two deliberate exemptions, both self-tested so they cannot silently swallow real findings: snippets the docs explicitly mark as counter-examples (`<!-- Fails — an array can't be an HTML attribute -->`), and JSX placeholder children (`{/* your components */}`).

## Headline

| | |
|---|---|
| Pages scanned | 126 |
| Fenced blocks | 406 |
| Snippets compiled | 280 |
| Entry points read | 14 |
| Exported names read | 1996 |
| `kai-*` elements read | 97 |
| **High-confidence findings** | **0** |
| Medium findings | 0 |
| Advisory findings | 80 |
| Flagged for human review | 50 |

### By finding kind

| count | kind | what it means |
|---:|---|---|
| 43 | `snippet-type-error` | a type error in a snippet not traceable to a kit type (advisory) |
| 18 | `undeclared-in-web-component-meta` | the kit HAS this (a declarative light-DOM child, or an event it dispatches) but `web-component-meta.json` does not declare it, so the generated types, docs tables and MCP catalog all omit it — a metadata gap, not a docs error |
| 12 | `unresolved-prose-name` | prose backticks a PascalCase name no entry point exports and the page never defines (advisory) |
| 7 | `literal-widening` | a plain object literal widens (`role: 'user'` → `string`) and no longer fits the kit's prop type — a TS reader who copies this does not compile; the fix is the kit's exported `ChatMessage` |

### By docs section, ranked by consumer impact

| section | pages | high | medium | advisory |
|---|---:|---:|---:|---:|
| `(root)` | 2 | 0 | 0 | 0 |
| `components` | 60 | 0 | 0 | 24 |
| `examples` | 14 | 0 | 0 | 2 |
| `guides` | 19 | 0 | 0 | 14 |
| `guides/frameworks` | 8 | 0 | 0 | 18 |
| `guides/recipes` | 4 | 0 | 0 | 2 |
| `integrations` | 9 | 0 | 0 | 17 |
| `patterns` | 9 | 0 | 0 | 3 |
| `theme` | 1 | 0 | 0 | 0 |

## Advisory findings (80)

Not gating. Type errors that do not trace back to a kit type, prose names the harness could not resolve, and snippets that re-declare a kit type without actually breaking against it.

### `guides/frameworks/react.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 155 | `snippet-type-error` TS2657 | JSX expressions must have one parent element. |

### `guides/compose-message-thread.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 46 | `literal-widening` TS2322 | Type '{ id: string; role: string; parts: { type: string; text: string; }[]; actions?: undefined; } \| { id: string; role: string; parts: { type: string; text: string; }[]; actions: string[]; }' is not assignable to type '{ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string;… |

### `guides/for-ai-agents.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 326 | `literal-widening` TS2322 | Type '({ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { ...; }; } \| { ...; } \| { ...; } \| { ...; } \| { ...… |
| 331 | `literal-widening` TS2322 | Type 'string' is not assignable to type '"user" \| "assistant"'. |
| 331 | `literal-widening` TS2322 | Type 'undefined[] \| ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { source: string; payload: unknown; }; } \| { ...; } \| { ...; } \| { ...; } \| { ...; })[]… |

### `guides/generative-ui.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 88 | `snippet-type-error` TS7006 | Parameter 'call' implicitly has an 'any' type. |
| 152 | `snippet-type-error` TS7006 | Parameter 'e' implicitly has an 'any' type. |

### `guides/state-and-hooks.mdx` — 6 finding(s)

| line | kind | detail |
|---:|---|---|
| 40 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 43 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 46 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 49 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |
| 149 | `snippet-type-error` TS2339 | Property 'kai-chat' does not exist on type 'JSX.IntrinsicElements'. |
| 164 | `snippet-type-error` TS2657 | JSX expressions must have one parent element. |

### `guides/theming.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 244 | `snippet-type-error` TS2322 | Type '{ proseSize: "base"; codeTheme: string; }' is not assignable to type 'IntrinsicAttributes & ChatConfigProps'. Property 'children' is missing in type '{ proseSize: "base"; codeTheme: string; }' but required in type 'ChatConfigProps'. |

### `guides/use-a-workspace.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 62 | `undeclared-in-web-component-meta` | `kai-sidebar-toggle` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

### `guides/frameworks/angular.mdx` — 8 finding(s)

| line | kind | detail |
|---:|---|---|
| 24 | `snippet-type-error` TS2307 | Cannot find module './app/app.component' or its corresponding type declarations. |
| 25 | `snippet-type-error` TS2307 | Cannot find module './app/app.config' or its corresponding type declarations. |
| 279 | `snippet-type-error` TS7006 | Parameter 'req' implicitly has an 'any' type. |
| 279 | `snippet-type-error` TS7006 | Parameter 'res' implicitly has an 'any' type. |
| 286 | `snippet-type-error` TS7006 | Parameter 'req' implicitly has an 'any' type. |
| 286 | `snippet-type-error` TS7006 | Parameter 'res' implicitly has an 'any' type. |
| 286 | `snippet-type-error` TS7006 | Parameter 'next' implicitly has an 'any' type. |
| 289 | `snippet-type-error` TS7006 | Parameter 'response' implicitly has an 'any' type. |

### `guides/frameworks/meta-frameworks.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 38 | `snippet-type-error` TS2307 | Cannot find module './chat-island' or its corresponding type declarations. |
| 67 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 114 | `snippet-type-error` TS7006 | Parameter 'm' implicitly has an 'any' type. |

### `guides/frameworks/solid.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 179 | `snippet-type-error` TS7006 | Parameter 'id' implicitly has an 'any' type. |
| 228 | `snippet-type-error` TS2322 | Type '{ class: string; }' is not assignable to type 'IntrinsicAttributes & ChatContainerContentProps'. Property 'children' is missing in type '{ class: string; }' but required in type 'ChatContainerContentProps'. |
| 259 | `snippet-type-error` TS2657 | JSX expressions must have one parent element. |
| 306 | `snippet-type-error` TS2339 | Property 'kai-chat' does not exist on type 'JSX.IntrinsicElements'. |

### `guides/frameworks/vue.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 24 | `snippet-type-error` TS2307 | Cannot find module './App.vue' or its corresponding type declarations. |
| 44 | `snippet-type-error` TS7006 | Parameter 'tag' implicitly has an 'any' type. |

### `components/audio-visualizer.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 125 | `unresolved-prose-name` | `SpeechRecognition` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 191 | `unresolved-prose-name` | `N` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/badge.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 27 | `unresolved-prose-name` | `Beta` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/chain-of-thought.mdx` — 5 finding(s)

| line | kind | detail |
|---:|---|---|
| 48 | `undeclared-in-web-component-meta` | `kai-step` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |
| 48 | `undeclared-in-web-component-meta` | `kai-step` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |
| 52 | `undeclared-in-web-component-meta` | <kai-step> is used by the kit (a declarative light-DOM child) but is not a registered element, so web-component-meta.json, the generated types and the MCP catalog all omit it |
| 53 | `undeclared-in-web-component-meta` | <kai-step> is used by the kit (a declarative light-DOM child) but is not a registered element, so web-component-meta.json, the generated types and the MCP catalog all omit it |
| 54 | `undeclared-in-web-component-meta` | <kai-step> is used by the kit (a declarative light-DOM child) but is not a registered element, so web-component-meta.json, the generated types and the MCP catalog all omit it |

### `components/command.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 53 | `unresolved-prose-name` | `Chats` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 53 | `unresolved-prose-name` | `Files` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 59 | `unresolved-prose-name` | `Actions` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 59 | `unresolved-prose-name` | `Settings` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/conversations.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 67 | `undeclared-in-web-component-meta` | `kai-conversation` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |
| 99 | `undeclared-in-web-component-meta` | `kai-conversation` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |
| 109 | `snippet-type-error` TS7006 | Parameter 'e' implicitly has an 'any' type. |
| 110 | `snippet-type-error` TS7006 | Parameter 't' implicitly has an 'any' type. |

### `components/message.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 119 | `unresolved-prose-name` | `Share` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `components/model-switcher.mdx` — 4 finding(s)

| line | kind | detail |
|---:|---|---|
| 61 | `undeclared-in-web-component-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |
| 65 | `undeclared-in-web-component-meta` | <kai-model> is used by the kit (a declarative light-DOM child) but is not a registered element, so web-component-meta.json, the generated types and the MCP catalog all omit it |
| 66 | `undeclared-in-web-component-meta` | <kai-model> is used by the kit (a declarative light-DOM child) but is not a registered element, so web-component-meta.json, the generated types and the MCP catalog all omit it |
| 70 | `undeclared-in-web-component-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

### `components/skills.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 17 | `undeclared-in-web-component-meta` | `kai-skill` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |
| 26 | `undeclared-in-web-component-meta` | `kai-skill` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

### `components/suggestions.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 47 | `undeclared-in-web-component-meta` | `kai-suggestion` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

### `integrations/cloudflare-ai.mdx` — 5 finding(s)

| line | kind | detail |
|---:|---|---|
| 20 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 24 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 54 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 54 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |
| 58 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |

### `integrations/connect-any-model.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 38 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |

### `integrations/harnesses.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 65 | `snippet-type-error` TS7031 | Binding element 'update' implicitly has an 'any' type. |
| 83 | `snippet-type-error` TS2591 | Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig. |

### `integrations/langgraph.mdx` — 3 finding(s)

| line | kind | detail |
|---:|---|---|
| 22 | `snippet-type-error` TS7031 | Binding element 'city' implicitly has an 'any' type. |
| 62 | `unresolved-prose-name` | `StateGraph` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 75 | `unresolved-prose-name` | `ToolMessage` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `integrations/vercel-ai-sdk.mdx` — 6 finding(s)

| line | kind | detail |
|---:|---|---|
| 51 | `snippet-type-error` TS2709 | Cannot use namespace 'ModelMessage' as a type. |
| 56 | `snippet-type-error` TS2709 | Cannot use namespace 'SystemModelMessage' as a type. |
| 221 | `unresolved-prose-name` | `FilePart` is not exported by any @kitn.ai/ui entry point and is not defined on this page |
| 233 | `snippet-type-error` TS2709 | Cannot use namespace 'JSONSchema7' as a type. |
| 236 | `snippet-type-error` TS2709 | Cannot use namespace 'ToolSet' as a type. |
| 237 | `snippet-type-error` TS2709 | Cannot use namespace 'ToolSet' as a type. |

### `guides/recipes/streaming.mdx` — 2 finding(s)

| line | kind | detail |
|---:|---|---|
| 31 | `literal-widening` TS2322 | Type '({ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { ...; }; } \| { ...; } \| { ...; } \| { ...; } \| { ...… |
| 47 | `literal-widening` TS2345 | Argument of type '({ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string; raw?: { source: string; payload: unknown; }; } \| { type: "reasoning"; text: string; label?: string; index?: number; streamId?: string; signature?: string; raw?: { ...; }; } \| { ...; } \| { ...; } \| { ..… |

### `patterns/compose-your-own.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 59 | `literal-widening` TS2322 | Type '{ id: string; role: string; parts: { type: string; text: string; }[]; actions?: undefined; } \| { id: string; role: string; parts: { type: string; text: string; }[]; actions: string[]; }' is not assignable to type '{ id: string; role: "user" \| "assistant"; parts: ({ type: "text"; text: string;… |

### `patterns/custom-chat-header.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 55 | `undeclared-in-web-component-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

### `patterns/popover-menu.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 51 | `unresolved-prose-name` | `Placement` is not exported by any @kitn.ai/ui entry point and is not defined on this page |

### `examples/model-context.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 108 | `undeclared-in-web-component-meta` | `kai-model` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

### `examples/skills-assistant.mdx` — 1 finding(s)

| line | kind | detail |
|---:|---|---|
| 91 | `undeclared-in-web-component-meta` | `kai-skill` exists in the kit (a declarative child element or a dispatched event) but is not a registered element or a declared event, so web-component-meta.json omits it |

## Reverse coverage

### Shipped but undocumented

**21 `kai-*` element(s) are never mentioned anywhere in `apps/docs`:**

`kai-agent-card`, `kai-checkbox-group`, `kai-coachmark`, `kai-dropdown`, `kai-editable-label`, `kai-kbd`, `kai-pane-grid`, `kai-panel`, `kai-panel-header`, `kai-progress-bar`, `kai-radio-group`, `kai-row`, `kai-row-group`, `kai-segmented`, `kai-slider`, `kai-status`, `kai-tab-bar`, `kai-tab-bar-item`, `kai-tabs`, `kai-view`, `kai-view-stack`

**16 element(s) are mentioned but have no `<Playground>`/`<Example>`/`<PropTable>` of their own** (named in passing only):

| element | mentioned in |
|---|---|
| `kai-checkbox` | `guides/frameworks/solid.mdx` |
| `kai-conversation-item` | `components/conversations.mdx` |
| `kai-dialog` | `guides/app-shell.mdx` |
| `kai-dock` | `guides/app-shell.mdx` |
| `kai-input` | `guides/field-formats.mdx` |
| `kai-nav` | `guides/app-shell.mdx` |
| `kai-pane` | `guides/app-shell.mdx` |
| `kai-pane-group` | `guides/app-shell.mdx` |
| `kai-prompt-dock` | `guides/app-shell.mdx` |
| `kai-screen` | `guides/app-shell.mdx` |
| `kai-search` | `components/conversations.mdx`, `components/prompt-input.mdx`, `guides/use-a-workspace.mdx`, `guides/use-the-chat-app.mdx` |
| `kai-select` | `components/checkpoint.mdx`, `components/command.mdx`, `components/conversations.mdx`, `components/file-tree.mdx` +7 |
| `kai-setting-item` | `guides/app-shell.mdx` |
| `kai-settings-group` | `guides/accessibility.mdx`, `guides/app-shell.mdx` |
| `kai-thread` | `guides/app-shell.mdx`, `guides/frameworks/solid.mdx` |
| `kai-voice-output` | `components/audio-visualizer.mdx` |

**67 SolidJS component export(s) are never named in the docs** (of 132 detected components on the root entry):

`AppHeader`, `ArtifactCard`, `AttachmentEmpty`, `AttachmentHoverCard`, `AttachmentHoverCardContent`, `AttachmentHoverCardTrigger`, `AttachmentInfo`, `AttachmentPreview`, `AttachmentRemove`, `BarsLoader`, `ChainOfThoughtItem`, `CheckpointIcon`, `CheckpointTrigger`, `ChoiceCard`, `CircularLoader`, `ClassicLoader`, `CodeBlockGroup`, `CollapsibleContent`, `CollapsibleTrigger`, `ConfirmCard`, `ContextCacheUsage`, `ContextContent`, `ContextContentBody`, `ContextContentFooter`, `ContextContentHeader`, `ContextInputUsage`, `ContextOutputUsage`, `ContextReasoningUsage`, `ContextTrigger`, `ConversationItem`, `DismissedStub`, `DotsLoader`, `Dropdown`, `DropdownContent`, `DropdownItem`, `DropdownTrigger`, `EmptyContent`, `EmptyDescription`, `EmptyHeader`, `EmptyMedia`, `EmptyTitle`, `FileUploadContent`, `HoverCard`, `MessageAction`, `MessageAvatar`, `MessageCopyButton`, `PromptInputAction`, `PromptSuggestion`, `PulseDotLoader`, `PulseLoader`, `ResponseCompare`, `RowGroup`, `ScrollArea`, `SlottedConversationItem`, `SourceContent`, `SourceList`, `SourceTrigger`, `TasksCard`, `TerminalLoader`, `TextBlinkLoader`, `TextDotsLoader`, `TextShimmerLoader`, `Textarea`, `ToastRegion`, `TypingLoader`, `WaveLoader`, `WorkSurface`

### Documented but gone

**0 `kai-*` token(s) appear in the docs but are neither a registered element nor a declared event:**

_none_

**0 `@kitn.ai/ui` subpath(s) referenced in the docs are not in the package's exports map:**

_none_

## Flagged for human review

Executable factual claims the harness extracted but cannot adjudicate. It does **not** guess whether these are true — someone has to check each against the build output, the exports map, or the running app.

### `default` (17) — claims about default values and automatic behaviour

| page | line | claim |
|---|---:|---|
| `guides/installation.mdx` | 79 | Inherited custom properties cross the Shadow DOM boundary, so these reach every `kai-*` web component automatically. The `--kai-` form is the one that gets there: each element's own shadow CSS declares the unprefixed `--color-*` names on `:host`, which beats anything inherited from your `:root`. Imp |
| `guides/frameworks/react.mdx` | 194 | This is the same immutable update React already wants for its own state, so idiomatic React code satisfies it by default. The trap is reaching for a mutation to avoid a re-render, or handing the web components state from a store that mutates in place. |
| `guides/theming.mdx` | 69 | \| `--radius-pill` \| A new rung, and a new `rounded-pill` utility with it (`--radius-pill: var(--kai-radius-pill, 4rem)`). It exists because Tailwind compiles `rounded-full` to a literal no custom property can reach, so badges, chips, tags, switch tracks and count bubbles could not follow a consumer' |
| `guides/use-the-chat-app.mdx` | 133 | Clicking a chip submits it immediately by default, firing `kai-submit` like a typed message — so the streaming loop above just works. To prefill the input instead and let the user edit first, set `suggestionMode = 'fill'` and read the chosen text from `kai-suggestion-click`. For a custom greeting wi |
| `guides/frameworks/html.mdx` | 170 | Wrap panels in `<kai-resizable>` with one `<kai-resizable-item>` each — drag handles are inserted automatically. Each item accepts a `size` (px or `%`) and optional `min`/`max`. Listen for `kai-change` to persist the layout. |
| `guides/frameworks/solid.mdx` | 48 | Tailwind v4 only scans your own `src/` by default. Without the `@source` line it strips every kit utility class as unused and the components render unstyled. Point it at wherever `@kitn.ai/ui` resolves from your CSS file. |
| `components/card.mdx` | 65 | `footer-actions` is responsive on its own: a right-aligned row when there is room, and a full-width vertical stack when the card is too narrow (a container query on the card's width). Author the **primary action last** so it sits at the row's right and rises to the top of the stack. `kai-button` fil |
| `components/file-tree.mdx` | 17 | <p class="kai-lede">Pass a flat array of files, each with a `path`, and get a collapsible, keyboard-navigable file explorer — folders are derived from the slashes automatically, no tree to build by hand.</p> |
| `components/prompt-input.mdx` | 29 | Reach for `<kai-prompt-input>` whenever you need a chat composer outside of SolidJS — React, Vue, Svelte, Angular, or plain HTML. It manages its own input state uncontrolled by default; listen for `kai-submit` to receive the typed text and any staged attachments. Pin it to the bottom of a chat surfa |
| `components/reasoning.mdx` | 27 | Place `<kai-reasoning>` between the assistant message content and any tool calls whenever the model produces a chain-of-thought trace. Set `streaming` while the thought arrives and clear it when the stream ends — the element handles expand/collapse automatically. Use `open` for programmatic control, |
| `components/resizable.mdx` | 53 | **Expand-to-fill** — when a `<kai-artifact expandable>` fires `kai-maximize-intent` (bubbling, composed), the nearest enclosing `<kai-resizable>` catches it automatically and hides siblings. No wiring needed. **Escape** or **Collapse** restores the layout. |
| `components/resizable.mdx` | 71 | Wire `<kai-artifact expandable>` inside an item — pressing **Expand** maximizes that panel automatically: |
| `components/workspace.mdx` | 65 | - **Collapse** is uncontrolled by default: the element manages it and fires `kai-aside-toggle` with `{ side, collapsed }`. Start collapsed with the `default-start-collapsed` or `default-end-collapsed` attribute, or control an aside by setting the `startCollapsed` / `endCollapsed` property and updati |
| `integrations/pydantic-ai.mdx` | 62 | The server binds to `http://localhost:8000` by default. Point `<kai-chat>` at `/api/chat` (or the full URL if it's on a different origin — CORS middleware is already in the route above). |
| `patterns/popover-menu.mdx` | 39 | By default the popover manages its own open state. To drive it from your app, set the `open` property and update it from `kai-open-change`: |
| `examples/agentic-assistant.mdx` | 52 | Each assistant message's `parts` array can include `reasoning` and `tool` parts alongside its `text` part. `<kai-chat>` renders every part in order automatically — no extra web components needed. |
| `examples/reasoning-assistant.mdx` | 19 | Reasoning lives in the message's `parts` array, not in a separate element. Add a `{ type: 'reasoning', text, label? }` part ahead of the `{ type: 'text', … }` part; `<kai-chat>` renders it as a collapsible block above the answer. The `label` is the trigger text (defaults to `Reasoning`) and `text` i |

### `packaging` (13) — claims about what the package ships, how it resolves, and what the bundler does with it

| page | line | claim |
|---|---:|---|
| `guides/installation.mdx` | 32 | Import the bundle once for its side effect. This registers every `kai-*` web component globally — put it near your app's entry point: |
| `guides/installation.mdx` | 109 | Writing Solid? You can skip the web components and import the native components for full compositional control. Add the peer dependency: |
| `guides/installation.mdx` | 115 | Then import from the Solid entry (`@kitn.ai/ui/solid`), which ships compiled and tree-shakes to what you use: |
| `guides/installation.mdx` | 137 | \| `@kitn.ai/ui/web-components/<module>` \| A single web-component module, tree-shaken. The path is the module's basename, which is usually but not always the tag minus `kai-`. See [Loading](/guides/loading/). \| |
| `guides/installation.mdx` | 140 | \| `@kitn.ai/ui/solid` \| The complete SolidJS catalog — compiled, tree-shakeable; Solid projects only \| |
| `guides/frameworks/overview.mdx` | 21 | Import `@kitn.ai/ui/web-components` once for its side effect. It registers every `kai-*` web component; after that they work like built-in HTML. |
| `guides/frameworks/overview.mdx` | 157 | Writing Solid? Skip the element wrapper and compose the kit's native components from `@kitn.ai/ui/solid`. That entry is the complete Solid catalog, compiled as its own bundle so the other frameworks never pay for it. It tree-shakes to exactly what you import and mixes freely with your own primitives |
| `guides/loading.mdx` | 15 | \| **Per-web-component** \| `@kitn.ai/ui/web-components/chat` \| You use only a couple of web components and your build tree-shakes. \| |
| `guides/loading.mdx` | 19 | `import '@kitn.ai/ui/web-components'` registers every web component in one import and works everywhere, including SSR. Reach for the other two only when you have a specific reason — a build that tree-shakes, or a no-build CDN page. |
| `guides/loading.mdx` | 28 | One import, every `kai-*` web component registered. The simplest setup, and the SSR-safe entry point — the import is side-effect-only and doesn't touch `document` until the browser runs it, so it won't throw in a server render. |
| `guides/frameworks/solid.mdx` | 11 | - **Native SolidJS components** (`@kitn.ai/ui/solid`) — import `ChatThread`, `Message`, `PromptInput`, and the rest directly into your JSX. Full compositional control, tree-shaken alongside your own code. Reach for this in a Solid app. |
| `guides/frameworks/solid.mdx` | 41 | Install the two plugins next to Tailwind. They are optional peer dependencies of the kit: |
| `guides/frameworks/solid.mdx` | 56 | The package ships **compiled**. `@kitn.ai/ui/solid` resolves to `dist/solid.js` in the browser and `dist/solid.server.js` under the `node` / `deno` / `worker` conditions; `@kitn.ai/ui` resolves to `dist/index.js` / `dist/index.server.js` the same way. All four are ESM, so your bundler still tree-sha |

### `runtime` (12) — claims about SSR, server rendering, hydration and where code runs

| page | line | claim |
|---|---:|---|
| `blocks.mdx` | 15 | {/* SERVER-rendered marker, the same as `.landing-page` and `.kai-fullscreen`. |
| `guides/loading.mdx` | 14 | \| **Register all** \| `@kitn.ai/ui/web-components` \| The default. You use several web components, or you just want it to work. Works in SSR. \| |
| `guides/loading.mdx` | 109 | The autoloader resolves web-component modules relative to its own URL and uses `document` + `MutationObserver`. Load it from a `<script type="module">` on a CDN or static host. Do **not** `import '@kitn.ai/ui/autoloader'` in a bundled (Vite/webpack/Next) app or a server render — the bundler relocate |
| `guides/loading.mdx` | 129 | \| Per-web-component (`@kitn.ai/ui/web-components/chat`) \| No — throws `window is not defined`; use inside a client-only boundary \| |
| `guides/frameworks/meta-frameworks.mdx` | 8 | <p class="kai-lede">AI/UI web components are custom elements with Shadow DOM, so they hydrate and run in the browser. In Next.js and TanStack Start that makes them client islands: the page renders on the server, and `kai-chat` comes alive where it mounts.</p> |
| `guides/frameworks/meta-frameworks.mdx` | 82 | Same idea, different gates. Wrap the chat in `ClientOnly` from `@tanstack/react-router` so it mounts only after hydration, and register the web components in an effect: |
| `components/conversations.mdx` | 67 | - **Declarative children** — nest `<kai-conversation>` tags directly; each carries its `id` as an attribute and title as text content. The element reads them on mount and watches via MutationObserver — works in plain HTML, SSR, or any framework that sets innerHTML. |
| `components/skills.mdx` | 26 | Place `<kai-skills>` beneath an assistant message row to annotate which tools were active. Use the `skills` property when skill data comes from app state; use declarative `<kai-skill>` children for server-rendered HTML with no JS wiring. |
| `components/skills.mdx` | 44 | - **Declarative path** — nest `<kai-skill id="…">Label</kai-skill>` children directly; useful for server-rendered HTML with no JS wiring. |
| `components/sources.mdx` | 26 | Place `<kai-sources>` directly below an assistant message when the reply cites external references. Use `numbered` when the answer body uses `[1]`, `[2]`, … citation markers. Use the `sources` property for dynamic or streamed lists; use declarative `<kai-source>` children for static or server-render |
| `integrations/connect-any-backend.mdx` | 74 | `<kai-chat>` is a web component — it hydrates and runs in the browser. That holds in a plain HTML page, a React app, or a Next.js route (where it's a client island). The component is the front end; your `/api/chat` route is the back end. Server rendering emits the tag; the chat comes alive on hydrat |
| `theme/editor.mdx` | 9 | {/* Server-rendered marker: drives the full-screen CSS in app.css (body:has(.kai-fullscreen)) |

### `size-perf` (4) — claims about bundle size, code splitting and lazy loading

| page | line | claim |
|---|---:|---|
| `guides/installation.mdx` | 138 | \| `@kitn.ai/ui/autoloader` \| CDN / static auto-loader — loads each `kai-*` on demand, no build. See [Loading](/guides/loading/). \| |
| `guides/frameworks/html.mdx` | 33 | `kai.es.js` registers every web component in one fetch — the right shape for a `<kai-chat>` page, which pulls most of the kit anyway. A page placing only a web component or two does better with the [autoloader](/guides/loading/#autoloader), which fetches each web component on demand. The streaming h |
| `components/resizable-item.mdx` | 26 | Use `<kai-resizable-item>` as a direct child of `<kai-resizable>` to define each panel. Give panels with a natural default width an explicit `size` (e.g. a sidebar at `"260px"`); leave `size` off the main content area so it fills remaining space. Use `locked` for chrome panels that must never resize |
| `patterns/artifact-from-message.mdx` | 8 | <p class="kai-lede">When the assistant generates a runnable result — a web page, an app, a component — your backend serves it at a URL and `<kai-artifact>` frames it live. You don't have to show it the moment it's ready: keep the conversation full-width, reference the generated file in the message, |

### `version` (4) — claims tied to a version number

| page | line | claim |
|---|---:|---|
| `guides/getting-started.mdx` | 36 | Pin an exact version in production (e.g. `@kitn.ai/ui@0.32.0/dist/...`). |
| `guides/getting-started.mdx` | 39 | The package is pre-1.0, so a minor release can break things. Pin `0.25.0` or newer: `0.14.1` through `0.24.0` are covered by a [critical security advisory](https://github.com/kitn-ai/ui/security/advisories), and a CDN fetch prints no deprecation warning to tell you. |
| `guides/installation.mdx` | 101 | An unpinned CDN URL tracks the latest release — fine for prototyping, risky for production. The package is pre-1.0, so a minor release can break things. Pin an exact version: `https://cdn.jsdelivr.net/npm/@kitn.ai/ui@0.32.0/dist/kai.es.js`. |
| `guides/loading.mdx` | 114 | An unversioned CDN URL tracks the latest release, which is risky on a pre-1.0 package. Pin an exact version: `https://cdn.jsdelivr.net/npm/@kitn.ai/ui@0.32.0/dist/web-components/autoloader.js`. |

## Honest assessment: how much of the drift is mechanically detectable

Roughly **a third**, and it is the cheap third.

What a machine settles conclusively: does this name exist, does this prop exist, does this element exist, does this event exist, does this snippet compile against the shipped types, is this local type compatible with the kit's. Every finding above is one of those, and each is either true or false with no judgement involved. That is the whole first two sections of this report.

What it cannot settle, and where the rest of the drift lives:

- **Whether a snippet that compiles actually works.** Nothing here is executed. The parts-flattening bug in `guides/frameworks/solid.mdx` compiled cleanly for as long as it existed, and was only caught because the page happened to re-declare a type the kit exports. A page that flattens `parts` without re-declaring anything would still be invisible.
- **Whether prose is true.** Names are checked; claims are not. The 47 items flagged for human review are the ones worth checking, and the harness deliberately refuses to guess at any of them.
- **Whether the recommended approach is still the recommended approach.** A page can be entirely correct and entirely outdated.
- **Whether an example is good.** Ordering, completeness, error handling, whether the snippet teaches the pattern the kit wants taught.

The honest framing: this harness makes the mechanical third *free and repeatable*, and it makes the other two thirds **visible and bounded** — 47 extracted claims and two reverse-coverage lists are a finite human task, where "read 120 pages and check everything" is not. It is a filter, not a verdict.

One more caveat worth stating plainly: this harness has already been wrong. Its first full run reported 73 high-confidence findings; 64 of those were its own bugs (Vue's `.prop` modifier, Svelte's `{messages}` shorthand, a CSS custom property parsed as an element, prose inside comments, the docs' own counter-examples). Each was found by checking a sample of findings against the source by hand. The number below is what survived that; it is not proof that nothing else is wrong with it.

## What this harness does NOT cover

Stated plainly, because a coverage claim nobody bounded is how a check ends up proving nothing.

- **Prose meaning.** Only names are checked, not sentences. "`ChatContainer` owns the scroll" is unverifiable here; the harness only knows `ChatContainer` exists.
- **Runtime behaviour.** No snippet is executed and no page is rendered. A snippet that compiles and renders nothing looks identical to a correct one.
- **Visual/UX claims.** Screenshots, layout, theming results.
- **Untyped JS depth.** `js` and inline `<script>` blocks are checked for kit-API drift only. A logic bug in an untyped snippet is invisible.
- **Non-code fences.** 42 block(s) in languages with no checker here (bash, json, jsonc, python, sh, toml).
- **Uninstalled third-party packages.** 15 specifier(s) resolve to `any` by design, so misuse of them is not caught: `@ai-sdk/openai`, `@angular/core`, `@angular/platform-browser`, `@langchain/core/tools`, `@langchain/langgraph/prebuilt`, `@langchain/openai`, `@mastra/client-js`, `@tanstack/react-router`, `@vitejs/plugin-vue`, `ai`, `https://cdn.jsdelivr.net/npm/@kitn.ai/ui/dist/kai.es.js`, `https://cdn.jsdelivr.net/npm/@kitn.ai/ui/dist/web-components/autoloader.js`, `openai`, `vite`, `zod`.
- **Angular / Vue / Svelte templates.** Only the `<script>` half is compiled; the template half gets the same structural `kai-*` checks as HTML.

## Reproducing

```bash
pnpm exec nx build ui                       # the harness reads dist/*.d.ts
pnpm --filter @kitn.ai/docs run verify:docs # gate: exits 1 on any high finding
pnpm --filter @kitn.ai/docs run verify:docs:report  # regenerate this file
```

### CI status: NOT wired in yet

Deliberately. Cost is not the reason — the whole run is about 10s and needs no network, which is well inside what the required job already spends on `verify:scaffold`. Two things have to be true first:

1. **The tree has to be green.** A gate added while it reports findings is a gate that is red on arrival, and a red-on-arrival gate gets bypassed rather than fixed. High and medium are currently at 0 and 0, so this precondition is met as of this run — re-check it rather than trusting this sentence.
2. **It needs one more adversarial pass.** The first full run reported 73 high-confidence findings and 64 were the harness's own bugs. That ratio came down by hand-checking findings against the source. Every finding since has been verified by reading the page, and the severity rules are now probed directly — but the harness has not yet survived anyone deliberately trying to make it lie.

Suggested sequencing: land it as a non-blocking job that posts the report, watch it for a few PRs, then promote it to required once the high count has stayed at zero on its own.

The self-test runs first, every time. It compiles six snippets that MUST produce a kit finding (unknown entry point, unknown export, bogus prop on a kit component, wrong argument type, wrong arity, removed wire API), seven structural probes that MUST report (unknown element, unknown prop, non-scalar-as-attribute, unknown event, unknown part, stale `<Example tag>`, stale `<Example config>` key), six severity probes that pin what GATES (widening gates in a `ts` block and is advisory in an untyped one, while shape drift — a missing or misspelled property against a kit type — gates in an untyped block too), a shadow probe that must go from clean to broken when the real type is substituted, and five probes that MUST stay silent (reader-owned undeclared names, omitted imports, uninstalled third-party imports, valid markup, non-scalar props bound as JSX expressions). If any probe misbehaves the run aborts before touching the docs, because a green result would be meaningless.
