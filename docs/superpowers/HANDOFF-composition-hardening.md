# HANDOFF — Composition initiative (composer showcase + new composition primitives)

**Date:** 2026-06-29 (SESSIONS 4–5) · **Repo root:** `/Users/home/Projects/kitn-ai/kitn-chat` · **Branch:** `main` — sessions 4–5 ✅ **MERGED via PR #114** (merge commit `4dbacf1`, 37 commits). `feat/new-components-epic` is now merged/stale → **branch fresh off `main` for new work.** · **Status:** green at merge (typecheck 4/4 · unit **1225** · build clean · **74 elements**). release-please **#105** open (Rob's call to publish).

> ★ Read **§0.0 (SESSIONS 4–5)** below FIRST + memory **`labs-apps-dogfood`** (SESSION-5) + **`footprint-reduction-research`**. §0.01 onward is now-MERGED session-1–3 history — do not trust its "UNCOMMITTED / current state" lines.

---

## 0.00 ★★CURRENT — SESSION 6 (2026-06-29): input/search field family + consumer packaging hardening. ✅ SHIPPED to npm 0.18.0.

**★Read these memories FIRST (they're authoritative; this doc is a pointer):** `consumer-packaging-hardening`, `input-search-field-family`, `monorepo-migration-plan`, `primitive-vs-element-taxonomy`, `defer-ivp-to-end`.

**SHIPPED this session:**
1. **Input/search field family — PR #115 (merge `132f072`).** +4 elements: `kai-input` · `kai-search` · `kai-kbd` · `kai-editable-label`; `kai-nav` per-item action/close; `kai-command`+`kai-menu` shortcut (kai-kbd caps); `kai-form` on a shared `Input` primitive. Dogfooded into perplexity-pro/proof-data-table/proof-auth/AMUX. 8 stories reclassified Components/Primitives→Elements (see `primitive-vs-element-taxonomy`).
2. **Consumer packaging hardening — PRs #116 + #117 → npm 0.18.0.** Two critical bugs (`import '@kitn.ai/ui/elements'` registered NOTHING — void-import tree-shaken; the `@kitn.ai/ui/react` wrappers didn't resolve in any bundler — wrong per-element specifiers) → fixed + TWO build guards (`scripts/verify-elements-bundle.mjs`, `verify-react-wrappers.mjs`, wired into `npm run build`) + Next.js & TanStack Start examples + a verified compatibility FAQ (`docs/framework-compatibility-faq.md`). Verified across React/Next App-Router/TanStack/vanilla+autoloader/Vue/Svelte. **ui.kitn.ai is fixed live** (auto-redeploy on the #116 merge). Full detail: memory `consumer-packaging-hardening`.

**★PROCESS LESSONS (reuse):** SSR-safe `void import()` registration is dead-code-elimination-fragile (bit us twice — storybook-static + the elements bundle) → guard built artifacts. **storybook-static can't register web components** → IVP must use `npm run dev` (see `defer-ivp-to-end`). **Serialize `build:api`/generated-file regen + git commits through the orchestrator** — parallel workers raced the react/types generated files and the branch index (recovered, no loss). Run the visual IVP only at the END (Rob's call). Real consumer apps catch packaging bugs the 1269-test unit suite + Vite-tolerant docs all miss.

**NEXT (resume here):** (1) **Monorepo migration** (NX + pnpm workspaces) — Rob is READY; memory `monorepo-migration-plan` has the full plan/structure/decisions. It would kill the docs-site `sync-kit` fragility class. Do via brainstorm→writing-plans→staged. (2) **Examples refresh** (task: stale react/vue/angular/solid examples pin 0.2/0.4 + pre-rename API) — fold INTO the monorepo migration (`workspace:*`); showcase strategy in `monorepo-migration-plan`. (3) Smaller follow-ups: Angular example (no scaffold support, hand-write); document autoloader static-only contract on the export. (4) On `main`, clean except Rob's uncommitted `docs/notes.md`/`docs/notes-native.md` (HIS — committed once on local main `57694ae`, unpushed; leave them).

## 0.0 (SESSIONS 4–5 — superseded by §0.00) — new-components epic continuation + AMUX. ✅ MERGED to main via PR #114. ★Read memory `labs-apps-dogfood` (SESSION-5) + `footprint-reduction-research` + `supervisor-operating-mode`.

**WHERE WE ARE:** sessions 4–5 are DONE and **MERGED to `main`** (PR #114, merge commit `4dbacf1`, 37 commits, merge-method=merge to preserve conventional history for release-please). On `main` now; `feat/new-components-epic` is merged/stale → **branch fresh off `main`** for any new work. Gate at merge: typecheck 4/4 · unit **1225** · build clean · 74 elements.

**SHIPPED (on main):**
- **Composable graduations:** `kai-pane`, `kai-agent-card`, `kai-dialog` (net-new modal), `kai-pane-group` (editor group); toast `warning`/`error`/`info` variants. All registered + drift-guarded slots/parts (build:api → 74 elements).
- **`kai-toast` `appearance` ('pill' default | 'card' = Sonner/macOS, 24rem, outline-button action) + `inverse` + `description`** (+11 tests).
- **React per-element registration:** wrappers lazy-register their OWN element via a client-only dynamic-import thunk instead of the register-all bundle → a `<Button>` consumer ships ~4 KB gz + its chunk, not 162 KB. SSR-safe; `registerAll()` opt-in kept. + **`sideEffects` allowlist** (the prerequisite). Spec/plan under `docs/superpowers/`.
- **Theme:** semantic + tinted-surface + interaction-state color tokens.
- **Labs:** `kai-prompt-dock`, `kai-segmented`, `kai-settings-group`/`kai-setting-item`, token-driven Proofs screens, a Lovable app, `kai-tasks` readonly, the kai-dialog broadcast dogfood, v0 snippet fix.
- **AMUX** (renamed from "Multi-Agent Workspace", `split-workspace.stories.tsx`, story id stays `labs-apps--split-workspace`, sorts LAST in Labs/Apps): an IDE-style multi-agent shell — editor-group tiling (gutter-as-header, group-level maximize, drag-to-split), activity-bar rail (Workspaces + Agents + a kai-file-tree explorer), portaled tab + needs-you context menus, inline rename, ⌥⇧-arrow pane moves, a keyboard-shortcuts kai-dialog, calm "waiting" state on the `ring` token, card toasts. **Full anatomy: memory `labs-apps-dogfood` SESSION-5.**

**★PROCESS that worked (reuse):** RAPID-DEV — Rob drives live in Storybook, IVP DEFERRED (he reviews; resume IVP only when he asks), commit checkpoints to protect work. ONE warm worker reused via SendMessage (~30× this session, serialized on the demo file); PARALLEL workers ONLY for disjoint files. **★Recurring bug class: Solid delegates `click` to `document`, so a trigger's `stopPropagation` can't stop a SEPARATE document click-listener** (a dropdown opened-then-instantly-closed; fix = drop it from the doc-listener + use a portal BACKDROP). Portaled menus escape `overflow-hidden`/`overflow-x-auto` clipping. Prefer kit TOKENS over raw Tailwind colors (theme-editor manageability) — but the theme has ONE blue hue (217°: `ring`/`tool-blue`/`info`), so blue-for-waiting reads like blue-for-working (open question Rob flagged — would need a NEW distinct token).

**NEXT (resume here):** (1) **release-please #105** is open + recomputed with the 37 commits' features → Rob's call to merge/publish the release. (2) **Branch off the updated `main`** for new work. (3) Open gaps: **kai-nav has no per-item close / trailing-action event** (drove hand-rolling the AMUX rail lists — candidate kit feature); the **storybook-chromium advisory a11y suite** carries pre-existing color-contrast failures (a future a11y pass; it is NON-required, not the unit gate); long-deferred **method-doc-rendering**; the AMUX **waiting-blue vs working-blue** distinction (if Rob wants them visibly different → add a distinct token). (4) Optional: dogfood AMUX onto `kai-pane-group`, tokenize remaining AMUX raw colors.

## 0.01 (SESSION 3 — superseded; ✅ MERGED to main via PR #114) the Labs/Apps multi-harness dogfood phase (7 apps) + a kit-gap-driven fix round. ★Read memory `labs-apps-dogfood` instead of the lines below.

**WHERE WE ARE:** session-1/2 work is COMMITTED (16 + 7 commits). On top, this session built the **Labs/Apps showcase phase**, currently **UNCOMMITTED: 28 modified + 12 untracked** (`git status`). Rob reviewed each app live while I orchestrated background sub-agents (supervisor mode).

★★STATE AT SESSION END: gate = **typecheck 4/4 ✓, build-storybook ✓, unit 1214/1215 — ONE FAILING TEST**: `tests/elements/resizable-element.test.tsx` › "size/min/max are reflected onto the panel data-* and flex-basis" asserts `flexBasis === '280px'` but the new resizable clamp yields `'calc(280px)'`. The closeout **evidence-IVP (a background ivp-verifier) was STILL RUNNING at session end** — check its result. ★DO NOT COMMIT until the test is fixed + the IVP checklist is green + Rob sees the diff.

**THE 7 APPS** (`Labs/Apps`, real kai-* elements, gap-finding → showcase-clean): `claude-code.stories.tsx` (renamed from app-prototype; id `labs-apps--claude-code`) · `t3code.stories.tsx` nested project→thread `kai-nav` (status dots on 2 live threads, real t3 favicon via the `icon` image-URL field, `no-conversations`) · `codex.stories.tsx` REBUILT to **Codex Web** (composer-led center column, Ask/Code dual-button, task feed w/ status+PR+diff badges, task detail — NOT a workspace) · `chatgpt.stories.tsx` (history sidebar + canvas via kai-artifact+kai-resizable) · `perplexity.stories.tsx` (kai-source/kai-sources citations, tabs) · `perplexity-pro.stories.tsx` (Assistant/Computer mode toggle, `no-conversations`) · `v0.stories.tsx` = **"Vesper"** fashion editorial (clean white neumorphism, **Playfair Display** base64 @font-face, **grayscale Unsplash fashion photos** base64, **glass-on-photo pills w/ dark text** [neumorphic elsewhere], AW26/2026 dates, artifact `displayUrl='/index.html'`). [A "NOIRE" variant was built for comparison then DELETED — Rob picked Vesper.]

**KIT CHANGES this session (uncommitted, green except the 1 test):** `kai-nav` +children/status/meta (nav.tsx/facade/test/story/slots); **`kai-tooltip`/`kai-source`/`kai-icon`/`kai-status`/`kai-badge` host → `:host{display:inline-flex}`** (the shared `styles.css:20 :host{display:block}` made inline elements block → citation chips stacked, tooltips mis-centered); `tasks-card` checklist bar → `tone="success"` (green); `kai-workspace` new **`no-conversations`** prop (suppress the built-in ConversationList — the recurring "Chats/No conversations yet" leftover); `kai-artifact` new **`displayUrl`** prop (friendly address over a data: blob); `kai-resizable`/`ResizablePanel` **initial-size clamp** to `[min,max]` (`clampBasis` in `src/ui/resizable.tsx` — fixes the snap-to-max; ★this broke the 1 test); icon registry +11 (`src/ui/icon.tsx`, incl. list-filter); `kai-coachmark` anchor → inline-flex. `src/elements/logos/` (brand SVGs + t3.ico — ★NOT excluded from the npm `files` glob; ADD `"!src/elements/logos"` to package.json BEFORE publish). `docs/labs-apps-gap-backlog.md`. **build:api was run** (66 elements; new props in element-meta/types/wrappers/llms; component-meta reset).

**★REMAINING KIT GAPS** (`docs/labs-apps-gap-backlog.md`): Core = a tone/status **`kai-badge`** variant (Codex hand-tints via ::part); a **segmented/toggle-group** control. [workspace-suppress = DONE.] App-specific candidates: inline-citations-in-markdown, kai-artifact version-history + toolbar slot, ConversationList date-grouping/pinned, kai-message overflow + variant pager, kai-suggestions trailing affordance, kai-nav gallery, lightweight generated-artifact card.

**★ROB'S STANDING GUIDANCE this session:** (1) the **kit-vs-custom LINE** — kit provides seams (view-switching, tabs, slots) + components; the DEVELOPER builds bespoke screens; examples build screens as consumer code, NOT kit deliverables. (2) apps showcase **DIVERSITY** (distinct layouts, not one homogenized shell). (3) examples must be **SHOWCASE-CLEAN** (gap boxes were scaffolding → stripped to the backlog doc). (4) ★**DON'T OVERCLAIM** — "done" only when verified-with-EVIDENCE (per-app screenshot/probe checklist) + the commit diff shown BEFORE committing; Rob got weary of repeating this (trust issue — deliver the checklist). (5) supervisor mode (memory `supervisor-operating-mode`): delegate edits to bg sub-agents on DISJOINT files, reuse via SendMessage, verify each; orchestrator runs build:api/gate/commit.

**NEXT (resume here):** (1) **Fix the failing `resizable-element.test.tsx`** (update assertion to the clamped form, or make `clampBasis` emit a bare value when in-bounds; the actual clamp works visually). Re-run unit. (2) **Read the closeout IVP result** + fix any failures (esp. verify the Perplexity Pro Assistant/Computer toggle renders TEXT LABELS — disputed). (3) **Show Rob the commit grouping/diff, then COMMIT** (groups: feat(nav) · fix(elements) inline-flex host · feat(workspace) no-conversations · feat(artifact) displayUrl · fix(resizable) clamp · feat(icon) registry · refactor(labs) the 7 apps+rename+logos+backlog · chore(api) regen). ★Add `"!src/elements/logos"` to package.json `files` FIRST. (4) Then the remaining kit gaps + deferred method-doc-rendering.

## 0.005 (SESSION 2 — superseded by §0.0 above) card WebAwesome pivot + kai-progress-bar + a big interactive-dogfood polish round on `Labs/Apps/Claude Code Desktop`. ALL UNCOMMITTED on `feat/new-components-epic`. Read memory `new-components-epic` (updated) + `storybook-solidjs-first`.

**HOW THIS SESSION WORKED (reuse it):** Rob drives the `Labs/Apps/Claude Code Desktop` interactive dogfood (formerly `Labs/App Prototype`), spotting gaps; I DELEGATE each fix to background sub-agents on DISJOINT files (Rob: "use sub-agents, don't do the work yourself, delegate it out"), and act as ORCHESTRATOR: I wire the shared glue (the per-file `declare module 'solid-js'` JSX intrinsics that must be byte-identical across story files or TS2717; `slots.ts` parts registry; `register-impl.ts`), run **build:api** ONCE per round (no agent runs it — avoids races on the generated files; then `git checkout -- src/components/component-meta.json`), then the gate (4-pass typecheck + `npm test -- --project=unit` + `npm run build-storybook`) + IVP. For bug hunts Rob wants a 2-phase pattern: investigate+fix agents → then IVP agents that report back.

**SHIPPED THIS SESSION (all green, all UNCOMMITTED):**
- **kai-card → WebAwesome single-element model** (Rob's pivot after pointing at webawesome.com/docs/components/card — LESSON: when the user cites a reference, FETCH it). New presentational primitive `src/ui/card.tsx` (NOT the contract `src/components/card.tsx`, which is untouched except dead `trailing` removed): named slots `media`/`header`/`header-actions`/`footer`/`footer-actions` (body=default, each rendered only when filled via a facade MutationObserver occupancy read), `appearance` outlined/filled/plain/accent, `orientation` vertical/horizontal/**responsive**, **`collapse` prop** = the responsive container-query breakpoint (@container can't read CSS vars, so it's baked into a per-instance injected `<style>`), `--kai-card-spacing`, dismissible/clickable/href. ★The injected `<style>` is PER-INSTANCE-SCOPED (`.kc-i-<uniqueId>` prefix) because the SolidJS Card renders in LIGHT DOM (its Components/Primitives story + autodocs page) where unscoped selectors collide across instances. Dropped heading/description/error-message/trailing/actions from the element (feat!). Docs `card.mdx` + sample rewritten.
- **NEW `kai-progress-bar`** element (primitive+facade+story+test): `value`/`max`/`label` + **`tone`** primary/success/warning/error/info (→ the kit tool-hue tokens), `::part(track|fill)`. Used inline by the kai-tasks progress checklist.
- **kai-button** gained `align` (start/center/end → inner-button justify) + `full` (block, full-width). `full`/`align` added to the 4-5 kai-button JSX decls.
- **kai-suggestions** `size` repurposed to md/lg (list-row height); `variant="ghost"` already existed.
- **kai-workspace** rail: `bg-surface` default + `::part(sidebar)` + **`collapse-below`** (auto-collapse via ResizeObserver) + defaults widened (sidebarWidth 22→26, sidebarMinWidth 200→240).
- **kai-coachmark**: reposition on anchor resize (ResizeObserver added to `usePosition`), dismiss on trigger click, **CSS-var color** `--kai-coachmark-bg`/`-fg` (overridable in CSS, default per tone), `tone` expanded to primary/info/success/warning/error, **`arrow` show/hide prop** (default true), and ★the ARROW BUG FIX: the arrow used a COMPUTED style key `[STATIC_SIDE[...]]: '-4px'` which **SolidJS's compiler silently DROPS** (dynamic keys in `style={{}}` object literals are not emitted) → the arrow rendered inside the bubble, same color, invisible, in EVERY placement → fixed with four explicit static side keys. (LESSON: never use a computed/dynamic key in a Solid `style={{}}`.)
- **kai-tasks progress mode**: smaller title, inline progress bar (done/total), strike-through on done, hover micro-animation, done-items hide their description and reveal it on hover/`:focus-within` (grid-rows-[0fr]→[1fr] height trick) + a keyboard-only `focus-visible` ring on the focusable done row (the "inconsistency" Rob saw was retained focus with NO visible indicator — the reveal itself always worked).
- **kai-conversations** per-item **`trailing`** field (right-aligned, ConversationSummary.trailing) + auto short relative-time from updatedAt/lastMessageAt when omitted + `::part(trailing)`. (Reactivity: updates when the consumer re-sets the `conversations` array — new reference; render-time snapshot, not a live ticker.)
- **Overlay anchor-removal teardown**: `usePosition` `onDisconnect` (MutationObserver on document + anchor's root) → coachmark/popover/tooltip/hover-card/dropdown all close + tear down their portal when the trigger leaves the DOM.
- **Transition-on-first-paint flash FIX** (`src/elements/define.tsx`): the shared shadow stylesheet was adopted in `onMount` (AFTER first paint) so `transition-colors` animated from unstyled→resting on load (ghost buttons looked "hovered then un-hovering"). Fixed by adopting the constructable sheet SYNCHRONOUSLY before first paint (attach shadow + adoptedStyleSheets in the render body). Affects ALL elements — verified dark/light + hover still transitions.
- **kai-stat UN-SHIPPED** (Rob: stat is demo-only): removed the `kai-stat` web component + both stories + register/slots entry; KEPT `src/ui/stat.tsx` as an internal SolidJS component (its `part=` attrs are excluded from the slots.test drift scan).
- **SolidJS `Components/*` stories** created for every new primitive (the canonical Storybook surface — see memory `storybook-solidjs-first`): Components/Primitives/{Card, ProgressBar, Status, Nav, Tabs, Icon, Notice, Command} + Components/Elements/{Coachmark, Screen, VoiceOutput}. kai-* element stories STAY in Labs.
- **Code-view snippets**: every element/lab story got a hand-written `parameters.docs.source.code` (HTML for kai-* elements, tsx for SolidJS primitives) — Storybook's auto-generated Solid-JSX source was unreadable. Pattern = `src()` helper, copy from card.stories/chat-slots/button.stories.
- **De-personalization**: "Rob" → **John** (RT→JD, roboncode@gmail.com→john@example.com) across home-replica/user-menu/primitives/workspace-home/app-prototype/avatar/docs. Greeting "Good day, mate".
- **New curated icon**: `message-square` (chat bubble) added to NAMED_ICONS (34 total); the conversation leading marker is now message-square (not a circle).
- **`Labs/Apps/Claude Code Desktop`** (renamed from `Labs/App Prototype`; `src/elements/app-prototype.stories.tsx`, title `Labs/Apps`, story name "Claude Code Desktop"): the interactive dogfood — Home/Code tabs swap the main slot, Design opens the kai-screen takeover, sidebar collapses, account menu, a **command-center palette** opened by the search button (kai-command, realistic grouped commands, dark `bg-black/50` backdrop), full-width toggle on the LEFT (functional `toggleSidebar()`) + search on the right. ★Element array/object props are set in `ref` CALLBACKS (not a single `onMount`) so they re-apply when the Home view unmounts/remounts on tab-switch (the Show-toggle data-loss bug). Coachmark colored via CSS vars (persists across remounts).

**Verification gates (run every round):** `npm run build:api` (→ `git checkout -- src/components/component-meta.json`) · `npm run typecheck` (4 passes) · `npm test -- --project=unit` · `npm run build-storybook`. Then restart Storybook + IVP. ★Storybook on :6006; the prototype id is now `labs-apps--claude-code-desktop`. ★IVP scripts in the SCRATCHPAD with repo node_modules symlinked; `deviceScaleFactor:1` if a shot exceeds the 2000px read limit; for interactions (open command center, hover a checklist row, open the coachmark) drive playwright + read the shot back. ★A NEW arbitrary Tailwind class (grid-rows-[0fr], @container, @min-[28rem], etc.) needs a Storybook RESTART (build:css) before it's in the shadow `compiled.css` — MANY "not working" reports this session were just stale compiled.css.

**★HARD LESSON (repeated, Rob caught me): DO NOT OVERCLAIM.** I twice said the coachmark arrow "renders / is fine" from eyeballing my own screenshot — it was NEVER rendering (the Solid dynamic-style-key bug). Verify a fix with a ZOOMED screenshot read back AND a computed-style probe, not a glance; cite the agent's evidence, not a vibe.

**NEXT / OPEN (Rob's call):**
1. **COMMIT** this round (83 files). Suggested grouping: `feat(card)!:` WebAwesome model + collapse + scoping; `feat: kai-progress-bar`; `feat(button): align + full`; `feat(coachmark): css-var color + tone presets + arrow prop + arrow fix`; `feat(workspace): sidebar bg/part + collapse-below`; `feat(conversations): trailing`; `fix(overlay): close on anchor removal`; `fix(elements): adopt shadow sheet before paint (no transition flash)`; `refactor!: un-ship kai-stat`; `docs(storybook): SolidJS Components/* stories + code snippets`; `refactor(labs): Claude Code Desktop app + de-personalize`. (Squash-merge each PR; the squash title's conventional type drives release-please.)
2. **T3 Code mock** — Rob wants a second `Labs/Apps/` app (`Labs/Apps` → "T3 Code") mocking the T3 Chat desktop (image was shared) to prove the kit is not Claude-only. DEFERRED follow-up phase.
3. Still DEFERRED from before: method-doc-rendering into gen-web-components-md/gen-llms/kai-MCP/element-types + docs-site updates (§0.05).

---

## 0.01 (session 1, 2026-06-27) — new-components epic + dogfood gap-fill + the interactive App Prototype (pre-card-pivot). Read memory `new-components-epic`.

**★READ memory `new-components-epic` first (authoritative for this branch).** Spec: `docs/superpowers/specs/2026-06-26-new-components-design.md`. Branch off `main`@7fa2b17, 12 feature commits + this handoff, NOTHING pushed.

**What's built (each verified: 4-pass tsc + unit 1148/1148 + drift + build-storybook + light/dark IVP):**
- NEW elements: `kai-status`, `kai-screen` (full-bleed overlay = the Design takeover; public prop is `headline`, NOT title), `kai-voice-output`, `kai-tabs` (now also `block` = full-width), `kai-nav` (sidebar nav), `kai-stat` (dashboard tile), `kai-coachmark` (anchored hint bubble + arrow; `tone` = primary | info-blue). build:api emits **66 elements**.
- EXTENDED: `kai-voice-input` (native Web Speech), `kai-card` (dismiss/clickable), `kai-conversations` (rail collapse), `kai-workspace` (sidebar-header / sidebar-footer / **main** injection slots + `compact` Recents), `kai-suggestions` (`layout=list`), `kai-tasks` (`mode=progress` checklist), `kai-menu` (radioGroup sections). + the user-menu recipe + 10 app-shell icons (home/clock/lock/box/briefcase/panel-left/circle/sliders-horizontal/workflow/square-pen → 33 curated).
- **`Labs/App Prototype`** (`src/elements/app-prototype.stories.tsx`) — a WORKING interactive Claude-app prototype on kai-workspace: Home/Code swap the `main` slot via kai-tabs; the Design kai-button opens the kai-screen takeover; the rail collapses; the account dropdown + filter are real kai-menus. Styled with **Tailwind classes** (preview `@source`-scans src/). IVP states saved in the scratchpad: proto-home/code/design/collapsed/menu.

**★IN THE MIDDLE OF — the App Prototype is the gap-finding deliverable. OPEN GAPS it surfaced (the real output to act on):**
1. **kai-workspace does NOT forward the conversation-list header** — it is hardwired to "Chats" + search. The underlying `ConversationList` HAS `header`/`footer` slots, but the workspace does not pass them through, so you cannot relabel it "Recents" or put the filter/group-by menu there. ★Clearest, most-actionable gap.
2. **kai-button has no toggle/pressed state** — the Cowork pill is a selected toggle; kai-button renders plain. Decide: an `active`/`aria-pressed` on kai-button, OR a separate toggle component? (Rob: if a component can't do it, ask whether it SHOULD or it's a different component — do not bolt on.)
3. **kai-coachmark portals over kai-screen** — when the takeover opens, the body-level coachmark portal floats on top (the screen inerts DOM siblings, not portals).
4. **Code view partial** — kai-stat tiles work; the contribution heatmap (app-specific) + the Code composer's "Accept edits / Local / Select folder" (would compose `kai-file-tree`) are unbuilt.
5. Sidebar cramping (tabs+nav+recents+checklist+design+usermenu squeezes Recents). Still DEFERRED from earlier: **method-doc-rendering** into the text generators (see §0.05) + docs-site updates.

**★DOCS IDEA (Rob, 2026-06-27 — for the docs-site at ui.kitn.ai; leave Storybook as-is for now):** document the **anchored-overlay / hint FAMILY** as one grouped set with a "which one do I use?" comparison, because the distinction is NOT obvious (Rob: "I didn't know that"): `kai-tooltip` (hover/focus hint, no dismiss) · `kai-popover` (click-triggered floating content) · `kai-hover-card` (rich content on hover) · `kai-coachmark` (anchored onboarding announcement = badge + dismiss, "new feature here"). The differentiator is **trigger + intent + dismiss semantics**. Adjacent: `kai-menu` (anchored actions dropdown). Ship as an "Overlays & hints" page/section with a side-by-side decision table. Fold into the deferred docs-site updates. (FWIW "coachmark" IS the real UX term for that pattern — one of several: callout / hint / spotlight / beacon — kept as `kai-coachmark`.)

**★HARD LESSONS this session (Rob got genuinely angry — DO NOT repeat):**
- **Do NOT overclaim.** Presenting a faked/partial result as "done / faithful / verified" reads as lying and destroys trust. IVP it and SHOW it before any claim; never call something done that is not the thing asked.
- **The dogfood test = build the screen WITH the components, as a consumer, to FIND GAPS.** Styling divs to *look* like the screenshot defeats the purpose (it hides the gaps). Use kai-workspace + the real elements; hand-write only layout glue; the moment you would reach for a raw div to fake behavior, that is a gap to log.
- **LOOK at the component, do not guess its API.** e.g. kai-button `label` is the aria-label; visible text is the children. Read `src/elements/element-meta.json` / the facade before asserting a capability or a gap.
- Use the components we already have (kai-workspace, kai-button, the user-menu recipe, kai-screen for Design). Routing around them was the cardinal sin here.
- Gotchas still hold: RESERVED prop names (`title`/`id`/`slot`/`lang`) throw at registration; empty `<slot>` is always truthy → gate with `readSlots`; stories import from `storybook-solidjs-vite` + declare each kai-* JSX intrinsic (two stories declaring the same tag = TS2717); a UI-component prop named like a native handler (onSelect/onChange) collides with HTMLAttributes (TS2430) → name it distinctly (onItemSelect); `git checkout -- src/components/component-meta.json` after `build:api`; new element / shadow-CSS / new-story needs a Storybook RESTART; write IVP scripts to the SCRATCHPAD with repo node_modules symlinked, `deviceScaleFactor: 1` if a shot exceeds the 2000px Read limit.

**Resume:** Storybook may still be running on :6006 (else `npm run storybook`). Next likely pieces (Rob's call): close gap #1 (forward kai-workspace's list header/footer + a filter menu), decide gap #2 (kai-button toggle vs a toggle component), or push the branch / open the PR. The merged history is §0.05 below.

## 0.05 (2026-06-26) — ★MERGED to main. Interaction-API rollout + Storybook overhaul shipped. Read memory cont. 40.
**★READ memory `composition-seams-direction.md` — cont. 40 is the authoritative current state.** Everything below in this doc (§0.5 onward) is older history; trust the memory.

**Where we are: the whole initiative is MERGED to `main` via two squash PRs.**
- **#111** (`0efc204`, `docs:` → no bump) — building-block component pages + auto slot/part tables (the original subject of this doc).
- **#112** (`7fa2b17`, `feat:` → minor bump) — the COMPLETE interaction API on every interactive `kai-*` element + the full Storybook overhaul + `fix(switch)`.
- ★Both SQUASH-merged on purpose (one revertable commit per PR — Rob's call). The squash title's conventional type drives release-please, so #111 was `docs:`, #112 `feat:`.
- **release-please PR #105 is OPEN: `chore(main): release @kitn.ai/ui 0.18.0`.** Merging it cuts the GitHub release + `npm publish`. ★Rob's call — left untouched.

**Interaction API — DONE for ALL interactive elements** (lightweight Shoelace/WebAwesome model; NOT React-controlled). Foundation in main: `src/elements/define.tsx` `ctx.expose`; `src/elements/disclosure.ts` `wireDisclosure(ctx, getApi, openProp)`; `gen-element-api.mjs` `HELPER_METHODS`. SPEC `docs/superpowers/specs/2026-06-26-interaction-api-audit.md`; agent guide `docs/superpowers/plans/2026-06-26-interaction-api-rollout-guide.md`. Every interactive element now carries its events/methods/props (overlays: open/defaultOpen/kai-open-change/show/hide/toggle/disabled; form controls: controlled value/checked; collapsibles: show/hide/toggle; cards: card-contract focus/select/send/dismiss/reopen; chain-of-thought accordion type/value/defaultValue/kai-value-change; artifact back/forward/reload/home/navigate/selectFile/openExternal/maximize/restore; voice start/stop). Regenerated into element-meta + custom-elements.json + React wrappers + llms.

**Storybook overhaul — DONE:** events wired to the Actions panel + real-value Code snippets across every story (helper `src/stories/docs/story-actions.ts` `attachKaiActions` auto-logs a kai-* element's declared events); complete Token Reference (doc-consistent typography/title/width + theme-editor link); AI/UI brand logo (magenta "/"); the dead Usage tab REMOVED (api-tab.tsx + framework-usage plumbing gone); KISS dev-audience rewrites of Getting Started + the Overviews; every element/primitive description trimmed; em dashes purged repo-wide (AI tell) with correct punctuation; `text-wrap: pretty` + tighter inline-code chips (no orphaned punctuation); Chat Slots demos fill the canvas; switch dark-mode thumb fix (`bg-primary-foreground`).

**★DEFERRED — Rob: "not yet" (he has other things to work on first; we WILL return to these):**
1. **Method doc-rendering** — methods land in element-meta + CEM only; the TEXT generators don't emit them yet. Extend `scripts/gen-web-components-md`, `scripts/gen-llms`, the `kai` MCP `component_reference`, add a docs-site `MethodTable.astro`, and emit method signatures into `element-types.d.ts`.
2. **Docs-site updates** for the new interaction API (not yet).
3. Minor: chain-of-thought/voice deeper IVP; kai-switch cross-shadow `<form>` `formAssociated`/ElementInternals pass.

**★Reusable process:** parallel background agents on DISJOINT files (source-only, tsc-verify, structured report) → consolidate per wave (build:api + 4-pass tsc + browser IVP each + unit/drift/build-storybook + one commit). PR strategy: squash-merge each PR for one-command revert; set the squash title's conventional type so release-please versions right.

**★LESSONS this session:** (a) re-run the FULL unit suite (`npm test -- --project=unit`) after DELETING source — removing the Usage tab orphaned `tests/scripts/framework-usage.test.ts` (it imported the deleted `gen-framework-usage.mjs`); CI caught it, I'd missed it because my earlier full run predated the deletion. (b) when stripping a feature, grep the WHOLE repo (`tests/`, `.storybook/`) for refs. (c) `git rebase --onto <new-base> <old-base>`: `<old-base>` must be the branch's true fork point (parent of the first session commit), not a commit mid-branch — using a mid-branch SHA replays already-merged commits and conflicts. (d) em dashes read as an AI tell — see memory `docs-copy-voice` (KISS, dev audience).

**★STORYBOOK/DOCS DEV:** Storybook `npm run storybook` :6006 (RESTART for new elements/shadow-CSS/storySort/.storybook config — won't HMR); docs `cd docs-site && npm run dev` :4321. `build-storybook` = compile gate; `cd docs-site && npm run build` = docs gate. After any kit build: `git checkout -- src/components/component-meta.json` (churn). ★Write IVP/screenshot scratch scripts to the session SCRATCHPAD, NOT the repo (symlink repo node_modules into it so playwright resolves). ★IVP: elements have no Storybook stories → `document.createElement('kai-x')` in any kit iframe (`labs-foundations--icons`); voice needs `--use-fake-device-for-media-stream` + grantPermissions; cards' `type` is the LOGICAL key (confirm/choice/form/tasks), not the raw tag.

--- (historical, pre-merge) ---
Done after the `c1981e9` snapshot below. Commits `04257ca`→`674ab7b` are **pushed to `origin/spike/kai-chat-seams`**. Element count is now **59**.

**Also added — 4 more composition primitives (thin facades over existing `src/ui` primitives):**
- `kai-separator` (divider, H/V, `::part(separator)`), `kai-scroll-area` (themed thin-scrollbar container, `::part(viewport)`), `kai-hover-card` (rich hover content via `slot="card"` — the markup-carrying sibling of `kai-tooltip`; slot-into-portal verified), and `kai-skeleton` (responsive loading placeholder; `variant` text/rect/circle + `width`/`height`/`lines`, prop-driven because classes can't cross the shadow boundary).
- The `Skeleton` PRIMITIVE gained the variant/responsive API (backward-compatible) and is now **dogfooded** in `link-preview` + `components/image`. (`kai-image`'s fallback stays unsized/`h-auto` ON PURPOSE — Rob's call: base64/bytes decode in-memory near-instantly so the skeleton barely shows, and we can't know the image's dimensions, so reserving guessed space would only risk a layout jump. Don't "fix" it.) All four verified rendering in light + dark via `_shot.mjs`. Doc headings authored in `web-components.md` for the 11 newer composition/interactive elements so their Slots/Parts tables inject. Decision (Rob): skeletons belong as a LOW-LEVEL responsive primitive, NOT content-shaped presets (message-skeleton etc. = the Cartesian-product trap).

> ★ GOTCHA REMINDER for screenshots: adding a NEW element needs a Storybook RESTART (`lsof -ti:6006|xargs kill; npm run storybook`) before `_shot.mjs` can see it.

**Shipped — `::part`/slot discoverability (queue item #2, now DONE):**
- `slots.ts` is now the single registry the build extracts: added `ELEMENT_COMPOSITION` (tag → `{slots, parts}`) + `BUTTON_PARTS`/`BADGE_PARTS`/`ICON_PARTS` (the parts that existed only in facade code). A `slots.test.ts` **drift guard** scans every `part="…"` in `src/` and fails if one isn't registered.
- `gen-element-api.mjs` reads `ELEMENT_COMPOSITION` from the AST (can't `import` TS under plain node) and emits **CEM-standard `slots` + `cssParts`** (with a `recipe` extension) into `custom-elements.json`, plus `slots`/`parts` into `element-meta.json`. Slots flagged `part:true` also become parts.
- Surfaced in: `docs/web-components.md` (Slots + Styleable-parts tables), `llms-full.txt`, and the **`kai` MCP `component_reference`** (new "Composition slots" + "Styleable parts (`::part`)" sections with the copy-paste recipe). 22 node tests green (15 slots + 7 reference).

**Fixed — pre-existing P0: `build:api` was crashing (stack overflow).** `renderType` in `scripts/_ts-helpers.mjs` inlined every named type with no cycle guard, so the self-referential `KaiMenuItem.items` / `FileTreeNode.children` recursed forever. Broken since `kai-menu` landed → `element-meta.json` was **8 elements stale** (missing avatar/badge/button/command/icon/menu/notice/tooltip). Added a `seen`-set cycle guard (a self-reference truncates to `Record<string, unknown>` — self-contained, tsc-valid). `build:api` now emits **55 elements**; **typecheck 4/4 green**. The regen brought the 8 missing elements into the React wrappers / `element-types.d.ts` / docs / llms — a large but correct diff.

**Test status (verified):** the only real failure my regen caused was `tests/stories/element-controls.test.ts` (proseSize) — TS union member order tracks the checker's global type-id ordering and shifted when 8 elements were added; fixed by asserting the option SET (order is cosmetic). **Pre-existing, NOT mine (don't chase):** the Storybook *vitest browser* project is flaky under full parallel load (failure count varied 46↔80 across identical runs; story files pass in isolation on clean HEAD too), and `chat-slots › Inject` has an axe a11y violation that reproduces on clean HEAD. Neither is the commit gate (§5 = `tsc` + the IVPs).

**New follow-up:** `docs/web-components.md` has no authored headings for the 8 new elements, so the generator can't inject their Slots/Parts tables there (the CEM + MCP + `llms-full.txt` DO cover them). Author headings to complete the human docs.

---

## 0. TL;DR — what to do next
Nothing is half-finished. Pick from the queue in §6. The likely next pieces, in order of leverage:
1. ~~**Extend the `slot="icon"` escape hatch**~~ — **`kai-notice` DONE** (slotted SVG overrides the severity glyph; via `noticeIconNode` + the primitive's `iconSlot`). `kai-suggestions` deliberately NOT done — its chips are data-driven (an `items` array / `<kai-suggestion icon>` data carriers rendered in shadow), so there's no single icon to slot; its `icon` field already accepts a data-URI/URL SVG as the consistent escape hatch.
2. ~~**`::part` discoverability**~~ — **DONE** (see §0.0). Remaining sub-item: author `docs/web-components.md` headings for the 8 new elements so their Slots/Parts tables get injected.
3. ~~**Surface-token migration**~~ — **DONE**. Migrated 11 static surfaces across tool/attachments/composer/conversation-list/artifact/form/file-upload to `bg-surface`/`-strong` (nested boxes)/`-sunken` (artifact sidebar+canvas). Left hover states, the outline-button control bg, conversation-item active, the artifact readonly-input state, and demo/story code. Verified tool light+dark (depth hierarchy reads correctly).
4. **The real graduation** (in progress): audit-driven production rollout. **`kai-conversations` DONE** (header replace / footer inject / empty replace, registered + discoverable, verified light+dark). Next P1: `kai-workspace`, then `kai-message` (the compose-your-own keystone: `before-body`/`after-body` inject + `avatar` replace). Unblocks issue #106. Spec: `docs/superpowers/specs/2026-06-23-composition-seams-audit.md`.

**Behavioral musts (Rob was burned by these — §7):** SHOW screenshots and let Rob judge; never overclaim "done"; drive visual work in a tight edit→`_shot.mjs`→Read loop; no emoji.

---

## 1. The big picture
Composition-over-configuration for `@kitn.ai/ui` (Shadow-DOM web components authored in SolidJS, consumed from any framework; elements prefixed `kai-`). Rob's thesis: **configuration forces US to author the Cartesian product of layouts×use-cases — unbounded and not ours to know.** So we expose **slots (positions) + well-made parts + tokens/`::part`**, and the consumer composes. Our job shrinks to "render parts well + add parts as new AI-UX patterns emerge." Reference target is Claude's composer (dark card: a "model unavailable" notice, input, a `+` menu, model+effort+mic+voice, suggestion chips).

---

## 2. What shipped THIS session (7 commits over `4af1ff7`)
- **`a236ed4`** `feat(prompt-input)!`: **slot rename** — `notice` *removed* (outer content = the dev's own layout, or `kai-chat`'s `composer-actions` when nested), `leading`→`input-top`, `toolbar-start` kept, `trailing`→`toolbar-end`. **`submit` trimmed** to `'always'|'auto'` (`never` → CSS `::part(send){display:none}`). Added **`PartDef`/`PROMPT_INPUT_PARTS` registry** in `src/elements/slots.ts` (declares the `send` part + a copy-paste recipe) and a `<kai-prompt-input>` composition diagram story.
- **`5a258e6`** `feat(theme)`: **calculated opaque surface tokens.** `--color-surface` / `--color-surface-strong` / `--color-surface-sunken` in `theme.css`, derived via `color-mix(... var(--color-muted) N%, var(--color-background))` — opaque (no bleed-through like `bg-muted/40`), theme-aware. **Declared in BOTH `@theme` (light) AND `.dark`** (a derived custom property resolves where it's *declared*, not used — light-only = wrong color in dark). `@utility bg-surface / bg-surface-strong / bg-surface-sunken`. Migrated `prompt-input.tsx` card `bg-muted/40` → `bg-surface`.
- **`9513dd1`** `feat(elements)`: **`kai-button`** (`src/elements/button.tsx` over the `Button` primitive; `variant` incl. new `subtle`, `size`, `icon`/`icon-trailing`, `label`, `kai-click`, `::part(button)`); **`kai-menu` trigger props** (`trigger-icon`/`trigger-label`/`trigger-icon-trailing`/`label` — slotted `trigger` still wins); **`kai-suggestions` per-item icons** (`{label,icon}` + `<kai-suggestion icon>`; `PromptSuggestion` renders a leading icon); **dropdown** checkbox items → TRAILING check (so an icon'd item aligns) + submenu chevron full `size-4` (a `pl-2` was shrinking it). Icons added to `src/ui/icon.tsx`.
- **`56ce22c`** `refactor(composer-showcase)`: banner became a **flow container** (dropped absolute/z-index/margin peek + the focus-ring overlap); all controls became kit elements; **all inline styles → Tailwind classes** (0 inline styles, 0 hand-rolled buttons); dropped hard-coded `theme="dark"`; `default-input` toolbar `pb-3`→`pb-0`.
- **`8ab6b6d`** `feat(elements)`: **`kai-avatar`** (image + initials fallback, sm/md/lg), **`kai-badge`** (default/count/citation, `::part(badge)`), **`kai-tooltip`** (hover/focus hint wrapping a slotted trigger, portaled in shadow). Thin facades over existing `src/ui/` primitives.
- **`65ee396`** `feat(elements)`: **`kai-notice`** (new `src/ui/notice.tsx` + facade; `severity` neutral/info/warning/error/success → colored icon via the `tool-*` hues + a11y `role` alert/status; `icon` default/`none`/custom; self-dismissing `×` → `kai-dismiss`; message + `slot="action"`); **`kai-icon`** (curated icons standalone; theme foreground; recolor via `::part(icon)`); **`kai-button` `slot="icon"`** escape hatch (any inline SVG; wins over the `icon` prop). + Notices/Icons stories.
- **`9670eef`** `refactor(composer-showcase)`: dogfood `kai-notice` for the banner (removed the hand-rolled div + dismiss wiring; now a stacked `<kai-notice severity="warning">` above the composer — dropped the behind-the-card peek; the component owns its box, the dev owns placement).

**Element count: 55.** tsc clean (run `npx tsc --noEmit`). IVPs green (`npm run test:slots-ivp`, `test:menu-ivp` — 7/7 each), `tests/ui/dropdown.test.tsx` 7/7, `slots.test.ts` 12/12.

---

## 3. Settled philosophy (the rules — DO NOT relitigate)
1. **`slot` = position · `::part`/token = styling · a prop ONLY for behavior CSS can't express.** (`submit="auto"` needs JS to know there's content; "hide send" is pure CSS.) This says *trim, not add*.
2. **A slot earns its place only where the shadow boundary blocks the consumer.** Outer content (above/below the card) is the dev's own light-DOM layout — that's why the prompt-input has no outer `notice` slot.
3. **Layout stays the dev's (Tailwind/CSS).** REJECTED `kai-flex`/`kai-layout`: flexbox is universal, not kit-specific; a layout component just re-types flexbox with worse ergonomics + shadow friction. We own *parts*, not layout.
4. **Expose polished primitives as elements** (the `kai-button` pattern): if a hard-to-get-right primitive (theming/a11y/hover/shadow) exists but isn't exposed, consumers hand-roll it → gap. That's why we shipped button/avatar/badge/tooltip/notice/icon.
5. **A notice/alert IS a component** (behavior + semantic severity + a11y role) — unlike layout. Provide one; devs can still hand-roll bespoke ones (both coexist).
6. **Icons — three tiers, not locked to any library:** `icon="mic"` prop (curated, on elements) · `<kai-icon name="mic">` (curated, standalone) · `slot="icon"` inline SVG (ANY library, inherits `currentColor`). Curated set lives in `src/ui/icon.tsx` `NAMED_ICONS` (~23); it's deliberately small (AI-UX vocabulary), not a general library — hold that line; the slot is the escape hatch.
7. **No hardcoded colors / no `theme="dark"` in stories** — everything is `var(--color-*)` and follows the toggle.
8. **These are PROTOTYPES** (proven, not production-polished). Production graduation (audit P1 `kai-conversations`) is a later, separate effort.

---

## 4. The composer showcase (current state)
`src/elements/composer-showcase.stories.tsx`, story `Spikes/Composer (production)` → `Composer` (id `spikes-composer-production--composer`). Fully component-driven, Tailwind classes only:
- `<kai-notice severity="warning">` above the composer (self-dismiss + Learn-more action slot).
- `<kai-prompt-input>` with `<kai-menu slot="toolbar-start" trigger-icon="plus">` (the `+`) and a `slot="toolbar-end"` cluster: `<kai-model-switcher>` · `<kai-menu trigger-label="High" trigger-icon-trailing="chevron-down">` · two `<kai-button variant="subtle" size="icon-sm">` (mic/voice).
- `<kai-suggestions>` below (per-item icons: pencil/book-open/code/smile).
- Data (menu items, suggestions, model list) set as JS props via refs in `onMount`; events wired via `addEventListener` there.
Primitives demo: `src/elements/primitives.stories.tsx` → `Spikes/New Primitives` → `Avatars`/`Badges`/`Tooltips`/`Notices`/`Icons`.

---

## 5. Environment / how to continue
- **Worktree:** `/Users/home/Projects/kitn-ai/kitn-chat-spike-kai-chat-seams`, branch `spike/kai-chat-seams` (sibling worktree; `node_modules` symlinked).
- **Storybook:** `npm run storybook` (port 6006; runs `build:css` then `storybook dev`).
- **Screenshot loop:** `_shot.mjs` in the worktree root (untracked; recreate if missing — see below). `node _shot.mjs "<iframe url>" spike-screens/x.png dark 920 740 kai-prompt-input` then **Read** the PNG. `spike-screens/` is gitignored. Add `.dark` to `<html>` for dark mode (preview.ts syncs `theme="dark"` onto kai-* elements). For other elements pass a different `selector` arg; for interactions (open a menu, hover a tooltip, click dismiss) write a one-off inline `node -e "import('playwright')…"`.
  ```js
  // _shot.mjs — args: url out [mode=dark] [w=920] [h=700] [selector=kai-prompt-input]
  import { chromium } from 'playwright';
  const [url, out, mode='dark', w='920', h='700', sel='kai-prompt-input'] = process.argv.slice(2);
  const b = await chromium.launch(); const p = await b.newPage({ viewport:{width:+w,height:+h}, deviceScaleFactor:2 });
  await p.goto(url,{waitUntil:'networkidle'}).catch(()=>{}); await p.waitForSelector(sel,{timeout:25000}).catch(()=>{});
  if (mode==='dark') await p.evaluate(()=>document.documentElement.classList.add('dark'));
  await p.waitForTimeout(2000); await p.screenshot({path:out}); await b.close();
  ```
- **⚠️ THE BIG GOTCHA — shadow CSS needs a Storybook RESTART.** `kai-*` elements inject `src/elements/compiled.css?inline` into their shadow root; `compiled.css` is built by `build:css` at Storybook startup and **Vite does NOT re-inline it on change**. So when you change a shared component's classes, add a new element, or add an `@theme` token / `@utility`, you must **kill + relaunch Storybook** (`lsof -ti:6006 | xargs kill; npm run storybook` in the background) to see it in the shadow DOM — HMR alone won't. (The Storybook *preview* CSS, `.storybook/styles.css`, DOES hot-reload, which is why page-level/light-DOM classes appear without a restart, misleadingly.) Build the new element's classes from existing utilities where possible to minimize surprises.
- **⚠️ Events from a SolidJS parent:** consuming a `kai-*` element's events in a story uses **`addEventListener` in `onMount`** (not Solid `onClick`/`on:` — delegation doesn't cross the shadow boundary; native click bubbles composed but the delegated handler still misfired). React/Vue consumers get `onKai*` props from the generated wrappers.
- **⚠️ Per-story JSX decls:** each story file `declare module 'solid-js'`-augments `IntrinsicElements` for the `kai-*` tags it uses. The SAME tag declared in two story files must be **structurally identical** or tsc errors (`TS2717`). Keep them in sync.
- **Tests before commit:** `npx tsc --noEmit`; the relevant `npm run test:*-ivp`. After `build:api`, `git checkout -- src/components/component-meta.json` (churns; not used at runtime). `build:api` was NOT run this session.
- **A faulty-test lesson:** verifying a dismiss, I checked the wrong `<div>` (the container, whose `textContent` started with the banner's) and wrongly concluded it was broken — it worked. When verifying DOM state, target the exact element (the control's `parentElement`, the shadow node), not a `find()` by text.

---

## 6. Queued work (none urgent; pick by leverage)
- ~~Extend `slot="icon"` to `kai-suggestions` and `kai-notice`~~ — **`kai-notice` DONE; `kai-suggestions` N/A** (data-driven chips; data-URI icon string is its escape hatch).
- ~~**`::part` discoverability**~~ — **DONE** (see §0.0): `ELEMENT_COMPOSITION` registry in `slots.ts` → `gen-element-api.mjs` AST-extracts it → CEM `slots`/`cssParts` + `element-meta` slots/parts → docs tables + `llms-full.txt` + the `kai` MCP `component_reference`. Drift guard in `slots.test.ts`. Remaining: author `docs/web-components.md` headings for the 8 newly-regenerated elements (avatar/badge/button/command/icon/menu/notice/tooltip) so their Slots/Parts tables inject.
- ~~**Surface-token migration**~~ — **DONE** (11 static surfaces → `bg-surface`/`-strong`/`-sunken`; hover/state/control/demo left as-is). Remaining `bg-muted/NN` are all intentional (hover tints, outline-button bg, conversation-item active, artifact readonly input, story snippets).
- **Production rollout** (the real graduation): **`kai-conversations` DONE** (header/empty replace + footer inject via the `readSlots` flag-gate pattern; `CONVERSATIONS_SLOTS` in slots.ts mirrors `kai-chat`'s names). Next: `kai-workspace` (sidebar-header/footer/sidebar-replace/footer), then `kai-message` (`before-body`/`after-body` inject + `avatar` replace). Unblocks issue #106. NOTE: also fixed a gen-llms bug — `fromElements`/`fromManifest` dropped slots/parts, so `llms-full.txt` never rendered them; now it does.
- Deferred minors in `.superpowers/sdd/progress.md` (gitignored, in the worktree): W4b composer→CommandList DRY; `gen-element-api` slot/part-table emission; misc a11y/cosmetic.

---

## 7. Lessons / how to work (Rob's, heed these)
- **Do NOT overclaim "done/polished."** SHOW screenshots vs. the reference; let Rob judge. Only call it good when it visibly is.
- **Drive visual work DIRECTLY** in a tight edit→`_shot.mjs`→Read loop. Verify in BOTH light and dark.
- When you fix something, **verify it in the real running app**, targeting the exact element (see the faulty-test lesson, §5).
- **No emoji, ever** (kit convention). Use lucide / `renderIcon`.
- Lead design forks with a decisive recommendation; reserve modal questions for genuinely strategic choices. Rob is the product owner and decides surface-area additions.
